# حالة بناء الذرات — نقطة الاستمرار (اقرأها لتكمّل من هون)

> **لأي محادثة جاية على مسار الذرات:** اقرأ **`قالب الذرة.md` (الدستور، ٢٣ قاعدة)** +
> **`دورة حياة النظام (التصميم الكامل).md`** + هذا الملف، وكمّل. كل ذرة **مكتفية بذاتها** (atom.py +
> manifest + الشرح + التاريخ + tests) — تُقرأ بلا محادثة.
> **المالك:** محمد رزوق · **آخر تحديث:** 2026-08-05.

## ✅ تحقّق حيّ على الحساب الحقيقي (2026-08-05) — الفحص ٢ الإلزامي (يُقرأ أول)
- **🔴 المعيار الحاكم (قرار المالك · بالدستور):** **«خلصت» = مرقت الفحص ٢ = حيّ على النواة الحقيقية + بيانات حقيقية + المالك شايف.** المدقّق والاختبارات-الموك **أرضية تكذب بالاتجاهين، مش إثبات.** (الدستور مُحدَّث: البوّابة الفحص ٢ إلزامي · قاعدة ٩ «الموك أرضية» · ثغرة الباش بقاعدة ٢١ أُلغيت — لا باش حتى داخليًّا.)
- **٦٥ ذرة · شُغّلت حيّ على النواة الحقيقية + جسر MT5 + حساب Exness 474099934 ($184k).** طريقة التشغيل: نافذة PowerShell ظاهرة `run_core.py` (الـhot-reload يلقط التعديلات كل ٥ث) · الصحة من `:8010/api/atoms`.
- **مقيس حيّ:** تكّات حقيقية تتدفّق (618: 40k+ · 101: 120k+ موحّدة · 103: 640+ شمعة على ٨ رموز)، وعمود التحليل (150) يحلّل **٨ رموز حقيقية** ويُخزَّن على القرص (712 stored حيّ).
- **✅ أصلحت هالجلسة (مثبَت حيّ، لا موك):** **611** (`BRIDGE_UNREADABLE`→`NO_READ_YET` · فحص وجود جدول `trade_events` بدل ترجمة غيابه خطأ · v2.1.0) · **وصل 712←166** (712 يسمع `analysis.raw.completed` الآن بدل `analysis.consolidated` · v2.1.0 · التحليل يُخزَّن فعلًا) · **اختبارات الجسور محكمة** (601/609/611/618: `os.environ.pop('NQ_BRIDGE_DB')` بأعلى الاختبار — كان جسر الإنتاج يتجاوز مسار الاختبار).
- **snapshot/restore:** فقط 007/611/618 (مطابق القرار).

## الطريقة (محسومة)
**قبر القديم + بناء نظيف على الدستور** (مو ترقيع). الكود القديم = مصدر معرفة يُحصد، لا يُرقَّع.
الجوّا (كود/مانيفست) إنكليزي خام · البرّا (اسم مجلد + الشرح + التاريخ) عربي.

## ✅ المبني (٦٥ ذرة · بمشروع `atoms/`) — كلها على الدستور · **مثبَتة حيّ على الحساب الحقيقي** · **مدقّق شامل ٠ مخالفة** · **بلوك 700 + بلوك 100 مكتملان · بلوك 150 التحليل: ١٥ محلّلًا حيّ (العمود كامل 151..165)**
**بلوك 000 — البنية التحتية (٥):**
- `003_الساعة` — مصدر الوقت الرسمي (نبضات) · صاحب ساعة.
- `608_مزامنة_الوقت` — قياس انحراف NTP (`time.utc.synced`) · صاحب ساعة + شبكة مسموحة.
- `806_نبضة_الوقت` — نبضات دورية (SYS_SECOND…DAY) · صاحب ساعة.
- `006_مراقب_التخزين` — مساحة القرص.
- `007_سلامة_الملفات` — كشف التلاعب بـ**بصمة محتوى SHA-256** للمنطقة المختومة `core/` + ملفات `config/` (يكشف تعديل بمكانه · **يتجاهل ضجيج `__pycache__`/`.pyc`**) · نشر مستمر · لا نسيان تلقائي · **snapshot أساس أمني — مُبقى**. `atoms/` يُضاف عند تجميد الذرات. **3.0.0 · ٧/٧.**

**الجسور (أرقام 600 · الطبقة المنطقية «جسور» · نسخة لكل أنبوب/طرفية) (٦ · ✅):**
- `618_مصدر_جسر_MT5` — يقرا `ticks` من الجسر → `feed.mt5.tick` (+ `market.symbol_specs`) · محلّي بلا شبكة · snapshot=مؤشّر.
- `613_تغذية_السوق` — يوجّه `feed.*.tick` → `market.tick` · كشف سقوط مزوّد.
- `619_حالة_الحساب` — يقرا الحساب → `platform.account.state` · **account_id Fail Closed**.
- `601_كاتب_جسر_الدماغ` — يكتب الأوامر للجسر · **حساب واحد/نسخة · Fail Closed** · نبضة حياة للإكسبرت.
- `609_مزامنة_المراكز` — `platform.positions.state` · account_id لكل مركز.
- `611_قارئ_الصفقات` — `platform.trade_event` · account_id لكل صفقة · snapshot=مؤشّر.

**بلوك 100 — استقبال البيانات (١٦ · ✅ اكتمل · مجلد `100` بالطقم القديم):**
- `101_مدير_بيانات_السوق` — يجمّع مخرجات المصادر → `market_data.state` (قابل للإعادة). *(نُقل من Desktop للمشروع.)*
- `102_مستقبل_السعر` — `market.tick` → `market_data.price_received` (صحّح خرق الوقت القديم).
- `103_باني_الشموع` — `market.tick` → `market_data.candle_closed` · OHLC من المنتصف + محاذاة فترة · تكّة بلا timestamp تُتجاهل (قاعدة ١٣). **٤/٤.**
- `104_مستقبل_الحجم` — `market.volume` → `market_data.volume_received` · إعادة تشكيل نظيفة. **٤/٤.**
- `105_مستقبل_السبريد` — `market.tick` → `market_data.spread_updated` (spread=ask−bid). **٤/٤.**
- `106_مستقبل_العمق` — `market.depth` → `market_data.depth_updated` · تحقّق دفتر (متقاطع/غير مرتّب) + مشتقّات (mid/imbalance). **٤/٤.** *(خطّاف مستقبل L2.)*
- `107_شريط_الصفقات` — `market.trade` → `market_data.trade_tape_updated` · نافذة متحرّكة + buy_ratio (بلا تخمين اتجاه). **٤/٤.** *(خطّاف مستقبل.)*
- `108_مستقبل_الأخبار` — `market.news` → `market_data.news_received` · منع تكرار + توحيد بلا اختراع تقييم. **٤/٤.** *(خطّاف مستقبل.)*
- `109_التقويم_الاقتصادي` — `market.calendar`+`SYS_SECOND` → عدّ تنازلي + نافذة تنبيه (الوقت من official_time). **٤/٤.** *(خطّاف.)*
- `110_المؤشرات_المرجعية` — `market.reference` → VIX/DXY/TNX · عتبة تغيّر + قائمة مراقبة. **٤/٤.** *(خطّاف.)*
- `111_جسر_الوقت` — `time.utc.* → market.time.*` (نقطة وصول موحّدة لمزامنة الوقت). **٣/٣.**
- **المعالجة (المسار):** `102–110` → `112 مدقّق` (edge-triggered `validation_failed`) + `113 منظّف` (منع تكرار → `market_data.cleaned`) → `114 موحّد` → **`market_data.normalized`** (`{type,data,timestamp}`). **112:٤/٤ · 113:٤/٤ · 114:٣/٣.**
- **الكشف:** `115_جودة_البيانات` (فجوة/قفزة/سبريد/تكرار · **حالة لكل رمز** — يمنع القفزة الوهمية) → `market_data.quality_alert`. **٦/٦.** · `116_كشف_الانقطاع` (صمت المصادر · **حدثي على SYS_SECOND** لا time.time · يسمع المصادر مباشرة فيبقى حيًّا لو ماتت 613) → `feed_interrupted`/`feed_recovered`. **٣/٣.**

**بلوك 700 — التخزين (١١ مخزن · ✅ اكتملت المخازن · مُثبَتة اختبارات + مدقّق ٠ لكلٍّ):**
- `701_مخزن_بيانات_السوق` — `market_data.price_received` → `market_data.db` · دفعات + احتفاظ + تقليم يومي · حساب-محايد (رمز). **٥/٥.**
- `702_مخزن_الصفقات` — `trade.opened/closed/rejected` + إثراء `market.outcome.realized` → `trades.db` · **`account_id` عمود** + منع تكرار `(kind,source_event_id)` · تخزين فوري · بلا تقليم (714 يؤرشف). **٥/٥.** *(خطّاف مستقبل.)*
- `703_مخزن_الأوامر` — `execution.order.built` → `orders.db` · **`account_id` عمود** + دفعات + احتفاظ. **٥/٥.** *(خطّاف مستقبل.)*
- `704_مخزن_الخط_الزمني` — سجل تدقيق موحّد لأحداث النظام (قائمة `watch_events` بالإعداد، نمط 613) → `timeline.db` · **`account_id` عمود** + احتفاظ + تقرير يومي. **٤/٤.**
- `708_سجل_الرموز` — يوحّد اسم الرمز (USTEC→NQ100) · **بالذاكرة لا قرص** · خريطة `.state` (يعيدها الناقل) + حلّ عند الطلب + منع بتر ذكي (PLATINUM يظلّ PLATINUM). **٦/٦.**
- `712_مخزن_التحليل` — `analysis.consolidated` → `analysis.db` · محايد للحساب (رمز) · دفعات + احتفاظ. **٤/٤.** *(خطّاف مستقبل.)*
- `713_مخزن_البنية` — `market.structure.updated` → `structure.db` · محايد للحساب · دفعات + احتفاظ. **٤/٤.** *(خطّاف مستقبل.)*
- `718_مخزن_السيولة` — `market.liquidity.updated` → `liquidity.db` · محايد للحساب · دفعات + احتفاظ. **٤/٤.** *(خطّاف مستقبل.)*
- `709_مخزن_المحفظة` — `portfolio.summary` → `portfolio.db` · **`account_id` عمود** + **كبح لكل حساب** (إصلاح: A2 ما ينكبح بساعة A1) + احتفاظ ٣٦٥ي. **٤/٤.** *(خطّاف مستقبل.)*
- `707_مخزن_القرارات` — ٦ مراحل قرار بنفس `request_id` → `decisions.db` · **`account_id`+`strategy_id`+`model_id`** (أي نموذج قرّر) · بلا تقليم. **٣/٣.** *(خطّاف مستقبل.)*
- `706_مخزن_النماذج` — طلب/ردّ: يحفظ نسخ النماذج (upsert) + يبقي آخر N + يحمّل → `models.db` · نموذج عام (لا account_id) · `save_failed` عند الفشل. **٥/٥.** *(خطّاف مستقبل.)*

**بلوك 700 — الصيانة (٧ من ٧ · ✅ اكتملت · منفصلة بقرار المالك «لا دمج» · التشريح أكّد اختلاف المسؤوليات):**
- `714_الأرشفة` — على `SYS_DAY` ينقل الصفوف الأقدم من `archive_after_days` من المخازن → `archive.db` ويحذفها من المصدر · **`time_column` لكل مخزن** (إصلاح: trades بـ`closed_at`، decisions بـ`decided_at` — القديم كان يفشل صامتًا) + **حرس معرّفات** (لا حقن SQL). **٤/٤.**
- `715_ضغط_الأرشيف` — على `storage.archived` يضغط ملف الأرشيف → `.gz`. **٥/٥.**
- `716_تنظيف_التكرار` — على `storage.archived` يحذف الصفوف المكرّرة · **dedup آمن لكل مخزن** (جداول التدفّق فقط + حرس معرّفات). **٣/٣.**
- `717_سلامة_البيانات` — على `storage.trading_data_cleaned` فحص سلامة (integrity/بلا ختم/مستقبلي) · **`time_column` لكل مخزن** · حكم SOUND/SUSPECT. **٤/٤.**
- `800_النسخ_الاحتياطي` — **حدثي على `SYS_DAY`** ينسخ store+config → tar.gz + احتفاظ N · وقت من `official_time` (لا خلفية/لا `time.time`). **٤/٤.**
- `802_أرشفة_الملفات` — **حدثي على `SYS_DAY`** يؤرشف ملفات journal/snapshots القديمة → tar.gz ويحذفها. **٤/٤.**
- `803_تنظيف_الملفات` — **حدثي على `SYS_DAY`** يحذف `*.tmp` القديمة · **فشل آمن بلا patterns**. **٤/٤.**

**سلسلة الصيانة الكاملة:** `SYS_DAY` → 714 أرشفة → `storage.archived` → {715 ضغط · 716 تنظيف} → 716 → `storage.trading_data_cleaned` → 717 سلامة · و(800/802/803) مستقلّة على `SYS_DAY`.

**بلوك 750 — قياس خام (١):**
- `753_موارد_الجهاز` — على `SYS_5MIN` يقرأ CPU/ذاكرة/شبكة/حرارة (أرقام خام، بلا تفسير) → `tools.device_resources.state` (قابل للإعادة) · psutil استيراد كسول (غيابها = DEGRADED). **٤/٤.** *(752 الأداء = تفسير «اتجاه» = حوكمة، مُعيَّف.)*

**بلوك 150 — التحليل الخام (العقل · بدأ 2026-08-05 · مرجع: [[قسم ١٥٠ — محرك التحليل الخام]]):**
- **العقد الموحّد (قانون القسم · قرار المالك):** كل محلّل يرجّع `symbol · id · status · signal · score · confidence · quality · warnings · metadata` (+ حقول الوحدة) · وقت من الناقل (١٣) · لاحقة `.state` (١٦) · حقيقة خام (١٥). **حدود القسم:** خصائص خام فقط — **ممنوع** BOS/CHoCH/سيولة/قرار (للأقسام اللاحقة).
- `151_الاتجاه` — `market_data.candle_closed` → `analysis.trend.state` · EMA سريع/بطيء + ميل + موقع السعر (٣ شروط تفلتر التذبذب) · `strength`+`phase` · signal=up/down/sideways · محايد للرمز · حالة لكل (رمز,فريم). **٦/٦ · مدقّق ٠ · حيّ (رحلة سعر: إحماء→صعود→ضعف→هبوط→تذبذب).** *(→ الدمج 166 → 712.)*
- `152_الزخم` — `market_data.candle_closed` → `analysis.momentum.state` · ROC+Impulse+Persistence · `level` weak/medium/strong · signal=up/down/sideways (sideways لو |ROC|≤عتبة أو اتساق<persistence_min). **٦/٦ · مدقّق ٠.** *(→ 166 → 712.)*
- ✅ `150_مدير_التحليل` — **Orchestrator** (نمط B · قرار المالك): يفتح دورة لكل شمعة (`cycle_id`) · يجمّع حالات الوحدات · مهلة على نبضة `SYS_SECOND` · حارس ترتيب → `analysis.cycle.collected`. **لا يحسب/لا يدمج.** **٤/٤ · مدقّق ٠.**
- ✅ `166_دمج_التحليل` — **Fusion (عقل القسم):** `analysis.cycle.collected` → يتجاهل الفاشل · إجماع موزون بالثقة · تعارض · ثقة نهائية · `contributors` → `analysis.raw.completed`. **٥/٥ · مدقّق ٠.** *(→ 712 عند وصل التخزين.)*
- ✅ `153_التذبذب` — `analysis.volatility.state` · ATR **نسبة ذاتية** (يحلّ المعايرة) → low/normal/high · سياق. **6/6 · حيّ.**
- ✅ `154_الحجم` — `analysis.volume.state` · Avg/Ratio/Spike/Trend + اتجاه السعر → accumulation/distribution/normal · **الحجم=tick (موثّق `source`)** · سياق. **6/6 · حيّ.**
- ✅ `155_السبريد` — `analysis.spread.state` · **توسّع/انكماش المدى** (قرار المالك «أول»: يفرق عن 153) → expansion/contraction/stable · سياق. **6/6 · حيّ.**
- ✅ `156_الشموع` — `analysis.candle.state` · engulfing/outside/inside/marubozu/pin_bar/doji (هندسة OHLC) · سياق. **9/9 · حيّ.**
- ✅ `157_الفجوات` — `analysis.gap.state` · Size/Type + تتبّع Filled/Age · سياق (`none` الغالبة بحقّ على M1). **7/7 · حيّ.**
- ✅ `158_الجلسات` — `analysis.session.state` · وقت الشمعة UTC (قاعدة ١٣) → asia/london/new_york/overlap/closed · سياق. **6/6 · حيّ.**
- ✅ **عمود التحليل مكتمل كليًّا مُثبَت حيّ على حساب 474099934:** ٨ رموز → 151..165 ذاتيًّا (١٥ وحدة) → 150 يجمّع (المتوقّع ١٥ · v1.13.0) → 166 يدمج (**يصوّت اتجاهيًّا فقط · الباقي سياق**) → `analysis.raw.completed` → **712 يخزّن على القرص.** كل محلّل: مدقّق ٠ + اختبار + **الفحص ٢ حيّ**.
- **العقد + الدورة الإلزامية:** كل وحدة على `Input→Validate→Calculate→Evaluate→Score→Confidence→Warnings→Publish` + `cycle_id` · signal={up/down/sideways للاتجاهي · تصنيف للسياق}. **150 يُضاف له كل حدث وحدة بـ`_UNIT_EVENTS`+المانيفست (مع رفع إصدار) — الآن v1.13.0 المتوقّع ١٥ (العمود كامل).**
- **✅ عمود التحليل اكتمل كليًّا (2026-08-06):** 159 وقت · 162 سرعة · 163 تسارع · 164 جودة حجم · 165 ضوضاء · **160 ارتباط + 161 قوة نسبية** (عبر‑رمز · مقابل السلّة · مرساة USTEC · قرار المالك). كلٌّ مدقّق ٠ · اختبار ٦/٦ · حيّ. **الماكرو (DXY/VIX) لاحقًا — مصدر مدفوع صحيح.** إحماء المعايرة الذاتية ~٢٠ شمعة/رمز (بصدق `insufficient_data` قبلها).
- **✅ 620 مصدر ياهو + 110 المؤشرات المرجعية حيّ (2026-08-05):** بناء نظيف (الفكرة من مرجع سطح المكتب `Desktop/ذرات` · **ولا سطر منقول** · مو للاستعمال) · **حدثي على نبضة ٨٠٦ لا حلقة خلفية** · إنكليزي · لا ساعة. 620 يسحب `^VIX/DX-Y.NYB/^TNX` من ياهو وينشر `market.reference` → **110 قام من الجوع** (`UNAVAILABLE_NO_REFERENCE_SOURCE` → healthy · `tracked=3`) → `market_data.reference_index_updated`. **قيمة حيّة مثبَتة:** `DX-Y.NYB=99.701`.
- **⏸️ قرار 160/161 (المرجع الحقيقي صار متاحًا فعلًا):** نقيس مقابل مين؟ — **مقابل السلّة** (رموزك الـ8 · مرساة USTEC) · **و/أو الماكرو الحقيقي** (DXY/VIX/TNX من 110). **159 أثر الوقت ✅ حيّ.**
- **✅ الأخبار 108 + التقويم 109 حيّ (2026-08-06):** بوت `QUANT_NQ_NEWS/bridge_writer.py` يكتب أخبار ياهو RSS بـ`nq_brain.db` · والإكسبرت `QUANT_NQ_Feed.mq5` v2.11 يكتب تقويم MT5 الأصلي (UTC · مجاني مع الحساب) · **616 جسر الأخبار** (v1.1.0 · batch_limit=500) يقرا الاتنين → 108 (٢٠ خبر حقيقي) + 109 (١٠٦ حدث MT5 حقيقي). ⬜ باقي جوعان: 106 عمق · 107 شريط صفقات (بلا مصدر بعد).
  - **⚙️ تشغيل (continuity):** الأخبار **مو تلقائية** — لازم `QUANT_NQ_NEWS\.venv\Scripts\python.exe bridge_writer.py` يشتغل دوريًّا (venv فيه feedparser/requests/dotenv). التقويم **تلقائي** بعد تصريف الإكسبرت (v2.11 · F7). **Finnhub = 403** (الباقة المجانية بلا تقويم · متروك · المفتاح بـ`.env.example` يُنقل لـ`.env` ويُدوّر).
- **اسم اللوحة:** مجلد `governance/` اختفى (يُعاد تنظيمه). الاسم العربي مصدره **مجلد الذرة** — الحلّ الدائم اشتقاقه تلقائيًا.

**المسار موصول ومُثبَت حيّ:** الإكسبرت → `ticks` → 618 → `feed.mt5.tick` → 613 → `market.tick` → 102 → `market_data.price_received` → 101 → `market_data.state`.

## قرارات محسومة (لا تُعاد)
- **الأرقام القديمة تبقى** (لا إعادة ترقيم — قرار المالك). السكيم بالبلوكات دليل منطقي فضفاض.
- **`account_id` مخبوز بكل ما يمسّ حساب/صفقة/أمر** (جسور 601/619/609/611 · مخازن 702/703/704/707/709) · **بيانات السوق محايدة للحساب** (618/613 + مخازن 701/708/712/713/718 — symbol فقط) · القرارات/الصفقات تحمل `strategy_id`+`model_id` (707/702).
- **`snapshot` يُشال إلا لسبب شرعي:** مُبقى فقط بـ007 (أساس أمني) · 611 و618 (مؤشّر تقدّم) — مو كذبة طزاجة. **القرص = الحقيقة** بكل المخازن.
- **أحداث الحالة بلاحقة** `.state`/`.snapshot`/`.synced` (قابلة للإعادة) · الأوامر لا تُعاد (حارس النواة).
- **الطبقات الحسّاسة (نسخ/أرشفة/تنظيف ملفات) حدثية على `SYS_DAY`** — لا حلقة خلفية ولا `time.time()` (قاعدة ١٣ + قاعدة «لا خلفية»). الفاصل الزمني يُحسب من `official_time`.
- **مبادئ مضافة (رأي تالت · وافقت):** كل مخزن يوثّق **مين يقرا بياناته** (منع «مقبرة بيانات») · الدمج **بعد تشريح المسؤولية** لا تشابه الاسم · اتصالات SQLite **عابرة** (تفتح/تسكّر لكل عملية) · حرس معرّفات SQL (`^[A-Za-z0-9_]+$`) بكل جدول/عمود من الإعداد.

## 🏛️ الفصل المعماري — طبقتان (محسوم 2026-08-05 · قرار المالك + الرأي التالت)
**طبقتان منفصلتان تمامًا — تحلّ التباس العزل من جذوره:**

**١. طبقة التنفيذ (Execution Plane) — لكل حساب/أنبوب مستقل:**
- كل أنبوب يملك: اتصال الوسيط · بيانات السوق · حالة الحساب · الاستراتيجيات · **مخاطر الحساب** · التنفيذ · إدارة الصفقات.
- **لا أنبوب يقرأ حالة أنبوب آخر.** نفس كود الذرات، **نسخة لكل طرفية/حساب** (لا قسم/كود لكل حساب).
- **مخاطر A توقف A فقط** — خسارة حساب ما توقف غيره (إلا سياسة حوكمة عليا صريحة).
- سوينغ/سكلب = **إعداد استراتيجية الأنبوب** (خيط جوّا الذرة)، مو عزل ببيانات.

**٢. طبقة الرؤية/الحوكمة (Portfolio Plane) — عبر الحسابات · قراءة فقط · لا قرارات تداول:**
- تجمع حالة كل الحسابات → إجمالي رأس المال · إجمالي المخاطر · الأداء · تقارير · تنبيهات · لوحات.
- **تقرا فقط:** `account.state` · `position.state` · `execution.confirmed` · `market.outcome.realized` · `risk.status`.
- **تنتج:** `portfolio.summary` · `portfolio.risk` · `portfolio.performance` · `portfolio.accounts`.
- **لا ترسل أي أمر شراء/بيع أبدًا.**

**القاعدة الذهبية:** الأنابيب ما تتلامس **بالقرار** · تلتقي **بالرؤية** عند المحفظة. `account_id` = **مفتاح الرؤية عبر الأنابيب** (لهيك هو بمخازن الحساب — صح وضروري، مو زينة).

**ترتيب البناء (محسوم):** بلوك 100 (جسور/بيانات) → مسار التداول الكامل (تنفيذ+إغلاق) → تأكيد أنبوب واحد يشتغل منفرد → **ثم** 650/651 (المحفظة تجمّع نتائج الأنابيب). المحفظة **مستهلك بيانات، مو بمسار القرار** (ما عندها شي تجمّعه قبل وجود الأنابيب).

**أين ذراتي:** التنفيذ = 618/613/619/601/609/611 (نسخة لكل أنبوب) + بلوك 100 · المخازن تحمل `account_id` (تخدم الرؤية) · **650/651 (المحفظة) لاحقًا** بعد اكتمال الأنبوب. (الخطأ اللي انحلّ: كنت بحاول أعزل النمط/الحوكمة بطبقة البيانات — غلط، الفصل بالطبقتين.)

## الأماكن
- **المبني الجاهز:** `C:\Users\NQ\QUANT_NQ\atoms\` (٤٦ ذرة).
- **بلوك 100 كامل بالمشروع** (101 نُقل من Desktop باسم عربي). حدث 101: `market_data.state` · نهاية المعالجة: `market_data.normalized` (114).
- **الطقم القديم (٢٠٨ مقبورة، مرجع حصاد):** `Desktop\ذرات\atoms\`.
- **مخازن SQLite:** `var/store/*.db` (تُنشأ عند التشغيل) · **الجسر الحيّ:** `…\MetaQuotes\Terminal\Common\Files\nq_brain.db`.

## الجاي
**✅ المكتمل (١٥٥ ذرة · مدقّق ٠ عدا مخالفة 157 القديمة · +✅ 550 التنفيذ كامل تحت):** تحتية (000·٥) · جسور (600·٧ · **+602 إحماء المحلّلات ✅ مُثبَت حيّ**: بثّ تاريخ `candles_history` من الجسر عند الإقلاع بشكل 103 + علَم `warmup:True` → المحلّلات تتحمّى بثوانٍ بدل ~٥٥د · مسار التنفيذ يجب يتجاهل warmup · الإكسبرت v2.12 يكتب الجدول · **أُثبت حيّ: ١٦٠٠ شمعة حقيقيّة برموزه · 602 بثّها كلها warmup=True**) · **استقبال البيانات (100·١٦)** · **تخزين+صيانة (700·١٨)** · قياس خام (753) · **🔒 عمود التحليل (150·مختوم): 151..165+166 → 712** · **✅ بنية السوق (200·١١ · مُثبَت حيّ): 201..208+209+210 → 713** · **✅ السيولة (250·١١ · مُثبَت حيّ): 251..258+259+260 → 718** · **✅ الإحصاء (300·١٨/١٨ كامل · مدقّق ٠): 301..316,318 + مدير 300 → `stats.*.state` + `stats.cycle.collected`** · **🟡 الاحتمالات (350·١٠ نواة · سقالة قابلة للمراجعة): 351..356 نماذج + 357 دمج + 358 هيرست + 359 ثقة + 350 مدير → `probability.*.state` + `probability.cycle.collected`** · **🟡 الاستراتيجيّات (400·١٣ · على تصميم المالك القديم · قابلة للمراجعة): 404..412 (٥ اتّجاهيّة+٣ سياقيّة+أخبار) + 413 دمج + 401 دخول + 402 خروج + 400 مدير → `strategy.*.state` + `strategy.cycle.collected`**.

**▶️ التسلسل (قرار المالك 2026-08-06): «كمل كل الأقسام · أجّل الأسئلة للآخر · لا توقف».**
- **🔒 150 مختوم** · **✅ 200 مُثبَت حيّ** (713: قمم/قيعان/اتجاهات حقيقية برموزه · نضج downtrend حيّ) · **✅ 250 مُثبَت حيّ** (718: برك/كنس/FVG حقيقية). **بلا دمج أي قطعة** (قرار المالك «لا تدمج شي»). المراجع: [[قسم ٢٠٠ — بنية السوق]] · [[قسم ٢٥٠ — السيولة وتدفّق الأوامر]].
- **✅ 300 الإحصاء (كامل · ١٨/١٨ · مدقّق ٠ + اختبارات خضراء):** 301 متوسط · 302 وسيط · 303 منوال · 304 انحراف · 305 تباين · 306 CV · **307 ارتباط** (بيرسون عبر‑رمز على العوائد) · 308 انحدار · 309 r² · 310 z-score · 311 التواء · 312 تفلطح · 313 شذوذ · **314 توزيع** (مدرّج) · **315 مدى** (min/max/range+موقع) · **316 جودة بيانات** (تعدّ الخبيث لا تُسقطه) · **318 مقارنة فترات** (نصف أحدث/أقدم) · **300 مدير** (نمط B يجمّع ١٧ وحدة → `stats.cycle.collected`). **الجزء 12/18 مُثبَت حيّ سابقًا** (بثّ `/ws/events`: XAUUSD تفلطح 1.81 · z-score متطرّف). **الستّة الجديدة (307/314/315/316/318/300): Gate-1 تمّ (مدقّق+اختبار) · Gate-2 الحيّ ينتظر إعادة تشغيل المالك للنواة** (تُحمَّل عند الإقلاع — زي تشغيل النواة الكامل المؤجّل). **سؤالان مؤجّلان: مخزن إحصاء أم تغذية 350 فقط · علاقة 307↔160 (بلا دمج) — [[أسئلة مؤجلة — للآخر]].** المرجع: [[قسم ٣٠٠ — الإحصاء]].
- **🟡 350 الاحتمالات (نواة كاملة · ١٠ ذرات · سقالة قابلة للمراجعة · مدقّق ٠ + اختبارات خضراء):** **351 اتجاه** (207+208) · **352 انعكاس** (CHoCH+MSS) · **353 اختراق** (BOS+FVG) · **354 ارتداد** (يستهلك كنس 254 الحيّ · تلاشٍ) · **355 زخم** (انحدار+r²) · **356 مدى** (هيرست+اتجاه) · **357 دمج** (السيناريو المهيمن) · **358 هيرست** (R/S حقيقي — قياس موضوعيّ) · **359 ثقة** (متوسّط×تغطية) · **350 مدير** (نمط B · ٩ وحدات → `probability.cycle.collected`). **⚠️ النماذج سقالات منهجيّتها مبدئيّة (كل العتبات إعدادات معلَنة) — «مبنيّة قابلة للمراجعة» لا مثبَتة · لا اعتماد قرار تداوليّ قبل رؤية المالك.** **⏸️ مؤجّل بوعي: 360..368 (عمليّات النماذج: دقّة/انجراف/أولويّة/اختيار/مستودع/محمّل/مقارنة/إعادة‑معايرة/تاريخ‑إصدارات) = بنية ML‑lifecycle + مراقبة، تحتاج رؤية المالك + قرارات بنية.** المرجع: [[قسم ٣٥٠ — الاحتمالات]] · [[أسئلة مؤجلة — للآخر]].
- **🟡 400 الاستراتيجيّات (كامل · ١٣ ذرة · على تصميم المالك القديم `Desktop\ذرات\400` كأفكار · مدقّق ٠ + اختبارات خضراء):** **الخمس الاتّجاهيّة (تصوّت BUY/SELL): 404 اتجاه(207)· 406 اختراق(204 dir)· 407 ارتداد(كنس254: buyside→SELL)· 408 زخم(152)· 410 سيولة(FVG255)** · **الثلاث السياقيّة (لا اتجاه): 405 انعكاس(transition)· 409 مدى(range)· 412 جلسة(active/cautious)** · **411 أخبار = خطّاف صادق UNAVAILABLE** (108 عناوين لا اتجاه — لا تلفيق) · **413 دمج** (يعدّ أصوات الخمس → buy/sell/neutral) · **401 دخول** (≥`min_agreement`=2 اتّفاق) · **402 خروج** (انقلاب الأغلبيّة · ذاكرة مستقلّة عن 401) · **400 مدير** (نمط B · ١٢ وحدة → `strategy.cycle.collected`). **قرار المالك المعماريّ: 350 و400 عمودان متوازيان يصبّان بـ450.** **⚠️ إشارات فقط — لا أمر شراء/بيع فعليّ · قابلة للمراجعة قبل الاعتماد.** **⏸️ مؤجّل: 403 طلب فلاتر · 414..417 عمليّات استراتيجيّات (زي 360..368).** المرجع: [[قسم ٤٠٠ — الاستراتيجيّات]].
- **🟡 450 القرار (نواة السلسلة · ٩ ذرات · على تصميم المالك القديم كأفكار · مدقّق ٠ + اختبارات خضراء):** **451 تجميع** (400 اتجاه + 350 ثقة) → **453 درجة** (0–100) → **454 فلتر/بوّابة** (score≥min_score=60) → **455 شراء · 456 بيع · 457 انتظار** → **466 موافقة** → **467 إرسال** (`execution.order.requested` = **حدّ 550**) · **450 مدير** (نمط B · ٧ وحدات → `decision.cycle.collected`). **✅ فلاتر مبنيّة وموصولة ببوّابة 454 (AND): 460 ثقة (350) · 461 شروط (جودة 316) · 462 توقيت (جلسة 158)** — فلتر يمنع القرار رغم درجة عالية (مُختبَر). **✅ 467 له مستهلك الآن (550 مبنيّ) — لكن آمن: مفتاحا 552/575 `enabled`=false → معاينة فقط، لا صفقة حتى يفعّلهما المالك على ديمو.** **⏸️ مؤجّل من 450: 452 مُقيّم · 458 حلّ تعارض · 459 أولويّة · 463 أحداث (108 لا اتجاه) · 464 سيولة (لا سبريد) · 465 مراجعة.** المرجع: [[قسم ٤٥٠ — القرار]].
- **🟡 500 المخاطر (نواة · ٣ ذرات · على تصميم المالك القديم · مدقّق ٠ + اختبارات خضراء):** **516 قاطع الأمان** (Circuit Breaker — يتحقّق من `execution.order.requested` → `risk.validation.completed` · يراقب `risk.loss_reported` → خسارة يوميّة/متتالية → `emergency.halt` · **إعادة تفعيل يدويّة فقط · يوم جديد لا يصفّي القاطع · لا ساعة**) · **500 مدير المخاطر** (مجمّع → `risk.unified.state` · يجمّع ولا يفرض) · **517 نتيجة الصفقة** (P&L → `risk.loss_reported` · **خطّاف صادق `NO_TRADES_YET`**) · **513 تحجيم المركز** (حجم اللوت: `لوت = (Equity×risk%)/(مسافة الوقف×قيمة الوحدة)` من 619 Equity + 618 مواصفات + سعر الشمعة · مُختبَر لوت 0.2 · **قرار: المالك طلب «حجم اللوت»؛ القديم كان تخصيص رأس مال — بنيت ما طلبه**). **بوّابة المخاطر بالمسار: 467→516 يتحقّق · 513 يحجّم لـ550.** **⏸️ مؤجّل من 500 (حدود مجوّعة — تحتاج صفقات/محفظة 651): 506 حدود جلسة · 507 حدود ربح · 508 مدير تعرّض.** المرجع: [[قسم ٥٠٠ — المخاطر]].
- **✅ 550 التنفيذ + الإدارة الذكيّة — مبنيّ + مختبَر (Gate-1 · ١٠ ذرات جديدة + تعديل 513/611 · مدقّق ٠ · ١٢/١٢ اختبار · ٥٩ حالة · 2026-08-06).** المالك اختار **(أ) إكسبرت جديد شامل** («عمق سبريد تنفيذ كل امور»).
  - **الذرات:** **512 وقف هيكليّ** (تحت قاع/فوق قمّة من سوينق 201) · **551 باني الأمر** (جهة+لوت هيكليّ 513+ستوب+هدف R:R=2) · **552 مدقّق+بوّابة** (المفتاح `enabled`) · **563 تأكيد** (611→`market.outcome.realized`→517) · **570 منسّق الإدارة** (الأضيق يفوز · لا يُرخى) · **572 تعادل**(+1R→الدخول) · **573 تتبّع**(هيكليّ) · **574 جزئيّ**(٥٠٪@1R) · **575 مرسل الإدارة** (المفتاح `enabled`) · **550 مدير** (→`execution.unified.state`). **استفدت من القائم: 601 يكتب الدخول · 609 مراكز · 611 (عُدّل يمرّر profit) للتأكيد** — فما بنيت 553/571/554.
  - **🔑 مفتاحا أمان (`552.enabled` + `575.enabled`) = false افتراضيًّا → معاينة فقط، لا صفقة حتى يفعّلهما المالك على ديمو.** halt (516) يقفل 552 أيضًا.
  - **✅ الإكسبرت الواحد `mt5\QUANT_NQ.mq5`** (أساس NQ_Arm التنفيذيّ + كتّاب الـFeed المُثبتين + **عمق DOM جديد** + عمود `profit` دقيق) **مبنيّ ومنسوخ للمنصّتين (٦٥١١٣ بايت) — يُصرَّف مرّة، الإكسبرت غبيّ، كل العقل بايثون، ما أعدّله ثانية.**
  - **⏸️ زيادة/scale-in مؤجّلة** (تزيد المخاطرة · تحتاج تصميم + مراجعتك). التصميم/الملاحظات كاملة بـ[[قسم ٥٥٠ — التنفيذ (تصميم)]].
- **▶️ التالي = فحص Gate-2 حيّ لدورة الحساب الواحد (جارٍ 2026-08-07 · التفاصيل الكاملة: `سياق/مراجعة ورق البناء والتشخيص الحيّ` + ذاكرة [[single-account-cycle-live]]):** المالك صرّف الإكسبرت الواحد وهو **يغذّي الجسر حيّ** (ديمو 474099934). تتبّعت الدورة حيّة: **الدماغ أخضر كامل**، والصفقات كانت صفر بسبب **باگ لافتة `timeframe` (413+451) — اتصلّح بالمفتاح‑بالرمز، مُثبت**. ضُبطت أرقام الديمو بإذن المالك (401→١·454 min_score=0·فلتر الثقة استشاريّ·552/575 مفعّل). **الحاجز الأخير = فلتر التوقيت** (158 بالساعة فقط، فجوة جلسات UTC~21، ما يعرف الكريبتو 24/7). **❓قرار المالك المعلّق: (١) ننتظر جلسة آسيا · (٢) نخلّي التوقيت يعرف الكريبتو → صفقة فورًا على BTC.** ثم **275 بيئة** ثم المحفظة 650/651. **⚠️ 550/570 تخالف تصميم المالك — قابلة لإعادة البناء.**
- **⚠️ مخالفة قديمة (ليست لي): 157_الفجوات فيه تعليقان عربيّان بـatom.py (سطر ١٠٨/١١٦) → المسح الشامل = مخالفة ١. ذرّة تحليل — قرار المالك بإصلاحها.**

**حوكمة متروكة (لا تُلمس — قرار المالك):** 700 مجمّع (`storage.unified`) · 752 أداء · 761 أخطاء · 766 تنبيهات · 768 توفّر · 801 مراقب نسخ.

## ✅ قرار محسوم (المالك 2026-08-05) — حدود الذرة/الحوكمة
**قرار المالك حرفيًّا:** «لا حوكمة عيّفها — أما إذا كان ذرة لازم إنت تبنيها.»
- **أترك تمامًا (حوكمة · ما ألمسها):** 700 مجمّع التخزين (`storage.unified`) · 761 مراقب الأخطاء · 766 مدير التنبيهات · 768 مراقب التوفّر · 801 مراقب النسخ. (كلها مراقبة/تنبيه/تجميع للعرض — قاعدة ١٥ + سابقة 999/اللوحة.)
- **أبني (ذرات — شغل فعلي / حقائق خام):** كل مخازن 700 (702..718) · الصيانة (نسخ احتياطي · أرشفة · ضغط · تنظيف · سلامة) · وقياس خام (753 موارد · 752 أداء) إن ثبت أنه ينتج حقيقة خام لا مجرّد عرض.
- **حكم الحدّ:** «يعمل شغلًا فعليًّا أو ينتج حقيقة خام» → ذرة أبنيها · «يراقب/ينبّه/يجمّع للعرض» → حوكمة أعيّفها.

## ⏸️ مؤجَّل / تنسيق (مهم)
- **كل ذرة مُثبَتة foreground** (اختباراتها تشتغل بالمالك «كمل وشغل وتأكد») — **النواة الكاملة** (كل الذرات مع بعض) لم تُشغَّل بعد، بانتظار أمر. (التزام: لا خلفية، لا منفذ.)
- **تنسيق نواة:** وراثة مفاتيح الهوية بالناقل (امتداد ٠٤) — ليصير `account_id`/`symbol` ينساب تلقائيًا (قاعدة ٢٢).
- **تنسيق نواة:** استمرار حفظ اللقطات (snapshot) — تعتمد عليه 007/611/618.
- **تنسيق لوحة:** المستهلك يشترك بالاسم الكامل `market_data.state`.
- **مؤجَّل بوعي:** استنتاج قدرات 613 + صحته المتقدّمة · جسور بايننس/ريثمك (مصادر مستقبلية).

## ✅ إثبات حيّ ٢ (2026-08-05 — كل الذرات على النواة الحقيقية + إصلاح قواعد قديمة)
- **٤٦ ذرة حُمّلت وأقلعت على النواة الحقيقية** (لوحة الحوكمة `governance/live/index.html`: ٢٧ سليمة · ١٩ متعثّرة · ٠ واقفة · متّصل بالنواة). **أول تشغيل كامل للطقم الجديد على المايكروكيرنل — مو بس اختبارات foreground.**
- **الـ19 متعثّرة = تصميم صحيح:** خطّافات مستقبل بحالة `DEGRADED` (جوع مدخل — مُنتِجها مو مبني بعد) لا فشل.
- **٣ خلل حقيقي (702/704/707) + إصلاحه:** قواعد قديمة (8/4، ما قبل القبر) بمخطط ناقص `account_id`/`strategy_id`/`model_id` — `CREATE TABLE IF NOT EXISTS` ما يهاجر جدولًا موجودًا. **الحلّ (قبر القديم):** دُفنت إلى `var/store/_قبر_مخطط_قديم_20260805/`، وأُنشئت نظيفة بالمخطط الصح (مُثبَت: الأعمدة موجودة). **الكود سليم** (orders/portfolio الجديدتان اشتغلتا بلا خلل — دليل قاطع).
- **درس محسوم:** قاعدة قديمة بمخطط متغيّر = **تُدفن** (قبر القديم)، لا تُرقَّع. `CREATE IF NOT EXISTS` لا يهاجر.
- **إصلاح #7 «فيها خلل» (اللوحة · 2026-08-05):** كان يبصم `atoms/` كاملة **ومنها ٤٦ بايتكود `__pycache__/*.pyc`** يتغيّر وقت التشغيل → «فرق» كاذب → أحمر دائم (لا نسيان تلقائي). **أُعيد بناؤه (3.0.0):** بصمة **محتوى** لكل مصدر (يكشف تعديل بمكانه — خلل قديم) · **يتجاهل `__pycache__`/`.pyc`** · يراقب المختوم **`core/`+config** لا `atoms/` (قرار المالك «تصرف» → الخيار أ · `atoms/` عند التجميد). **حيّ:** أساس `core/` HEALTHY (٢٩) → churn بايتكود HEALTHY ٠ إنذار → محاكاة تلاعب UNHEALTHY modified · مدقّق ٠ · ٧/٧. بعد إعادة التشغيل #7 تصير خضرا.
- **`market_data.db` ≈ ٢٣ ميغا حيّ** (701 يخزّن السوق فعلًا على النواة).
- **التالي:** المالك يعيد تشغيل النواة → 702/704/707 تفتح القواعد النظيفة وتتعافى (702/707 → «تنتظر» · 704 غالبًا أخضر لأنه يسمع أحداث نظام حيّة).

## ✅ إثبات حيّ (2026-08-04 — مسار بيانات السوق كامل على تك حقيقي)
- **إكسبرت `mt5/QUANT_NQ_Feed.mq5`** (كتبه كلود · صرّفه ووصّله المالك) يغذّي الجسر: ticks + prices + account + positions + symbol_specs · ولوحة حالة على الشارت (CONNECTED · الحساب 474099934).
- **🎯 السلسلة الكاملة مقيسة حيّة (٦٠ تك حقيقي لكل خطوة):**
  `EA → ticks → 618 → feed.mt5.tick → 613 → market.tick → 102 → market_data.price_received → 101 → market_data.state`
  · مثال ناتج: BTCUSD 64134.82/64141.82 · USTEC 29809.86/29810.75. **أربع ذرات نظيفة على السوق الحقيقي.**
- **اللوحة = شغل المالك/طبقة الحوكمة، مو كلود.** (لوحة ويب تجريبية بـ`governance/dashboard/` من تجاوز سابق — متروكة كمرجع، كلود وقف عنها.)
- **تقدّم منذ هذا الإثبات:** بلوك 100 كامل (103–116) + بلوك 700 (تخزين+صيانة) + 753 — كلها مبنية ومُثبَتة (اختبارات foreground + مدقّق ٠). التالي = التحليل 150+.

## كيف تتحقّق (بلا نواة/منفذ)
كل ذرة عندها `tests/test_atom.py` مكتفٍ (FakeEventBus / sqlite مؤقّت) — تشغيله foreground يثبت الذرة بلا نواة ولا منفذ ولا خلفية.
