# ورقة بناء — النواة ٠٢: نافذة الرقابة (العيون) — رقابة الناقل

**نوع الورقة:** تشريح تنفيذ كامل — يُجاب على **كل سؤال** فيه **قبل** أي سطر كود.
**التاريخ:** 2026-08-03 · **الوحدة:** النواة ① · الجزء ٠٢ (أول جزء من فتحة V2.0)
**الحالة:** ✅ **مُعتمَدة (مقفولة) 2026-08-03** — بعد رأي ثالث + تصفية المالك. قرار دورة
حياة العدّادات: **تراكمي بسيط بالنواة + لقطات بطبقة ٢** (النواة غبية، الذكاء فوق).
**يمسّ النواة** (جزء من الختم V2.0، يُنفَّذ مع بقية أجزاء النواة).
**✅ منفّذة ومختومة — Core V1.8.0 (2026-08-04). الإثبات والتفاصيل: ورقة ٠٨.**

> طريقتنا: من الجذر لا ترقيع · نرفع لا نبسّط · العزل/الدقة/الجودة خطوط حمر · لا كود قبل
> ما تُجاب أسئلة القسم (٨) وتقول "نفّذ".

---

## ١. ما هي الوحدة (بلغة بسيطة)
**طبقة قياس (Instrumentation/Telemetry) داخل الناقل — لا مراقبة.** فرق مهم (من الرأي
الثالث): النواة **تقيس وتسجّل حقائق خام**، أمّا **المراقبة** (تفسير/عرض/تنبيه) فتكون في
**طبقة ٢** — وهذا يحافظ على جهل النواة.

اليوم البوس "ديكتاتور بلا محاسبة": حدث بلا مشترك **يُبتلَع بصمت** ومحدا بيعرف كم حدث
انلغى. هالوحدة تضيف **عدّادات** تخلّي البوس **يقيس نفسه**: كم حدث نُشر · كم وصل ·
**كم بلا مشترك (`no_subscribers`)** · كم عطّله مهلة · كم عطّله خطأ. حقائق خام فقط — بلا
فهم معناها.

## ٢. الهدف و"ليش"
- **يحلّ "الناقل بلا رقابة" (قسم ٨/٢) من الجذر:** نعرف الأحداث المُسقَطة بالرقم.
- **أساس طبقة ٢ (الحوكمة):** الدماغ فوق يقرأ هالحقائق ويعرضها مقروءة.
- **يكشف أخطاء الوصلات مبكرًا:** حدث ناشر بلا مشترك = وصلة مكسورة تظهر برقم، لا بصمت
  (نفس فخّ الأسماء المعكوسة اللي جوّع ١٧ ذرة سابقًا).

## ٣. الموجود نبني عليه (قِسته بالكود — `core/event_bus.py`)
البوس **أصلًا بيعرف كل شي نحتاجه، بس ما بيعدّه:**
- `publish()` عنده `event_name` و`publisher` و`subs` (المشتركون).
- **السطر `if not subs: return`** = نقطة الابتلاع الصامت بالضبط — هون نعدّ "مُسقَط بلا مشترك".
- حلقة النتائج بعد `asyncio.gather` **تميّز أصلًا** كل معالِج: `TimeoutError` (مهلة) أو
  `BaseException` (خطأ) أو نجاح.
- موجود جاهز: `subscriber_count(event)` و`event_names()`.

**فما نبني ناقلًا جديدًا — نضيف عدّ حيث المعلومة موجودة أصلًا.**

## ٤. التشريح الدقيق — شو راح أعمل بالضبط
**ملف يُمسّ: `core/event_bus.py` فقط** (جزء من V2.0). التغيير **إضافي بحت** — ما
يغيّر سلوك التوزيع القائم.

1. **في `__init__`:** أضيف قواميس عدّ لكل اسم حدث:
   `_published` · `_delivered` · **`_no_subscribers`** (بدل dropped — التسمية الأدق من
   الرأي الثالث: ما "سقط بعد محاولة"، بل ما وُجد له مشترك) · `_timeout` · `_error`.
   (عدّاد إجمالي عام `_total` **اختياري** — مشتقّ من جمع `_published`، أضيفه إن طُلب.)
2. **في `publish`:**
   - `_published[event_name] += 1` (كل نشر).
   - عند `if not subs:` → `_no_subscribers[event_name] += 1` **ثم** return (الابتلاع صار
     **معدودًا** لا صامتًا).
   - بعد `gather`: من حلقة النتائج القائمة، أزيد `_delivered`/`_timeout`/`_error` حسب نوع
     كل نتيجة (المعلومة موجودة أصلًا هناك).
3. **دالة قراءة `stats() -> dict`:** ترجع **نسخة غير قابلة للتعديل (Snapshot، deepcopy)**
   لا مرجعًا حيًّا — فلا يقدر أحد يعدّل العدّادات الداخلية من برّا (إصلاح صحّة من الرأي
   الثالث). قراءة فقط، بلا أي منطق أعمال.
4. **التعريض ليس من شأن النواة (تصحيح الرأي الثالث):** النواة تقدّم **`stats()` فقط.**
   وطبقة ٢ (الحوكمة) هي اللي تقرّر: تنشره؟ تعرضه؟ تخزّنه؟ توفّر API؟ — فلا تدخل النواة
   في تفاصيل الوصول. **(يحسم سؤال "API أم Event" — لا واحد منهما داخل النواة.)**

**ملاحظة صريحة:** قياس **حياة/موت الذرات** (الجزء الثاني من "العيون") **ليس هنا** — له
ورقته المنفصلة (مع `registry`/`health_manager`)، لأنه ليس جزءًا من EventBus أصلًا.

## ٥. فحص الخطوط الحمر
- **جاهلة (Zero-Knowledge):** نعدّ **أسماء الأحداث ونتائجها** فقط — لا محتوى حمولة، لا
  معنى أعمال. النواة تبقى عمياء عن "شو يعني الحدث". ✓
- **العزل:** ما نلمس ذرة، ولا نكسر عزلًا — عدّ داخل الناقل نفسه. ✓
- **رفع لا تبسيط:** نضيف محاسبة لم تكن. سلوك التوزيع القائم **بلا تغيير.** ✓

## ٦. الإثبات الحيّ (تشوفه بعينك)
- على النظام الشغّال: أقرأ `stats()` → أرقام حقيقية: كم نُشر لكل حدث، وكم **بلا مشترك.**
- **اختبار ① بلا مشترك:** أنشر حدثًا باسم بلا مشترك → `no_subscribers += 1` (بدل الصمت).
- **اختبار ② مشترك واحد (ناجح):** → `published=1 · delivered=1 · no_subscribers=0`.
- **اختبار ③ ثلاثة مشتركين — نجاح/مهلة/خطأ** (من الرأي الثالث): →
  `published=1 · delivered=1 · timeout=1 · error=1`. **هذا يحرس منطق العدّ كله.**
- أعرض الأرقام الحيّة كاملة قبل ما نعتبرها ناجحة.

## ٧. المخاطر والتراجع
- **تغيير نواة، إضافي** (عدّادات + دالة قراءة) — لا يمسّ منطق التوزيع، **خطر منخفض جدًا
  (لا صفر — تصحيح الرأي الثالث):** لسا نلمس `publish()`، وأي غلط فيها يمسّ النظام كله.
  فيُكتب بحذر، ويُثبَت بالاختبارات الثلاثة (قسم ٦) قبل أي شي.
- **الكلفة:** زيادة عدّاد = رخيصة جدًا (لا I/O).
- **التراجع:** git (م٠ شبكة الأمان) + الختم محفوظ. والختم يُعاد V2.0 بعد اكتمال أجزاء
  النواة كلها (٠٢–٠٦)، لا بعد كل جزء.

## ٨. أسئلة مفتوحة — جاوبها قبل ما أنفّذ (لا كود قبلها)

**⭐ السؤال الأخطر (من الرأي الثالث — كان ناقصًا):**
**دورة حياة العدّادات — تراكمية منذ الإقلاع، أم لها سياسة؟** بعد أسبوع `published=1.8M` —
هل مفيد؟ الخيارات: منذ الإقلاع · منذ آخر Reset · نافذة زمنية · Lifetime+Snapshot. قرار
معماري (طبقة ٢ تبني عليه الرسوم والتنبيهات)، ويربط بقلق دورة حياة البيانات.
> **توصيتي:** النواة تحتفظ **بعدّادات تراكمية بسيطة (Lifetime، منذ الإقلاع)** فقط —
> غبية ورخيصة. وطبقة ٢ **تاخد لقطات دورية (Snapshots)** تحسب منها المعدّلات/النوافذ/الرسوم.
> فالنواة ما فيها منطق نوافذ ولا reset — الذكاء فوق. **القرار إلك.**

**محسومة بالرأي الثالث (للتأكيد فقط):**
- **التعريض:** النواة `stats()` فقط، وطبقة ٢ تقرّر (API/حدث/تخزين). ✔
- **لكل مشترك؟** لا — لكل حدث يكفي (لكل مشترك مستوى أعلى، بيانات أكبر). ✔
- **حياة/موت الذرات:** ورقة منفصلة (٠٢ب). ✔
- **الترتيب:** أول قطعة من العيون، بعد م٠ (git). ✔

## ٩. قرار المالك
☐ نفّذ كما هو  ·  ☐ عدّل (اكتب)  ·  ☐ أوقف / غيّر

---
**التالي بعد اعتمادها:** ٠٣ النبضة (الحالة عند الاشتراك).
