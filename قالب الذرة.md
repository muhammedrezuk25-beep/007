# قالب الذرة (القالب اللي كل ذرة تمشي عليه)

> **الغرض:** المعيار الواحد اللي كل ذرة لازم تطابقه. أي ذرة تُعطى لكلود
> **تُمشّى على هالقالب**. يُحدَّث أول بأول كل ما يضيف المالك قاعدة.
> **المالك:** محمد رزوق · **آخر تحديث:** 2026-08-05 · **الحالة:** النسخة المرجعية (الدستور الموحّد للذرات · ٢٣ قاعدة + بوّابة · **الفحص ٢ «حيّ على الحقيقي» = شرط تسليم إلزامي**).

---

## القاعدة ١ — كل ذرة = ٥ ملفات

| # | الملف | شو هو | فيه عربي؟ |
|---|---|---|---|
| 1 | `atom.py` | الذرة نفسها (الكود) | ❌ لا |
| 2 | `manifest.yaml` | البطاقة | ❌ لا |
| 3 | `الشرح.md` | الشرح الكامل (دمج ٣ ملفات) | ✅ نعم |
| 4 | `التاريخ.md` | سجل التعديلات | ✅ نعم |
| 5 | `tests/` | الاختبارات | — |

## القاعدة ٢ — النظام بيقرا ملفّين بس

- **`manifest.yaml`** = السيّد. النواة بتفتّش عنه، بتقراه، بتتحقّق منه — ومنه بتعرف كل شي.
- **`atom.py`** = الكود. النواة بتعرفه **لأن المانيفست بيدلّها عليه** (`entrypoint`).
- **الباقي** (`الشرح` · `التاريخ` · `tests/`) = **النظام ما بيقراهن وقت التشغيل** — للإنسان.

## القاعدة ٣ — `atom.py` كود خام نظيف بس

- **لا حرف عربي جوّاه.** لا شرح، لا تفصيل، لا أي حشو.
- كود **مستوى مؤسسات** — مو ألعاب ولا كلام زايد.
- الشرح مكانه `الشرح.md`، **مو جوّا الكود.**

## القاعدة ٤ — `manifest.yaml` نفس الشي: نظيف بس

- **لا حرف حشو، ولا أي شرح — لا إنكليزي ولا عربي.**
- بس الحقول اللي النظام بيحتاجها فعلًا (المعرّف · الإصدار · العقد · الصحة · الإعدادات).
- **ممنوع** الوصف الطويل والتعليقات (متل `metadata.description`).

## القاعدة ٥ — `الشرح.md` = دمج الـ٣ توثيق بملف واحد

- **يدمج:** `api_spec.yaml` + `README.md` + `ATOM_SPEC.md` بملف **واحد**.
- **فيه عربي** — لأنه ملف الشرح، هون العربي مسموح ومطلوب.
- مكتفي بذاته: مين يقراه يفهم الذرة كاملة بلا يفتح غيره.
- **بلا تكرار:** المعلومة تنكتب مرّة وحدة (خلص التناقض بين ٣ نسخ).
- **يوثّق «مين يقرا» (المستهلك):** قسم يحدّد مين يستهلك مخرجات الذرة — **يمنع «مقبرة بيانات»** (ذرة
  تنتج ما محدا يقراه). حتى «خطّاف مستقبل» يُسمّى مستهلكه المقصود. (رأي تالت 2026-08-05 · وافق المالك.)

## القاعدة ٦ — `التاريخ.md` = سجل حياة الذرة

- كل ما نعدّل الذرة، نكتب سطر: **شو عدّلنا · ليش · إيمتى (التاريخ).**
- عشان بعد شهر أو سنة نعرف **ليش الذرة صارت هيك**، مو نخمّن.

## القاعدة ٧ — الذرة ما تعرف غير عقدها (العزل)

- **ممنوع** تستورد ذرة تانية مباشرة.
- التواصل **بس** عبر Event Bus أو الخدمات الرسمية.

## القاعدة ٨ — لا أسرار جوّا الذرة

- كلمات السر والمفاتيح **ما تنكتب** بـ`atom.py` ولا بـ`manifest.yaml`.
- كلها تجي من **نظام الإعدادات** (الأسرار برّا — النواة والذرة جاهلتان عنها).

## القاعدة ٩ — كل ذرة لها اختبارات

- **ما تُعتمد ذرة بلا اختبارات** تغطّي **المسار الطبيعي** + **أهمّ حالات الفشل.**
- مجلد `tests/` موجود = مو كافٍ؛ لازم يغطّي فعلًا.
- **⚠️ الاختبارات (موك) أرضية لا إثبات (قرار المالك 2026-08-05):** تنجح ولا تعني الذرة تشتغل حيّ (تكذب
  بالاتجاهين). **الإثبات = الفحص ٢ بالبوّابة — حيّ على النواة الحقيقية والمالك شايف.**

## القاعدة ١٠ — الأحداث جزء من العقد

- جوّا `الشرح.md` قسم ثابت يحدّد العقد:
  - **الأحداث اللي بتنشرها.**
  - **الأحداث اللي بتشترك فيها.**
  - **شكل كل Payload.**

## القاعدة ١١ — الإعدادات المسموح تعديلها معلنة

- أي إعداد ممكن يتغيّر من **لوحة الحوكمة** لازم يكون **معلَن بـ`manifest.yaml`**،
  مو مخبّى جوّا الكود.

## القاعدة ١٢ — إثبات حيّ (نوريه ما ندّعي)

- بآخر `الشرح.md` قسم ثابت: **«كيف أتأكّد إن الذرة شغّالة؟»** — خطوات عملية:
  - شغّل الذرة.
  - انشر الحدث X.
  - لازم تنشر Y.
  - لازم تظهر النتيجة Z.
- هيك كل ذرة عندها اختبار عملي سريع يشوفه المالك بعينه.

## القاعدة ١٣ — التوقيت: الذرة ما تقرا الساعة (تسلسل مع ورقة ٠٥)

- **الذرة ما تختم حدثها بوقتها الخاص.** الناقل بيختم تلقائيًا **بالوقت الرسمي
  المصحّح** (خام + إزاحة ٦٠٨) — بـ`setdefault`: **يضيف ولا يعدّل.**
- **بس ٣ ذرات مسموح لهن يقروا الساعة:** `3` · `806` · `608` (أصحاب الساعة).
  أي ذرة تانية تختم بـ`time()` = **خرق يمنع الاعتماد** (يفرضه `validate_atoms`).
- **لو الذرة احتاجت «الآن»** (لقياس قِدَم / جدولة) → تاخده من **المصدر الرسمي**
  (نبضة ٣/٨٠٦ أو ختم الناقل)، **مو تقرا الساعة بنفسها.**
- **وقت السوق (للتداول) = من التكّة نفسها**، **مطبّع UTC** + **مفحوص عند الدخول**
  (مرتّب/معقول) + **بديل آمن = وقت الاستلام** لو البروكر بعت زبالة. مو من ساعة الجهاز.
- **ممنوع تخلط وقت السوق بوقت «الآن».** حدث جايي بوقته الخاص (تكّة MT5/Binance)
  **يبقى بوقته، ما يتلمس.**
- **استثناء مسموح:** القياس الداخلي (`received_at` للتأخير · `requested_at`
  للمهلة) — هاد قياس، مو ختم حدث يمرّ على الناقل.
- **الجدولة الدورية = حدثية على النبضة، لا حلقة خلفية.** أي عمل دوري (نسخ · أرشفة · تنظيف · فحص · عدّ
  تنازلي · كشف صمت) **يتفاعل مع نبضة الساعة** (`SYS_SECOND`/`SYS_DAY` من 806) — **ممنوع حلقة `asyncio`
  داخلية بـ`time.time()`+`sleep`** (خرق ١٣ + قاعدة «لا خلفية»). «الآن» والفاصل الزمني يُحسبان من
  `official_time` بالنبضة. (درس حيّ 2026-08-05: 800/802/803/116 أُعيد بناؤها من حلقة خلفية إلى حدثية.)

## القاعدة ١٤ — الإعدادات تتغيّر وهي شغّالة (من اللوحة، بلا فتح ملف)

- كل إعداد معلَن (قاعدة ١١) لازم الذرة **تقدر تغيّرو وهي شغّالة** لمّا اللوحة تطلب —
  **بلا ما المالك يفتح ملف، ولا يعيد تشغيل بإيده.**
- **الإعلان يكفي للتعديل الآمن:** المانيفست يقول **نوع الإعداد · حدّه المسموح ·
  الافتراضي** — عشان اللوحة تعرضو وتمنع القيمة الغلط **قبل** ما تبعتها.
- **الذرة هي اللي تغيّر إعدادها بنفسها** لمّا **بوّابة الأوامر** تطلب — **محدا يدخل
  جوّا الذرة** (العزل، قاعدة ٧). **بوّابة الأوامر تطلب، والذرة تطبّق** — واللوحة أو
  تلغرام أو أي واجهة تستعمل **نفس البوّابة** (السلطة مكان واحد، واللوحة مجرّد عميل).
- **تتحقّق قبل ما تطبّق:** القيمة الغلط **تُرفض والذرة تكمّل على القديم** — **ما
  تتعلّق، ما تنكسر.**
- **الإعداد الجديد ينحفظ** (يرجع لمصدر الإعدادات الواحد) فيبقى بعد أي إعادة تشغيل —
  **ما يرجع القديم أبدًا.**
- **الذرة تعلن أيّ إعداد يتغيّر فورًا، وأيّهم بدّه إعادة تحميل سريعة** (الثقيل، متل
  اتصال مصدر) — عشان اللوحة تعرف قبل.
- **بعد التطبيق تعلن «طبّقت»** (حدث) → اللوحة تشوفه، والنواة **تسجّل: مين غيّر شو
  وإيمتى.**

**⚙️ كيف تتبنى (مهم — مرّة، مش ٢٠٠ مرّة):**
- **الآلة تتبنى مرّة وحدة بالأساس المشترك** اللي كل ذرة بترثه — الاستقبال · التحقّق ·
  رفض الغلط · الحفظ · الإعلان، **كلها هناك مرّة.**
- **الذرة بس تعمل شغلتين صغار:** (١) تعلن إعداداتها (قاعدة ١١)، (٢) تقول «لمّا يتغيّر
  هالرقم، هيك بستعملو». الباقي بيجي من الأساس **مجانًا.**
- **ممنوع كل ذرة تكتب الآلة من جديد** — ٢٠٠ نسخة = الفخّ القديم. الآلة بالأساس، لا بالذرة.
- **البوّابة (الصراط) بترفض** أي ذرة تعلن إعداد وما بتعرف تغيّرو صح — **فما تمرق ذرة ناقصة.**

## القاعدة ١٥ — الذرة ما تعرف الحوكمة

- **الذرة ما تستورد ولا جزء من طبقة الحوكمة.**
- **الذرة ما تنشر أحداث خاصة بالواجهة، ولا تنتظرها.**
- **الذرة تنشر حقائق أعمال خام بس** — والحوكمة هي اللي تراقب الخام، تعرضه، وتتحكّم
  **عبر بوّابة الأوامر.**
- **حذف الحوكمة أو استبدالها = ما بيغيّر ولا سطر جوّا الذرة.**
- **ليش:** الحوكمة طبقة **مستقلّة فوق النواة**، مش جزء منها (لو ماتت، النظام والتداول
  بيكمّلوا). هاد عزل متل قاعدة ٧ — بس تجاه الطبقة اللي فوق.

## القاعدة ١٦ — تسمية أحداث الحالة تخلّيها قابلة للإعادة (تسلسل ٠٣ + ٠٨ + ١١)

- حدث يحمل **حالة أعمال** (سعر · تحليل · محفظة · مخاطر…) → اسمه **بلاحقة حالة**
  (`.state` / `.snapshot` / `.synced`) → الناقل يعيده للمشترك المتأخّر → **اللوحة
  تلحق فورًا، ما تفضى عند الفتح.**
- حدث **أمر** (تنفيذ / شراء / بيع) → **بلا** لاحقة حالة → **ما يُعاد** (خط أمان، لا تنفيذ مزدوج).
- **ليش:** الناقل يعيد **الحالات بس** (٠٣). ذرة تنشر حالة باسم بلا لاحقة = صفحتها **تفضى كل فتح**.
- **✅ الآليّة مبنيّة بالنواة (ورقة ٠٨ — لا تخمين):** `subscribe_all` + إعادة آخر حالة عند الاشتراك جاهزين.
- **🛡️ حارس الأوامر بالنواة (ورقة ٠٨):** أي حدث اسمه فيه أثر أمر —
  `order` · `buy` · `sell` · `execute` · `cancel` · `final_decision` · `command` · `submit` · `send`
  — **ما يُخزَّن ولا يُعاد أبدًا، حتى لو سُمّي بلاحقة حالة بالغلط.** فـ**ممنوع** تسمّي حدث أمر بلاحقة
  حالة، والنواة كمان بتحميك من التنفيذ المزدوج.

## القاعدة ١٧ — المانيفست يطابق الكود (لا كذب بالعقد)

- كل حدث الذرة **تنشره أو تسمعه فعلًا** لازم يكون **معلَن** بالمانيفست (`publishes` / `subscribes`).
- نشر/اشتراك بلا إعلان = **خرق يمنع الاعتماد** (يفرضه المدقّق) — فما يصير المانيفست يكذب على الكود.

## القاعدة ١٨ — حدود الكود التقنية (كلها يفرضها `validate_atoms`)

- **لا `print`** ولا كتابة مباشرة لـ`stdout`/`stderr`.
- **`atom.py` ≤ ٣٠٠ سطر فعلي** — **⚠️ استثناء الطبقات الحسّاسة** (تنفيذ · أوامر · مخاطر · جسور مال
  حقيقي): **ما نبتر منطقًا لازمًا لأجل الرقم.** الحلّ المفضّل: **قسّم لملفات مساعدة** (`atom.py` ≤٣٠٠ +
  `<اسم>.py` مساعِدات يستوردها → المدقّق يبقى أخضر). وإن تعذّر التقسيم نظيفًا: **تجاوز موثّق** (يحتاج إعفاء
  بالمدقّق = تنسيق نواة). **الجودة الحسّاسة قبل الرقم** (قرار المالك 2026-08-04).
- **لا أرقام سحرية** (كل رقم يتسمّى أو ينعلن بالإعدادات).
- **دوال دورة الحياة موجودة وكلها `async`** (`initialize`/`start`/`stop`/`shutdown`).
- **ما ترفع استثناء بالإقلاع** (فشل الربط = حالة صحية `UNHEALTHY`، لا رمي).
- **ما تصل لخاص ذرة تانية** (احترام العقد).
- **لا اتصال شبكي مباشر:** ممنوع `socket`/`requests`/`urllib`/`http`/`websockets`/`aiohttp`/`httpx`
  — الشبكة **بس** عبر بوّابة ذرية موثّقة.
- **`timeout_ms` < `interval_ms`** بإعداد الصحة.
- **الاعتماديات معلنة وموجودة** (ما تعتمد على ذرة مو موجودة).
- **اسم وحدة مو عام** (ما يتصادم مع غيرها).
- **حرس معرّفات SQL:** أي اسم جدول/عمود يجي من الإعداد ويُركّب باستعلام لازم يُفحَص `^[A-Za-z0-9_]+$`
  قبل الاستعمال — يمنع حقن SQL من إعداد خبيث (درس 714/716/717 · مخازن SQLite بـWAL/busy_timeout).

## القاعدة ١٩ — الجوّا إنكليزي · البرّا عربي (المالك يقرا البرّا)

- **الجوّا (يقراه النظام):** `atom.py` + `manifest.yaml` = **إنكليزي نظيف، صفر عربي** (قواعد ٣/٤).
- **البرّا (يقراه المالك):** **اسم مجلد الذرة** + `الشرح.md` + `التاريخ.md` = **عربي** — المالك ما بيعرف إنكليزي.
- **شكل اسم المجلد:** الرقم + اسم عربي — مثلًا `101_مدير_بيانات_السوق`.
- **آمن (مُتحقَّق):** النظام ما بياخد الهوية من اسم المجلد — بياخدها من `manifest.yaml` (الـ`id`)،
  وبيحمّل الكود بالمسار. فاسم المجلد العربي **ما بيكسر شي.**

## القاعدة ٢٠ — الترقيم منطقي · المجلد حرّ (لا شجرة إلزامية)

- **الرقم = تسمية منطقية بس** — يدلّ على طبقة الذرة. **بلوكات الطقم الفعلية (أرقام قديمة مُبقاة):**
  000 تحتية · 100 استقبال بيانات · 150 تحليل · 500 مخاطر/تنفيذ · 600 جسور/حساب/تنفيذ · 700 تخزين/صيانة ·
  750 مراقبة · 800 نسخ. للفهم والتوسّع.
- **مكان المجلد حرّ تمامًا** — أي ذرة بأي مجلد/عمق، مجمّعة أو مفرّقة. **لا شجرة إلزامية.**
- **آمن (مُتحقَّق):** النواة تلقى الذرات بـ**فحص متكرّر لكل `manifest.yaml`** مهما كان اسم/عمق
  المجلد (`manifest_loader.py`)؛ الهوية من `id` بالمانيفست، مو من المكان.
- **الخلاصة:** الترقيم للفهم · المجلدات لراحة المالك · النظام ما بيفرق معه وين الذرة.
- **🔒 قرار المالك (2026-08-04): الأرقام القديمة تبقى كما هي لكل الذرات — لا إعادة ترقيم.**
  سكيم البلوكات (000 تحتية · 100 استقبال بيانات · 600 جسور · 700 تخزين…) = **دليل منطقي فضفاض للفهم، مش إلزام.**
  والنواة تبقى بأرقامها المهرّدة (`CLOCK_OWNERS` · `NETWORK_ALLOWED`) بلا تغيير. المجلد يبقى عربي (قاعدة ١٩).

## القاعدة ٢١ — الكود ويندوز-سليم (cross-platform)

- **جهاز المالك ويندوز** — الكود لازم يشتغل عليه بلا تعديل.
- **ممنوع استدعاءات خاصة بلينكس:** `os.fchmod` · `os.chmod`/صلاحيات يونكس · شيبانغ يُعتمَد عليه · مسارات
  ثابتة بنمط يونكس. استعمل `pathlib` والمكتبة القياسية cross-platform (متل `asyncio`/`time`).
- **ممنوع اعتماد على باش (`.sh`):** أي أداة **يشغّلها المالك** = **PowerShell أو بايثون cross-platform**، مو سكربت باش.
  (درس حيّ: حارس النواة `install_guard.sh` باش + git → ميت على ويندوز؛ ونهايات الأسطر كسرت بصمة قبل ما تُطبَّع.)
- **نهايات الأسطر:** ما يعتمد الكود على `\n` مقابل `\r\n`؛ أي بصمة/مقارنة تُطبَّع أولًا.
- **لا باش أبدًا — حتى تحليل كلود الداخلي (قرار المالك 2026-08-05 · مطلق):** كل تشغيل/فحص/اختبار/تحليل =
  **PowerShell أو بايثون على ويندوز**، لا أداة Bash ولا Git Bash ولا صياغة لينكس. الثغرة القديمة («الباش
  للتحليل الداخلي عند كلود فقط») **أُلغيت** — منها مرق خرق حيّ (استعمال أداة باش · صحّحه المالك).

## القاعدة ٢٢ — مفاتيح الهوية على كل حدث (من «دورة حياة النظام»)

عشان النظام يتوسّع (حسابات · رموز · صفقات · نماذج) **بلا إعادة بناء**، كل حدث يحمل مفاتيح هويّته:

| المفتاح | على أي حدث | مصدره (source of truth) |
|---|---|---|
| `account_id` | يمسّ حساب/مركز/أمر/مخاطر/محفظة | جسر الحساب (وحده يعرف أي حساب) |
| `symbol` | يمسّ رمزًا | التكّة/المصدر |
| `ticket` | يمسّ صفقة | تأكيد التنفيذ |
| `strategy_id` | من/عن استراتيجية | الاستراتيجية |
| `model_id` · `model_version` · `confidence` · `prediction` | تحليل/تداول مرتبط بالذكاء | النموذج |

- **ليش:** التوسّع بلا كسر + معرفة أي نموذج قرّر وقياس أدائه والتراجع عنه لو ساء.
- **كيف (مرّة، مش ٢٠٠ مرّة):** المفاتيح **تُورَّث بالناقل زي `trace_id`** (تسلسل مع ٠٤) — تُضبط عند
  المصدر وتنساب مع السلسلة تلقائيًا؛ **الأساس المشترك يحقنها، لا كل ذرة تكتبها.** (تنسيق مع النواة:
  امتداد لوراثة الأثر ٠٤.) الذرة تُمرّر المفاتيح اللي وصلتها، وتضيف اللي هي مصدره فقط.
- **مرجع كامل:** `سياق/دورة حياة النظام (التصميم الكامل).md` (١٨ مرحلة).

## القاعدة ٢٣ — الفصل بطبقتين: تنفيذ (لكل حساب) · محفظة/حوكمة (رؤية) — محسوم 2026-08-05

**عزل الحسابات = بالطبقة/النسخة، لا بعمود يميّز جوّا «دماغ» مشترك.**

- **طبقة التنفيذ (Execution Plane) — لكل أنبوب/حساب مستقل:** الجسر · البيانات · حالة الحساب ·
  الاستراتيجيات · **مخاطر الحساب** · التنفيذ · إدارة الصفقات. **نفس كود الذرة، نسخة لكل طرفية/حساب**
  (لا قسم/كود لكل حساب). **لا أنبوب يقرأ حالة أنبوب آخر.** **مخاطر A توقف A فقط** (خسارة حساب ما توقف
  غيره — إلا سياسة حوكمة عليا صريحة). سوينغ/سكلب = **إعداد استراتيجية الأنبوب** (خيط جوّا الذرة)، مو عزل ببيانات.
- **طبقة المحفظة/الحوكمة (Portfolio Plane) — عبر الحسابات · قراءة فقط:** تجمّع حالة كل الحسابات →
  إجمالي رأس المال/المخاطر/الأداء/تقارير/تنبيهات/لوحات. **تقرأ فقط** (`account.state` ·
  `position.state` · `execution.confirmed` · `market.outcome.realized` · `risk.status`) وتنتج
  `portfolio.*`. **ما ترسل أمر شراء/بيع أبدًا.**
- **القاعدة الذهبية:** الأنابيب ما تتلامس **بالقرار** · تلتقي **بالرؤية** عند المحفظة. `account_id` =
  **مفتاح الرؤية عبر الأنابيب** (لهيك هو بمخازن الحساب — قاعدة ٢٢، صح وضروري).
- **الأثر على تصميم الذرة:** كل ذرة **بلا حالة عامة مشتركة** ومساراتها (جسر/مخزن) **من الإعداد** → قابلة
  للتنسيخ نسخة لكل أنبوب. أي ذرة «تجمّع كل الحسابات» أو «تراقب/تنبّه/تعرض» = **طبقة المحفظة/الحوكمة، لا
  التنفيذ** (تسلسل مع قاعدة ١٥).
- **ترتيب البناء:** بيانات/جسور → مسار التداول الكامل → أنبوب واحد يشتغل منفرد → **ثم** المحفظة (650/651).
  المحفظة **ما عندها شي تجمّعه قبل وجود الأنابيب.**
- **مرجع كامل:** `سياق/حالة بناء الذرات.md` (قسم «الفصل المعماري — طبقتان»).

---

## البوّابة (الصراط) — كيف تُعتمد الذرة (٣ فحوص · التفاصيل بورقة ٠١، مرجع)

1. **بنيوي:** `validate_atoms` يفحص كل قواعد هالدستور — لا يمرّ خرق.
2. **حيّ — شرط التسليم الإلزامي (لا تُعتمد ذرة بدونه · قرار المالك 2026-08-05):** الذرة تقلع على **النواة
   الحقيقية الشغّالة** وتتصرّف صح **على بيانات حقيقية** (جسر/حساب المالك)، **والمالك يشوفها حيّة بعينه** —
   مو بس بنيتها سليمة (الفحص ١) ولا موك.
3. **الحكم:** ✅ تثبت · 🔧 تُصلَّح (بالسبب) · ⚠️ تُعرَض على المالك.

**⚠️ المدقّق والموك يكذبوا بالاتجاهين (درس حيّ 2026-08-05 · نوريه ما ندّعي):** الموك ينجح والذرة مكسورة حيّ ·
والموك يفشل والذرة شغّالة حيّ. **مُثبَت:** موك 611/601/609/618 فشل وهنّ شغّالين على الجسر الحقيقي (تلوّث
`NQ_BRIDGE_DB` يتجاوز مسار الاختبار) · و611 مرّ المدقّق وهو يسيء تصنيف حالته حيّ. **فـ«خلصت» = مرقت الفحص ٢
(حيّ على الحقيقي)، لا «مدقّق أخضر». المدقّق والموك أرضية، مش إثبات.**

**درس الجوع (٤١٣):** ذرة `degraded` بسبب **جوع مدخل من عطل أعلى** = «مجوّعة»، **مو «فاشلة»**
— ما نلوم ذرة بريئة جوّعها غيرها.

---

**🔒 الدستور مفروض بالكود، مو ورق:** المدقّق `validate_atoms` بيفرض فعلًا: منع العربي بـ`atom.py`
والمانيفست + منع الترويسة (قواعد ٣/٤) · الوقت (١٣) · العزل (٧) · الاختبارات (٩) · مطابقة المانيفست
(١٧) · وحدود الكود (١٨). **المخالف ما بيمرق البوّابة.**

**⚠️ لكن المدقّق يفرض الفحص ١ (بنيوي) فقط. الفحص ٢ (حيّ على النواة الحقيقية + بيانات حقيقية، المالك شايف)
ما ينفرض بآلة — لازم تشغيل فعلي، وهو شرط التسليم. مدقّق أخضر ≠ ذرة شغّالة.**

---

**هاد الدستور الموحّد للذرات — ٢٣ قاعدة + بوّابة.** كل ذرة جديدة أو مُعاد بناؤها **لازم
تطابقه قبل اعتمادها.** ورقة ٠١ (الصراط) تبقى **مرجعًا** (تفاصيل البوّابة)، ما تُحذف.
أي قاعدة جديدة من المالك تُضاف هون فورًا.

---

## 📋 ملاحظات عامة (تواصل عبر الورق — شاملة · علمية · مشروحة · لأي محادثة)

### ملاحظة: آليّة أحداث الحالة — الناشر والمستهلك معًا
**المبدأ:** الناقل يحفظ **آخر قيمة لكل حدث باسمه الكامل** ويعيدها **فورًا للمشترك المتأخّر** — لكن
**فقط** للأحداث المصنّفة «حالة» (اسمها ينتهي بلاحقة `.state` / `.snapshot` / `.synced`). الأحداث
التي تحمل أثر أمر (`order`/`buy`/`sell`/`execute`/`cancel`/`final_decision`/`command`/`submit`/`send`)
**لا تُخزَّن ولا تُعاد أبدًا** (حارس مزدوج ضد التنفيذ المكرّر — ورقة ٠٨).

**الأثر على الطرفين:**
- **الناشر (الذرة):** أي حالة أعمال تُنشَر باسم **ينتهي بلاحقة حالة**، وإلا لا تُعاد (قاعدة ١٦).
- **المستهلك (اللوحة / أي مشترك):** لازم يشترك بالاسم **الكامل مع اللاحقة**؛ الاشتراك بالجذر بلا لاحقة
  **لا يستقبل شيئًا** ولا يلحق الحالة الحاليّة.

**لماذا (علميًا):** المشترك المتأخّر أو المُعاد تشغيله كان يفوته آخر حالة نُشرت قبل اشتراكه → عمى إقلاع
وصفحات فارغة (نفس جذر توقّف الصفقات ٦١٩→٦٥١→٤١٣). الإعادة عند الاشتراك تحلّه **مرّة واحدة في الناقل**
لكل النظام، بدل ترقيع كل ذرة ومستهلك على حدة — والتمييز باللاحقة (حالة تُعاد / أمر لا يُعاد) يحفظ خط
الأمان ضد التنفيذ المزدوج.
