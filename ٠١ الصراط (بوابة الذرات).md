# ورقة بناء — الوحدة الأولى: الصراط (قالب الذرة + بوابتها)

**نوع الورقة:** تشريح تنفيذ كامل — يُقرأ ويُجاب على **كل سؤال** فيه **قبل** أي سطر كود.
**التاريخ:** 2026-08-03 · **الوحدة:** الصراط (بوابة جودة الذرة) · **الحالة:** مسودة بانتظار قرار المالك

> **طريقتنا:** ورق قبل تنفيذ. لا نبني على الباطل، لا نرقّع، نرفع لا نبسّط، والعزل والدقة
> والجودة خطوط حمر. لا كود قبل ما تُجاب أسئلة القسم (٨) وتقول "نفّذ".

---

## ١. ما هي هذه الوحدة (بلغة بسيطة)
**الصراط** = قالب واحد كل ذرة تمشي عليه، + **بوابة من ٣ فحوص** تعدّيها كل ذرة قبل ما
نعتبرها "مثبّتة":
1. **مطابقة القالب بنيويًا** — شكلها وعقدها صحيحان.
2. **فحص حيّ على الجهاز** — تُقلع فعليًا وتنتج/تتصرّف صح (مو بس بنيتها سليمة).
3. **تثبت وتعدّي** — أو تُصلَّح لحدّ ما تنضبط، أو تُعرَض عليك.

## ٢. الهدف و"ليش"
- **يثبّت الـ٢٠٠:** يجيب كل ذرة لوضع موحّد صحيح **مُتحقَّق حيًّا**، بلا فتح ملفاتها.
- **يوحّد أي ذرة جاية** (+٧٥...) على نفس الصراط.
- **الناتج:** أمر واحد يصنّف كل ذرة: ✅ مثبّتة · 🔧 تحتاج شغل (والسبب) · ⚠️ معروضة عليك.

## ٣. الموجود نبني عليه (لا نعيد اختراع — قِسته بعيني بالكود)
- **القالب:** `Desktop\NEW_ATOM_GUIDE.md` — الستّ ملفات (atom·manifest·api_spec·tests·
  README·ATOM_SPEC). **كتبته إنت.**
- **الفحص البنيوي #١ (موجود وقوي):** `scripts/validate_atoms.py` — مدقّق AST يفحص:
  العزل (لا استيراد ذرة)، لا `print`، حدّ ٣٠٠ سطر، لا أرقام سحرية، دوال دورة الحياة
  async، **مطابقة المانيفست للكود** (publishes/subscribes)، مصدر الوقت، timeout<interval،
  الاعتماديات موجودة، وجود اختبارات. → **جاهز، نستعمله كما هو.**
- **فحص الاختبارات (موجود):** `scripts/test_atoms.py` — يشغّل اختبارات كل ذرة معزولة
  بعملية مستقلة. → جزء من #١.
- **الناقص = الفحص الحيّ #٢:** هل الذرة تُقلع **فعليًا** على النظام الشغّال وتنتج صح.

## ٤. التشريح الدقيق — شو راح أعمل بالضبط
- **ملف جديد واحد فقط:** `scripts/atom_gate.py`. **لا تعديل على أي ذرة ولا على النواة**
  في هذه الوحدة (فحص فقط).
- **ماذا يفعل `atom_gate.py`:**
  1. يشغّل `validate_atoms.py` (الفحص #١) على ذرة أو الكل.
  2. يشغّل `test_atoms.py` (الاختبارات).
  3. **الجديد — الفحص الحيّ #٢:** يقرأ حالة الذرة من `http://127.0.0.1:8000/api/atoms/<id>`
     على النظام الشغّال، ويحكم:
     - `running` + `healthy` → ✅.
     - `degraded` بسبب **جوع مدخل** (مثال: "لم أستقبل مدخلاتي") → **يميّزها "مجوّعة من عطل
       أعلى" لا "فاشلة"** (درس ٤١٣: لا نلوم ذرة بريئة جوّعها غيرها).
     - `failed` أو خطأ فعلي → 🔧 مع السبب.
  4. يطبع تصنيفًا واحدًا مقروءًا لكل ذرة.
- **اسم مبدئي، يتغيّر بأمرك.**

## ٥. فحص الخطوط الحمر
- **العزل:** أداة **فوق** الذرات، تفحص فقط — ما تدخل فيها ولا تكسر عزلها. ✓
- **الرفع لا التبسيط:** تضيف فحصًا (الحيّ) لم يكن موجودًا = رفع. ✓
- **لا تلمس نواة ولا ذرة** في هذه الوحدة. ✓

## ٦. الإثبات الحيّ (كيف نتأكد إنه اشتغل — تشوفه بعينك)
- `python scripts/atom_gate.py --atom 618` (ذرة منتِجة) → لازم ✅.
- `python scripts/atom_gate.py --atom 451` (مجوّعة من باگ ٤١٣) → لازم "مجوّعة من عطل
  أعلى"، **لا** "فاشلة".
- أعرض المخرَج الحيّ كامل قبل ما نعتبرها ناجحة.

## ٧. المخاطر والتراجع
- **صفر خطر عمليًا:** ملف جديد واحد، بلا لمس أي شغّال. التراجع = حذف الملف.
- **قيد صادق:** الفحص الحيّ **لكل الـ٢٠٠ أصدق بعد م١** (باگ ٤١٣ يجوّع ذرات بريئة).
  فبهالوحدة **نبني الأداة ونثبتها على أمثلة**، ونشغّلها شامل بعد م١.

## ٨. أسئلة مفتوحة — جاوبها قبل ما أنفّذ (لا كود قبلها)
1. **الأهمّ — النطاق:** الصراط هو الوحدة الأولى الصح؟ ولا تفضّل نبدأ بشي آخر (م٠-git،
   أو تشريح البناء الكامل أول)؟
2. **الصيغة:** هالورقة (أقسامها) بتجاوب "كل سؤال" اللي بدك؟ ناقصها شي؟
3. اسم `atom_gate.py` مناسب، ولا تفضّل اسمًا؟
4. الفحص الحيّ عبر `/api/atoms` — تمام، ولا مصدر آخر؟
5. القالب: `NEW_ATOM_GUIDE` كما هو، ولا فيه تعديلات؟

## ٩. قرار المالك
☐ نفّذ كما هو  ·  ☐ عدّل (اكتب التعديل)  ·  ☐ أوقف / غيّر الوحدة

---
**ملاحظة:** نكتب ورقة تشريح مثل هذه **قبل كل وحدة تنفيذ**، وتُحفظ في مجلد `سياق` بأسماء عربية.
