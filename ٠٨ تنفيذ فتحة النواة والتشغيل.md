# ٠٨ — فتحة النواة V2.0: سجل التنفيذ والتشغيل

**نوع الورقة:** سجل عملي (مو تصميم) — شو **انبنى فعلًا** وكيف **تُشغَّل النواة**.
**التاريخ:** 2026-08-04 · **الحالة:** ✅ منفَّذة · شغّالة حيّة · **مختومة Core V1.8.0** (بكلمة المالك «اختم» 2026-08-04، بعد مراجعة دقيقة كاملة). ⚠️ اقرأ «تصحيحات 2026-08-04» تحت.
**مرجعها التصميمي:** أوراق ٠٢–٠٧ (التشريح) · **اختبارها الدائم:** `tools/test_core_v2_opening.py`.

> **⚠️ تحديث تشغيل (2026-08-05):** النواة الآن على المنفذ **:8010** (لا :8000 — محجوز لنظام V7 القديم عند المالك) · مختومة **V1.8.0** (كل ذكر «2.0.0» أو «:8000» تحت = تاريخي). التشغيل: `$env:PYTHONUTF8=1; venv\Scripts\python.exe scripts\run_core.py` · فحص الختم: `... scripts\freeze_core.py verify`. الحالة الحيّة الكاملة: بلوك «تحقّق كامل 2026-08-05» في `٠٠ السياق`.

---
## 🔧 تصحيحات 2026-08-04 (بعد فحص كلود + قرار المالك) — تسبق وتَنسخ ما دونها عند التعارض
1. **الترقيم: 2.0.0 → 1.7.0.** الفتحة (٠٢–٠٧) متوافقة خلفيًا (لا كسر عقد) = رفعة صغيرة؛ و2.0.0 كان يرفض كل ذرة تشترط `core_version<2.0.0` فتُستبعد عند الإقلاع. فكل ذكرٍ لـ«2.0.0» تحت = تاريخي، والصحيح **1.7.0**.
2. **الختم أُزيح** إلى `core/CORE.lock.retired_2026-08-04` (النموذج السابق ختم بلا طلب المالك؛ قرار «إمتى نختم» يبقى للمالك). فكل «مختومة/سليمة ومطابقة» تحت = تاريخي؛ الحالة الآن **غير مختومة**.
3. **إثبات حيّ:** الذرتان **850 + 851 تُقلعان** الآن (`نجاح=True · بدأت=[850,851] · فشلت=[] · استُبعدت=[]`)، و850 على :8001. (كانتا مُستبعَدتين بسبب 2.0.0.)
4. **فجوة توصيل الساعة (٠٥) — انكشفت وانسدّت:** الناقل كان يملك `set_time_offset` لكن **لا أحد يغذّيه** (صفر اشتراك على `time.utc.synced`) → الإزاحة صفر أبدًا، والاختبار كان يخدع لأنه ينادي `set_time_offset` مباشرة. أُضيف الاشتراك بالمُقلِع (`Bootloader._on_time_synced`) + **اختبار تكاملي** عبر `boot()`. صارت **١٦ اختبار أخضر**.
5. **تنظيف:** `tools/test_trace_middleware.py` (تست ميت يفشل) أُزيح إلى `.retired_2026-08-04`؛ وتعليق `run_core` صُحّح («حُذف» → «أُبطِل»).
6. **✅ الطقم الكامل شُغّل (pytest منصَّب):** `tests/core/` → **103 نجحت · 4 فشلت**، والأربعة كلها `test_core_is_frozen` تفشل **لأن الختم مُزال بقرار المالك** (الحارس يشتغل صح: يفشل عمدًا حين تكون النواة غير مختومة) — ترجع خضراء لحظة إعادة الختم. **صفر فشل حقيقي بمنطق النواة.** (ملاحظة صغيرة: `@pytest.mark.timeout` في `test_end_to_end` غير مفعّل لأن `pytest-timeout` غير منصَّب — والتستات نجحت رغم ذلك.)
---

> ليش هالورقة: الأوراق ٠٢–٠٧ هي **التصميم**؛ وهاي **التنفيذ** — عشان أي محادثة جاية تعرف شو
> صار بالضبط وكيف تشغّل، بلا ما تعيد الفحص ولا تخمّن. (نفس مبدأ: نسجّل أول بأول · صفر إعادة شرح.)

---

## 🆕 طبقة ٢ — المرحلة ١ / حركة ١ (ورقة ١١): تعرية النواة من العرض ✅ (2026-08-04)
النواة صارت **API خام + بثّ بس، بلا أي HTML** (أنا — مسؤول النواة — نفّذتها وفحصتها):
- شِلت مسار `GET /` + `_DASHBOARD_HTML` + استيراداته (`Path`/`FileResponse`) من `core/api/app.py` (F401 نظيف).
- العلَم `enable_dashboard` → **`enable_api`** (`config/core.yaml` + `scripts/run_core.py`) — كان يطفي **كل** الـAPI، هلّق الخام يبقى شغّال.
- `core/api/dashboard.html` **اتنقل برّا النواة** → `var/archive/dashboard.html.retired_2026-08-04` (تراجع سهل، مو محذوف).
- **إثبات حيّ:** `GET / → 404` · `GET /api/atoms → 200 []` · `/api/health → 1.7.0` · **طقم النواة: 103 نجحت / 4 فشلت** (الأربعة = حارس الختم `test_core_is_frozen`، يفشل عمدًا لأن النواة غير مختومة). **صفر انحدار.**
- **② firehose ✅:** `EventBus.subscribe_all` — الناقل يبثّ **كل** حدث خام لطبقة ٢ (برّا العملية)، النواة تبقى غبية (تمرير بلا تفسير)، محكوم بـ`_DroppingQueue`. `app.py /ws/events` صار يستعمله (شِلنا القائمة الثابتة `_LIFECYCLE_EVENTS`).
- **③ توسيع replay ٠٣ ✅ (بأمان):** المشترك على الكل (اللوحة) ياخد **آخر حالة لكل تدفّق عند الاتصال** → ما تفضى عند الفتح. **حارس أوامر (`_COMMAND_MARKERS`: order/buy/sell/execute/cancel/final_decision/command/submit/send): أي حدث فيه أثر أمر لا يُخزَّن ولا يُعاد أبدًا** — دفاع ضد التنفيذ المزدوج، حتى لو سُمّي بلاحقة حالة بالغلط.
- **⚠️ شرط تغطية صفحات الأعمال (تنسيق مع `قالب الذرة`):** الإعادة باللاحقة (`.state`/`.snapshot`/`.synced`). عشان لوحة الأعمال ما تفضى، **الذرات الجديدة لازم تنشر حالتها للّوحة بأحداث تنتهي بهالّلواحق** — قاعدة تُضاف لـ`قالب الذرة` (نطاق المالك/الذرات)؛ ما خمّنتها من عندي (ما نبني على افتراض).
- **الإثبات:** فتحة V2.0 = **21 اختبار أخضر** (منها ٥ للـfirehose/الحارس) · طقم النواة **103 نجحت / 4 فشلت** (الأربعة = حارس الختم، متوقّع) · `GET / → 404` · `/api/atoms → 200`. **صفر انحدار.**
- **المرحلة ١ (حصّة النواة) خلصت.** الجاي = طبقة ٢ (الحوكمة/العرض) + الذرات — **مو النواة، مو أنا**؛ صاروا يقدروا يبنوا (الخام + الإعادة + الـAPI جاهزين).
- **🔒 الختم (بكلمة المالك «اختم» 2026-08-04، بعد مراجعة دقيقة + إكمال):** قبل الختم أُضيف **أثر الأوامر بالجورنال** (نجاح + فشل، اتّساقًا مع بقية الإخفاقات) + توثيق صادق (app.py/event_bus). **Core V1.8.0 مختومة · ٢٣ ملف · بصمة `8d813dfa…` · `freeze_core.py verify` = «سليمة ومطابقة».** الإثبات: **107/107** طقم النواة (الأربعة الحارسة صارت خضر) · فتحة V2.0 **21/21** · إقلاع حيّ نظيف (`نجاح=True`). **قرار إعادة الفتح لاحقًا = `freeze --reseal` بعد رفع الرقم فقط** (مبدأ «لا نفتح النواة مرتين»).

## ١. شو انبنى (الأوراق ٠٢–٠٧ صارت كود حيّ)
- **٠٢ العيون:** الناقل صار يعدّ كل حدث (نُشر · سُلّم · بلا مشترك · مهلة · خطأ · أُعيد) + `stats()` لقطة قراءة فقط.
- **٠٣ الحالة عند الاشتراك:** الناقل يحفظ آخر حدث «حالة» ويعيده **فورًا** للمشترك المتأخّر → **يفكّ جذر توقّف الصفقات (٦١٩→٦٥١→٤١٣)**. الأوامر لا تُعاد أبدًا (لا تنفيذ مزدوج). المعيار: لاحقة الاسم (`.state`/`.synced`/`.snapshot`).
- **٠٤ وراثة الأثر:** كل حدث يُختَم بـ`trace_id`+`event_id`+`parent_event_id`+`parent_event` → شجرة سببية بدل أحداث مقطوعة.
- **٠٥ الساعة:** ختم الناقل = الوقت المصحّح (خام + إزاحة ٦٠٨)؛ **لا يدوس وقت مصدر خارجي** (تكّة MT5 تبقى بوقتها).
- **٠٦ المهلات:** دالة `call_lifecycle` موحّدة (مهلة على كل نداء دورة حياة خارج الإقلاع — hot_reload/health/api) · تطهير `sys.modules` عند السحب · **المُقلِع صار يحترم `startup_mode`** (كان يتجاهله فالمطفي يرجع كل إقلاع) · خُطّاف تشغيل الذرة الموقّفة (`POST /api/atoms/{id}/start`).
- **٠٧ إرجاع الالتفافات:** توريث الأثر رجع جوّا الناقل؛ `scripts/trace_middleware.py` أُبطِل (جذع مهجور)؛ المُشغّل صار «تشغيل + توصيل» فقط.

## ٢. الملفات
- **مُعدَّلة:** `core/event_bus.py` · `core/logger.py` · `core/bootloader.py` · `core/hot_reload_service.py` · `core/health_manager.py` · `core/api/app.py` · `core/__version__.py` (→ 2.0.0) · `scripts/run_core.py`.
- **جديد:** `core/lifecycle.py`.
- **مُبطَل (جذع مهجور):** `scripts/trace_middleware.py` · `tools/test_trace_middleware.py`.
- **نسخة أمان قبل التعديل:** `var/backups/core_pre_V2_20260804_060310`.

## ٣. الإثبات (لا ادّعاء)
- **١٥ اختبار أخضر:** `python tools/test_core_v2_opening.py` → **١٥/١٥**.
- **إقلاع حيّ:** نجاح=True · ٠ ذرة · ٠ فشل · انطفاء نظيف.
- **الختم:** `Core V2.0.0` · **٢٤ ملف** · بصمة `4af5d77d36d66e02aa159b69222fae2d6306029a69ccffe6f1de9a3a19f44543` · `python scripts/freeze_core.py verify` = «سليمة ومطابقة».

## ٤. كيف تُشغَّل النواة (عملي — تعلّمناه بالتجربة اليوم)
**الأمر:** `python scripts/run_core.py` (من `C:\Users\NQ\QUANT_NQ`).
- **المنفذ:** اللوحة/الـAPI على `http://127.0.0.1:8000`. `/api/health` بيرجع `core_version` — لازم يطلع **2.0.0** (لو طلع 1.6.0 = نسخة قديمة شغّالة، أوقفها أول).
- **الترميز (مهم):** شغّل مع `PYTHONIOENCODING=utf-8` (وبنافذة أوامر: `chcp 65001`) — وإلا العربي بالسجل بيتكسّر (؟؟؟).
- **الأسرار:** المخزن LOCKED (بلا مفتاح) — النواة تكمل عادي، غير مؤذٍ.
- **الإيقاف:** `Ctrl+C` (نظيف)، أو سكّر النافذة.
- **⚠️ الاستمرار:** النواة عايشة **جوّا العملية/النافذة اللي شغّلتها** — إذا انسكّرت النافذة أو انتهت الجلسة اللي شغّلتها، **بتوقف**. للتشغيل الدائم المستقلّ: تُشغَّل كعملية خلفية ثابتة (لا كنافذة تابعة لجلسة أداة عابرة).
- **⚠️ نسخة واحدة فقط:** لو صار منفذين على :8000، وحدة بتفشل تحجز المنفذ وتصير بلا لوحة. أوقف القديمة قبل الجديدة.

## ٥. الحالة والتالي
- **الحالة:** النواة V2.0 نظيفة · مختومة · شغّالة حيّة (٠ ذرة).
- **التالي (بالخطة):** ② **طبقة الحوكمة/اللوحة (م٢)** — لوحة مقروءة لبشر + سجل موحّد بزر + محاسبة الأحداث المُسقَطة؛ ثم ③ **الذرات على الصراط (ورقة ٠١)**.
- **الصفقة بترجع** لمّا نبني الذرات فوق هالنواة النظيفة — والجذر **٠٣** هو اللي بيخلّيها تشتغل وقتها.
- **قرار تشغيل مفتوح للمالك:** نخلّيها شغّالة (①) دائمة مستقلّة بالخلفية، ولا (②) بنافذة ظاهرة تبقى مفتوحة؟ (الترمنال الخام مفضّل للمالك للإثبات — لا لوحات مصقولة.)
