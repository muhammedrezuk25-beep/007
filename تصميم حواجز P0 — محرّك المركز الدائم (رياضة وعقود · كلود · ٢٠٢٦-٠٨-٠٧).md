# تصميم حواجز P0 — محرّك المركز الدائم (رياضة وعقود · كلود · ٢٠٢٦-٠٨-٠٧)

> **الوضع: بحث + تصميم — لا بناء.** هاي الورقة تحسم حواجز P0 اللي قال المالك إنّها **شغل كلود** (قرارات تصميم/رياضة، لا أسئلة له):
> «حواجز P0 المتبقّية = قرارات تصميم/رياضة (شغل كلود) — أبحثها وأحسمها وأسجّلها.» (`شرح المالك` · سطر ٢٨٧)
> **قابلة للمراجعة والتصحيح — لا يُبنى منها كود إلا بأمر المالك.** المرجع الأعلى = `شرح المالك — تدفّق الأصول والأوامر`. حدود: **لا نواة · لا 350 · لا حوكمة (فولت).**
> الرقم الأصليّ لكلّ فكرة من مصدره: `شرح المالك` (رؤية) · لوحة `NQ_Manual v5.5` (معادلات) · مراجعة الطرف الثالث المعتمدة · الكود الحيّ (513/551/618/609…).

---

## ٠. المفردات والرموز (تعريف واحد نستعمله بكلّ الورقة)

| الرمز | المعنى | المصدر |
|---|---|---|
| **(A×S)** | (حساب × أصل) = الأنبوب المعزول | `شرح المالك` نقطة ٥ |
| **R** | ميزانيّة المخاطرة بالدولار لهذا (A×S) — يحطّها المالك (مثال $50) | `شرح المالك` أجوبة ٥+٦ |
| **leg i** (رِجل) | مركز تنفيذ واحد عند البروكر (تذكرة) | نموذج المحفظة (P0-5) |
| **side_i** | +1 شراء · −1 بيع | — |
| **vol_i** | حجم الرِجل (لوت) | — |
| **entry_i** | سعر تعبئة الرِجل الفعليّ (بروكر) | `NQ_Manual` §2 |
| **close_price_i** | سعر الإغلاق الآن = bid لو long · ask لو short (تقييم واقعيّ) | آليّة السوق |
| **vpp_i** | قيمة النقطة للوت واحد = `tick_value × point / tick_size` (بعملة الحساب) | `NQ_Manual` §1 · 618 |
| **point / tick_size / tick_value** | مواصفات الرمز من البروكر | 618 (`market.symbol_specs`) |
| **commission_i / swap_i** | عمولة مدفوعة + سواب متراكم للرِجل (بعملة الحساب) | الإكسبرت يبعتها |
| **K** | مخزون الربح المحقّق المُبقى بالدفتر (يوسّع المجال · يقلّ بالتخريج) | طرف ثالث بند ٢ |
| **X** | مجموع المخرَّج للحساب (خرج · لا يُعاد استعماله) | `شرح المالك` تخريج |
| **V_net** | صافي اللوت = `Σ side_i × vol_i` | اشتقاق |
| **W** | `Σ side_i × vol_i × entry_i` (لحساب سعر الستوب) | اشتقاق |

> **قاعدة عملة:** `tick_value` من MT5 **بعملة إيداع الحساب** → **R وكلّ الدفتر بعملة الحساب، بلا تحويل داخل الحساب.** التحويل يلزم فقط لتوحيد المحفظة للعرض عبر حسابات مختلفة العملة (P1).

---

## P0-1 — دفتر مخاطر الأصل (Asset Risk Ledger) · القلب الرياضيّ

**الحاجز:** لازم نعرف بأيّ لحظة: **كم الأصل خاسر اقتصاديًّا**، عشان نطبّق «الخسارة ≤ R» بالضبط. مش سعر مجرّد — **خسارة اقتصاديّة كاملة** (عمولة + سواب + تكلفة).

### الحساب (per A×S · بعملة الحساب)
```
floating_i  = (close_price_i − entry_i) × side_i / point × vol_i × vpp_i        (تقييم على سعر الإغلاق الواقعيّ)
cost_i      = commission_i + swap_i + est_close_commission_i                    (نحجز عمولة الإغلاق المقدّرة)
economic_i  = floating_i − cost_i
net         = K + Σ_i economic_i                                                (صافي اقتصاديّ للدفتر · المخرَّج X خرج ولا يُحسب)
loss_exposure = max(0, −net)
القيد:       loss_exposure ≤ R      ⟺      net ≥ −R
```

### لماذا هذا يطابق رؤية المالك بالضبط
- **«$50 → $70» (توسيع الستوب من الربح المصغّر):** لو المراكز العكسيّة أعطت `K = +$20` → القيد `net ≥ −$50` يسمح لـ`Σ economic_i` تنزل لـ**−$70** والصافي يظلّ −$50. → **المجال الإجماليّ = R + K = $70، والصافي المسقوف = R = $50.** (`شرح المالك` سطر ١٦٧) ✅
- **«الصافي دائمًا ≤ الرقم القطعيّ»:** القيد على `net` (الصافي)، لا على رِجل مفردة. (جواب المالك ٩) ✅
- **«العمولة/السواب داخل الحساب»:** `cost_i` مطروح من كلّ رِجل → «1% يعني 1%» صافٍ. (`شرح المالك` سطر ٥١) ✅

### قرار تصميم (حجز عمولة الإغلاق)
نطرح `est_close_commission_i` (بنفس معدّل الفتح) **قبل** ما تُدفع فعليًّا → `loss_exposure` **محافِظة** (تبالغ بالخسارة بمقدار ضئيل) → **آمنة لسقف** (ما نكسر R بسبب كلفة إغلاق غير محسوبة).

### ذرات المسار (لا بناء الآن)
- **جديدة `518 دفتر مخاطر الأصل`:** يستهلك 609 (مراكز/تذاكر) + 618 (مواصفات) + 611 (صفقات مغلقة) + بيانات الإكسبرت (عمولة/سواب) → ينشر `risk.asset_ledger.state` per (A×S).
- 500 (مدير المخاطر) يجمعه للرؤية · 516 (القاطع) يقرأ `loss_exposure` للعتبات.

---

## P0-2 — أهليّة ربح التحوّط بالميزانيّة (متى يوسّع المجال)

**الحاجز:** الربح العكسيّ إيمتى «يعدّ» ويوسّع المجال؟ الفرق بين ربح عائم (هشّ) وربح محقّق (ثابت).

| النوع | التأثير | لماذا |
|---|---|---|
| **ربح عكسيّ عائم** (رِجل مفتوحة) | يدخل `net` تلقائيًّا عبر `economic_i` → يوسّع المجال **لحظيًّا فقط** | ممكن ينعكس · لا نبني عليه مخزونًا دائمًا |
| **ربح عكسيّ محقّق** (رِجل أُغلقت برِبح، بعد الكلفة) | يُضاف لـ**K** → مخزون **دائم** يوسّع المجال | ثابت، لا ينعكس (طرف ثالث بند ٢) |
| **مخرَّج للحساب (X)** | يُطرح من K · **لا يُعاد استعماله أبدًا** | `شرح المالك` تخريج · طرف ثالث بند ٢ |

**القرار:** `K = Σ(ربح رِجل محقّق صافي الكلفة، مُبقى) − (المخرَّج من K)`. **التخريج يسحب من الربح المحقّق فقط، لا من العائم** (ما نخرّج ربحًا وهميًّا يتبخّر). المعادلة الحيّة (P0-1) تتكفّل بالعائم عبر `V_net/W` تلقائيًّا — فما في ازدواج.

---

## P0-3 — توحيد الميزانيّة ✅ (محسوم مسبقًا · للتذكير)

**per (A×S) مستقلّ تمامًا** — ناسداك $50 على حساب · $60 على حساب آخر. المحفظة **توحّد للعرض فقط** (تعرّض ناسداك الكلّيّ)، **الميزانيّة لا تتوحّد.** (`شرح المالك` سطر ٢٨٣ · جواب المالك.) لا عمل تصميم إضافيّ — قرار المالك.

---

## P0-4 — الخسارة الاقتصاديّة (عمولة/سواب) · مدمجة

مُدمجة أصلًا بـP0-1 عبر `cost_i`. المتطلّبات على الإكسبرت (الغبيّ):
- **يبعت لكلّ مركز:** `commission` (مدفوعة) · `swap` (متراكم) — إضافةً لمواصفات الرمز.
- **العمولة round-trip:** نحجز `est_close_commission` = عمولة الفتح (نفس المعدّل) حتى تُدفع فعليًّا.
- **السواب:** يتراكم يوميًّا (ثلاثيّ الأربعاء عادةً) → يُقرأ من المركز، يدخل `cost_i` مباشرة.
> فحص واقع: 618 حاليًّا يبعت contract_size/tick_value/tick_size/digits. **ناقص للإكسبرت:** `commission` · `swap` per position · `point` · `vol step/min/max` · `stops_level+freeze_level` · `filling` · **سعر التعبئة الفعليّ** (للانزلاق). = مهمّة «مواءمة الإكسبرت↔بايثون» (شغل كلود · `شرح المالك` §5).

---

## P0-5 — نموذج محفظة الأصل + الأرجل (Asset Portfolio) · يستبدل «صفقة واحدة»

**الحاجز:** الرؤية = **مركز دائم متعدّد الأرجل** (long/short/hedge/counter)، مش صفقة open→SL/TP→close. لازم نموذج بيانات جديد.

### الكيان: `AssetPortfolio` (per A×S)
```
identity        : (account_id, asset_canonical)      # التوحيد يعرف "ناسداك"
broker_symbol   : اسم الوسيط (عبر 708 سجلّ الرموز)
account_mode    : HEDGING (مطلوب) | NETTING (غير مدعوم — P0-6)
dial            : عيار 0–100 (per A×S)
risk_budget R   : $ يحطّه المالك
legs[]          : قائمة Leg
risk_ledger     : أرقام P0-1 (net · loss_exposure · V_net · W · P_stop)
profit_ledger   : K · X · مضاعفات التخريج المنجزة
state           : NORMAL|WARNING|HEDGING|FROZEN|MANUAL_RELEASE (P0-7)
```
### الكيان: `Leg` (رِجل تنفيذ = تذكرة بروكر)
```
leg_id (لنا) + broker_ticket        role: PRIMARY|COUNTER|HEDGE
side · vol · entry (بروكر) · entry_ref (مرجع) · open_time
commission · swap                    broker_SL (الجزء الصلب) · broker_TP?
translation_snapshot: {reference_entry, broker_entry, reference_sl, broker_sl}
status: PENDING|OPEN|CLOSING|CLOSED   version + stamp   (لمنع السباق · P0-9)
```
**تجميعات مشتقّة:** `V_long · V_short · V_net · W · avg_entry`. **المدير يشتغل على المحفظة، لا على صفقة.** (طرف ثالث بند ٣: «1 أصل = محفظة منطقيّة = N مراكز تنفيذ».)

### ذرات المسار (لا بناء)
هذا يعيد تأطير **551** (باني أمر لمرّة → **باني/محرّك محفظة**) و**513** (تحجيم %-إكويتي → **تحجيم بميزانيّة $-per-asset**، تحت) و**570** (منسّق إدارة → **محرّك مركز دائم**: إضافة/تخفيف/تحوّط/عكسيّ/جزئيّ). ذرة حالة جديدة `519 محفظة الأصل` تحمل الكيان.

---

## P0-6 — Hedging vs Netting

**الرؤية تتطلّب HEDGING** (أرجل long+short معًا في آن واحد). **NETTING = غير مدعوم — لا ترقيع** (طرف ثالث بند ٤).
- الإكسبرت يبعت `ACCOUNT_MARGIN_MODE` (RETAIL_HEDGING / RETAIL_NETTING) عند البدء.
- بايثون تفحص per account عند الإقلاع: **NETTING → الأصل UNSUPPORTED، لا تداول، علم صحّة واضح** («هذا الحساب netting — الرؤية تحتاج hedging»). لا محاولة محاكاة.
- في `account_card` (بطاقة البيئة): `account_mode` معلَن ومحقَّق.

---

## P0-7 — آلة حالات الحماية (0.95 → تجميد) · الأهمّ للأمان

**المتغيّر القائد:** `u = loss_exposure / R` (نسبة الاستهلاك · من P0-1).

```
        u<0.95                u≥0.95 (نظام حيّ)          تحوّط مؤكَّد            u→1 أو فجوة        أمر المالك
NORMAL ─────────────► WARNING ──────────────────► HEDGING ──────────► FROZEN ──────► MANUAL_RELEASE ──► NORMAL
   ▲       u≤0.90                                   (لا رجوع تلقائيّ من هون فصاعدًا)                    (بميزانيّة/أرجل جديدة)
   └───────┘ (تراجع فقط قبل التحوّط · هستيرة تمنع churn)
```

| الحالة | الشرط | السلوك | الستوب الصلب |
|---|---|---|---|
| **NORMAL** | u < 0.95 | إدارة كاملة (إضافة/تخفيف/عكسيّ/جزئيّ) | فعّال عند `net = −R` (P_stop) |
| **WARNING** | u ≥ 0.95 · نظام حيّ · حساب غير منحرف | يبدأ إجراء التحوّط | لسا فعّال (حتى يتأكّد التحوّط) |
| **HEDGING** | نُفّذ تحوّط (V_net≈0) | صافي مجمَّد · لا دخول اتّجاهيّ جديد | **يُشال بعد تأكيد التحوّط** (التحوّط = الحماية) |
| **FROZEN** | u بلغ ~1 (فجوة/انزلاق تحوّط) | مجمَّد ≤ R (±2%) · لا صفقات | تحوّط عند البروكر |
| **MANUAL_RELEASE** | المالك فقط | يفكّه المالك → NORMAL بميزانيّة/أرجل جديدة | حسب أمر المالك |

**تتابع التحوّط عند 0.95 (نظام حيّ · دقّة مطلقة · الترتيب حرج):**
1. احسب `V_net` الحاليّ → افتح حجم تحوّط **مساوٍ** بالاتّجاه المعاكس (يسطّح لـV_net≈0).
2. **انتظر تأكيد التعبئة** (ACK + actual · P0-9).
3. **بعد التأكيد فقط:** ألغِ/شِل ستوبات البروكر للأرجل الاتّجاهيّة (صار التحوّط هو الحماية).
> **لا نشيل ستوبًا قبل تأكيد التحوّط أبدًا** — لو النظام مات بالفجوة بين القرار والتعبئة، يبقى الستوب الصلب يمسك (P0-8/9).

**لا رجوع تلقائيّ:** بعد HEDGING، ما في عودة لـNORMAL إلا بأمر المالك (`شرح المالك` جواب ١٠: «يظلّ متحوّطًا حتى يتدخّل المالك بنفسه · المالك وحده يفكّه»). الهستيرة (u≤0.90) للتراجع **قبل** التحوّط فقط، تمنع churn حول العتبة (طرف ثالث بند ٥).

---

## P0-1b — الستوب الصلب: السعر الدقيق (الجزء «كلّ الشغل زبالة لو ما دُقّق»)

**المطلب (المالك · حرج):** ستوب 1% (= R) حقيقيّ **دائمًا عند البروكر** يمسك لو مات النظام، وبايثون **تُبقيه عند R بالضبط** كلّما تغيّر المركز. (`شرح المالك` سطر ١٣٤ · ١٤٢)

### اشتقاق سعر الستوب (إطار البروكر · نفس الرمز)
مجموع العائم دالّة خطّيّة بالسعر P:
```
Σ floating(P) = (vpp/point) × (P·V_net − W)          حيث  V_net = Σ side_i·vol_i ,  W = Σ side_i·vol_i·entry_i
```
نفرض `net(P_stop) = −R` :
```
K + (vpp/point)(P_stop·V_net − W) − C = −R           (C = Σ cost_i)

⇒  P_stop = [ W + (−R − K + C)·(point/vpp) ] / V_net           (V_net ≠ 0)
```
**بصيغة المسافة (أوضح):**
```
ΔP (نقاط) = (R + K − C) × point / (vpp × |V_net|)
P_stop = avg_entry − ΔP   (لو صافي شراء)   |   avg_entry + ΔP   (لو صافي بيع)
```
**التحقّق المنطقيّ:** المجال (R+K) أكبر أو التعرّض الصافي |V_net| أصغر → الستوب **أبعد**. = **بالضبط «الربح العكسيّ يوسّع الستوب»** (K↑ ⇒ ΔP↑). ✅

### 🔒 تثبيت التعريفات والإشارات (حاسم — خلط K/C يكسر الرؤية · مراجعة الطرف الثالث ٢٠٢٦-٠٨-٠٧)
`room = R + K − C` = **الدولار المسموح خسارته من حركة السعر.**

| الرمز | التعريف الدقيق | الاتّجاه على ΔP |
|---|---|---|
| **R** | ميزانيّة المخاطرة ($ · سقف الخسارة الصافية) | — (الأساس) |
| **K** | **رصيد ربح التحوّط المحقّق المُبقى (credit)** = `buffer_k` بـ518 | **K↑ ⇒ ΔP↑ (يوسّع)** |
| **C** | **تكلفة العمولة المقدّرة (cost)** = `commission_est` بـ518 | **C↑ ⇒ ΔP↓ (يضيّق)** |
| **W** | `Σ side·vol·entry` (أسعار الدخول · لنفس الرمز) | — |
| **V_net** | `Σ side·vol` — **لنفس الرمز حصريًّا** · BUY=+1 · SELL=−1 | \|V_net\|↑ ⇒ ΔP↓ |
| **vpu** | `tick_value/tick_size` = قيمة حركة سعر 1.0 للوت (عملة الحساب) — **كودي (525) يشتغل بهذا (بلا point)** | — |
| **vpp/point** | صيغة النقاط البديلة: `vpp = vpu × point` (مكافئة) | — |

- **⚠️ لا تُقلَب K و C:** K = ربح التحوّط (يوسّع) · C = العمولة (يضيّق). **مثال المالك: R=50 · K=20 · C≈0 → room = 50+20−0 = $70** ✅ (لو قُلبا: 50+0−20 = $30 ❌ عكس الرؤية — مرفوض). أثبته اختبار 525 (`ΔP` من 50→70 بدخول K=20).
- **⚠️ V_net لنفس الرمز فقط (نفس vpu):** الدفتر (518) يجمّع per **(حساب×رمز)** → كلّ أرجل الدفتر نفس الرمز → الجمع الموقّع صحيح. **أرجل رموز مختلفة (حتى نفس الأصل المنطقيّ عبر وسطاء) لا تُجمَع أبدًا** — كلّ رمز دفتره وستوبه (525). التوحيد عبر الوسطاء = طبقة المحوّل (526)، لا جمع V_net أعمى. (نقطة الطرف الثالث المحقّة، معتمَدة.)
- **سلسلة المحرّك:** `R + K − C → room → ΔP → P_stop → BrokerAdapter → SL مشروع عند البروكر` (تعالج فجوات ١+٣+١٣).

### الوضع بحساب hedging (صدق تقنيّ)
- **طور الانكشاف (V_net اتّجاهيّ):** ستوبات على الأرجل الاتّجاهيّة عند `P_stop` (كلّها نفس الجهة → نفس السعر → نظيف). الأرجل العكسيّة الصغيرة تربح بالحركة الضدّيّة = مخزون K — تُترك.
- **إعادة الحساب:** أيّ تغيّر بالأرجل (إضافة/تخفيف/عكسيّ/تخريج/تحديث K/C) → أعِد حساب `P_stop` → أرسِل **MODIFY** للبروكر. (`شرح المالك` سطر ١٣٤.)
- **صدق فجوة:** `P_stop` **هدف** — فجوة/سبريد منفجر قد يتجاوزه → خسارة > R. نضمن **الهدف الحسابيّ + SL دقيق + ستوب بروكر (آخر حماية)**، لا ضدّ الفجوة. → **هامش ±2% اللي أقرّه المالك** (`شرح المالك` سطر ٢٨٠). لا «تجاوز ستوب لمقاومة الصيد» (قاعدة ذهبيّة).

### ذرات المسار
يعيد تأطير **512** (الوقف الهيكليّ → **حاسبة ستوب الـ$ للمحفظة**) + **575** (مرسل الإدارة → يرسل MODIFY للبروكر) + **609** (يبلّغ الأرجل/التذاكر الحيّة لإعادة الحساب).

---

## P0-8 — سلوك فشل المرجع/البيانات/البروكر

**مبدأ:** فشل المرجع/البيانات **≠ إلغاء حماية مركز قائم.** نبقي الستوب الصلب، نمنع الدخول الجديد، نسجّل.

| العطل | الكشف | السلوك |
|---|---|---|
| **مرجع STALE/INVALID** | صحّة مرجع الأصل (primary+fallback) | **يحجب دخول جديد** لكلّ الحسابات على هالمرجع · يكمّل إدارة المفتوح بآخر مستويات جيّدة + حماية البروكر · طال العطل → ينحاز للأمان (تحوّط) لا تداول أعمى |
| **حساب DIVERGED** (`\|broker−reference\| > max_deviation`) | مراقب انحراف per account (SYNCED/DEGRADED/DIVERGED/STALE) | `execution.broker_divergence` → **يحجب دخول هذا الحساب فقط** · باقي الحسابات تكمّل · المحفظة تشوف الحالة |
| **بيانات STALE** (لا ticks من وسيط) | عمر البيانات `data_age` | تجميد دخول جديد لهالأصل · المفتوح محميّ بستوب البروكر (موضوع أصلًا) |
| **فصل البروكر** | فقد الاتصال | Reconciliation عند العودة (P0-9) |

**قاعدة ذهبيّة (طرف ثالث بند ٧+قاعدة المالك):** لا نبني «تجاوز ستوب». التدهور → **لا دخول جديد + سجلّ كامل.** الخروج الذي لا يقدر الوسيط ينفّذه لا نعتمده من المرجع وحده (لهيك الستوب الصلب عند البروكر).

---

## P0-9 — محرّك المطابقة (Reconciliation) · المرغوب ↔ البروكر

**الحاجز:** بعد restart/disconnect/partial-fill — لازم نطابق **حالتنا المقصودة** مع **الواقع عند البروكر** (مال حقيقيّ · لا تخمين).

**البروتوكول: `command → ACK → actual → reconcile`** (لا send-and-assume). كلّ رِجل لها `version + stamp` (يمنع سباق تعديلين بنافذة الـ5s · طرف ثالث بند ٦).

**عند العودة:** الإكسبرت يبعت **لقطة كاملة** لمراكز الحساب (tickets · vol · side · entry · SL · TP · commission · swap) → نقارن بـ`Desired` (المحفوظة على القرص):

| التصنيف | المعنى | الإصلاح |
|---|---|---|
| **MATCH** | مطابق | لا شيء |
| **MISSING_AT_BROKER** | عنا مفتوحة · البروكر لأ | أُغلقت/توقّفت → **قيّد المحقّق، حدّث الدفتر** (K/net) |
| **EXTRA_AT_BROKER** | البروكر عنده رِجل مجهولة | تبنّاها بحذر أو **جمّد + صعّد للمالك** |
| **MISMATCH** | vol/SL مختلف | أصلِح (أعِد إرسال SL · أو صغّر) |

**غموض غير قابل للحلّ → جمّد الأصل + صعّد للمالك** (ما نخمّن بمال حقيقيّ). **نحفظ `Desired` على القرص** (لقطة) → الإقلاع عنده أساس. (يطابق أمان اللوحة اليدويّة §4: استرداد بالـmagic.)

### ذرات المسار
جديدة `520 المطابقة` تستهلك لقطة الإكسبرت (عبر 609/611) + `Desired` المحفوظة → تنشر `execution.reconcile.state` + تصعّد للمالك عند الغموض.

---

## P0-10 — سياسة تغيير اللوحة أثناء مركز مفتوح

كلّ إعداد لوحة يحمل `value / effective_from / scope / version` + تصنيف (طرف ثالث بند ٩):

| التصنيف | مثال | التطبيق |
|---|---|---|
| **Hot** (حيّ فورًا · آمن) | عيار يؤثّر على تحليل جديد · تفضيلات عرض | فورًا |
| **Safe-boundary** (على الجديد فقط) | خفض `risk_budget R` · تغيير reward | **الأرجل المفتوحة تبقى على قيمتها وقت الفتح** · الجديد بالقيمة الجديدة |
| **Dangerous-needs-confirm** | تبديل حساب · تعطيل حماية · رفع مخاطرة كبير | **تأكيد صريح من المالك** · منسوخ بإصدار |

**القاعدة الحرجة (خفض R ومركز مفتوح):** لو `R_new < loss_exposure` الحاليّ → **لا إغلاق/تحوّط فوريّ قسريّ** (لا نكسر مركزًا فجأة · طرف ثالث بند ٩). بدلًا:
- علّم الأصل «فوق الميزانيّة · يخفّض» → **يمنع الإضافات**، يفضّل **تخفيف/جزئيّ على الحركات الملائمة** نحو R_new.
- **حرّك الستوب الصلب لـR_new فقط لو التضييق ما يسبّب ستوب-أوت فوريّ** (السعر الجديد لازم يبقى بالجهة الصحيحة من السعر الحاليّ) · وإلا **درّجه**. **ما نحرّك ستوبًا للجهة الغلط من السعر أبدًا.**
- **رفع R** يُطبَّق فورًا (يوسّع · آمن).

---

## ١١. أولويّة التعارض (تحكم كلّ الحواجز · طرف ثالث بند ٨)

```
حماية البروكر  >  قاطع الحساب (516/الحدّ اليوميّ)  >  مخاطر الأصل (R)  >  جودة التنفيذ/الانحراف
              >  الإدارة (إضافة/تحوّط/عكسيّ)  >  الإشارة  >  العيار  >  الأهداف/التخريج
```
**السلامة فوق الاستراتيجيّة دائمًا.** أيّ تعارض يُحسم بهذا الترتيب (مثال: عند DIVERGED، حجب الدخول يغلب أيّ إشارة عيار).

---

## ١٢. خريطة «الدقّة المطلقة» → أين بالضبط (مطلب المالك الحرج)

| النقطة | المعادلة/الفعل | القسم |
|---|---|---|
| سعر الستوب الصلب | `P_stop = [W + (−R−K+C)(point/vpp)] / V_net` | P0-1b |
| عتبة 0.95 | `u = loss_exposure/R ≥ 0.95` على الصافي الاقتصاديّ | P0-7 |
| حجم التحوّط | `= V_net` الحاليّ (يسطّح تمامًا) | P0-7 |
| إبقاء R بالضبط | إعادة حساب P_stop + MODIFY عند كلّ تغيّر أرجل | P0-1b |
| توقيت شيل الستوب | **بعد** تأكيد تعبئة التحوّط حصريًّا | P0-7 |
| تحجيم اللوت | `lot = R / (stopPoints × vpp)` + قصّ «مو أكثر أبدًا» | `NQ_Manual` §1 |
| تعديل الانزلاق | SL من سعر التعبئة الفعليّ → الخسارة = R | `NQ_Manual` §2 |
> كلّها **شرط، لا تحسين.** لا تقريب. صدق الفجوة: هدف بهامش ±2%، لا ضمان فيزيائيّ (P0-1b).

---

## ١٣. أثر التصميم على التحجيم الحاليّ (513) — تصحيح جوهريّ

**513 اليوم:** `risk_amount = equity × risk_pct/100` (نسبة من الإكويتي). **الرؤية الجديدة:** `risk_amount = R` (**دولار per (A×S)، لا علاقة بالإكويتي**). (`شرح المالك` أجوبة ٥+٦: «مش نسبة %، ومالها علاقة بحقوق الحساب».)
- **إعادة التأطير (لا بناء الآن):** 513 يأخذ `R` من ميزانيّة الأصل بدل `equity×%` · يبقى القصّ «مو أكثر أبدًا» (floor بخطوة اللوت) · العيار يحدّد `stopPoints` (سكالب=ضيّق/لوت كبير · طويل=أوسع/لوت أصغر · علاقة عكسيّة، جواب ٧).
- 551 يتحوّل من «أمر لمرّة» لـ«محرّك محفظة» (P0-5).

---

## ١٤. ما بقي حافة المالك (P2 · لوحة · لا يحسمه كلود)

هاي **ليست حواجز رياضيّة** — هي **تحكّم لوحة + حافة المالك** (`شرح المالك` §بحث الأهداف · سطر ١٩٩، ٢٨٩): **دالّة العيار الدقيقة** (0–100 → فلترة/مدى/لوت) · **مقدار التوسّع على الربح + إشاراته** · **توقيت الإضافات** · **شروط فتح العكسيّ** · **أرقام الأهداف** (pips/%/moveSL/trail لكلّ هدف) · **قواعد التخريج** (مضاعفات · النصّ · التخريج الكامل $). كلود يبني المحرّك ويكشف الإعدادات للّوحة (قاعدة ١١+١٤)؛ المالك يعيّرها حيًّا.

---

## ١٥. الخلاصة + المطلوب

- **حواجز P0 محسومة تصميمًا (١٠/١٠):** دفتر المخاطر (رياضة) · أهليّة ربح التحوّط · التوحيد (كان محسومًا) · الخسارة الاقتصاديّة · نموذج المحفظة/الأرجل · Hedging/Netting · آلة الحالات 0.95→تجميد · سعر الستوب الدقيق · فشل المرجع/البيانات/البروكر · Reconciliation · سياسة اللوحة. + أولويّة التعارض + خريطة الدقّة.
- **الحالة: مصمَّمة · مسجَّلة · مُراجَعة — لا مبنيّة.** لا كود، لا نواة، لا 350، لا فولت.
- **المطلوب من المالك:** **يراجع هالورقة** (خصوصًا: معادلة الستوب P0-1b · آلة الحالات P0-7 · تصحيح 513 للـ$ · حجز عمولة الإغلاق) → يصحّح أو يعتمد → **وبأمره فقط** نبدأ بناء أوّل ذرة (أقترح الترتيب: `518 دفتر المخاطر` → `519 المحفظة` → `512/P_stop` → `P0-7 الحالات` → `520 المطابقة`).
- **P1/P2** (صحّة المرجع · الرموز · العملة · شرعيّة الستوب · اللقطة · التدقيق · الساعة · الهامش · التجاوز اليدويّ ثمّ حافة المالك) بعد اعتماد P0.

> **قواعد ١+٢ لا تزال دَينًا عليّ** (`operating-constitution`) — أستأذن المالك أعرّفهما بعد مراجعته لهالورقة.
