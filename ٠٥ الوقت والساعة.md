# ورقة بناء — النواة ٠٥: الساعة (ختم الناقل بالوقت الرسمي)

**نوع الورقة:** تشريح تنفيذ كامل — يُجاب على **كل سؤال** فيه **قبل** أي سطر كود.
**التاريخ:** 2026-08-04 · **الوحدة:** النواة ① · الجزء ٠٥ · **الحالة:** ✅ **مُعتمَدة (مقفولة) 2026-08-04** — بعد رأي المستشار + تصفية كلود + موافقة المالك
**يمسّ النواة** (جزء من الختم V2.0).
**✅ منفّذة ومختومة — Core V1.8.0. وتوصيل الساعة اكتمل بورقة ١١:** المُقلِع يشترك على `time.utc.synced` ويغذّي `set_time_offset` (كان ناقصًا) + اختبار تكاملي. التفاصيل: ورقة ٠٨.

> **صدق مسبق:** هي **قطعة صغيرة**. الحلّ الأكبر لمشكلة الساعة كان **الوضوح المفاهيمي**
> (نموذج الوقت المتّفق عليه بالوثيقة). معظم تطبيق النموذج **شغل ذرات** (تطبيع التكّة ·
> الجلسات · حدود نيويورك) موجَّه بالنموذج — **مو هنا.** ٠٥ = الجزء الجذري الصغير بالنواة فقط.

---

## ١. ما هي الوحدة (بلغة بسيطة)
اليوم **الناقل يختم أي حدث بلا وقت بساعة الجهاز الخام** (بلا تصحيح NTP). فأي حدث ما بيحمل
وقته الخاص، بياخد وقت **غير مصحّح**، وممكن ما يطابق "الوقت الرسمي". هالوحدة تخلّي **ختم
الناقل يستعمل الوقت الرسمي المصحّح (UTC)** — فكل ختم تلقائي بالنظام يصير **بنفس المرجع**.

## ٢. الهدف و"ليش"
- **اتّساق:** ختم الناقل يصير من **مصدر موحّد للوقت** (خام + إزاحة ٦٠٨) بدل ساعة الجهاز الخام،
  فما يمرّ حدث تلقائي بمرجع مختلف.
- **تصحيح دقّة (من المستشار):** الورقة تعطي **مصدرًا موحّدًا للوقت**، لا "وقتًا رسميًا" بالمطلق —
  الدقّة تتبع دقّة إزاحة ٦٠٨، والنواة بس **تطبّق الإزاحة اللي وصلتها** بلا ما تدّعي صحّتها.
- جزء من إغلاق ملف الساعة نهائيًا (مع بقية النموذج على مستوى الذرات).

## ٣. الموجود نبني عليه (قِسته بالكود)
- **٦٠٨** يقيس الإزاحة وينشر `time.utc.synced {offset_s}` (جاهز).
- **الناقل يتجاهلها:** `base.setdefault("timestamp", time.time())` — خام، بلا إزاحة.
- الفجوة: الناقل ما بيعرف الإزاحة.

## ٤. التشريح الدقيق — شو راح أعمل بالضبط (بسيط)
**ملفان: `core/event_bus.py` + `core/bootloader.py`.**
1. **الناقل يحمل إزاحة (مو ساعة):** `self._time_offset_s = 0.0` (افتراضي صفر = سلوك اليوم
   بالضبط قبل أي مزامنة). الاسم **`_time_offset_s`** عمدًا (تصحيح المستشار) — النواة ما تملك
   ساعة، تملك **إزاحة**، والاسم لازم يعكس ذلك.
2. **الختم مصحَّح:** `base.setdefault("timestamp", time.time() + self._time_offset_s)`.
3. **تحديث الإزاحة:** الـ`Bootloader` يوصل معالِجًا صغيرًا على `time.utc.synced` يحدّث
   `_time_offset_s` من الحدث. (الناقل ما بيعرف إنها من ٦٠٨ — بس اسم حدث عام + رقم إزاحة.)
4. **دالة `now()` صغيرة بالناقل** (`time.time() + offset`) للاستعمال الداخلي.
5. **قاعدة معمارية صريحة (كانت ضمنيّة — والمستشار طلب توثيقها) — الناقل يضيف ولا يعدّل:**
   `setdefault` معناها الناقل يحط `timestamp` **فقط لو الحدث بلا وقت**. أي حدث بيحمل وقته
   الخاص (خصوصًا **وقت مصدر خارجي: تكّة MT5 / Binance**) **يبقى كما هو بلا لمس ولا إعادة
   تفسير.** ختم الناقل = **"وقت النشر الافتراضي" (الآن المصحّح)**، لا وقت السوق — ومنع الخلط
   بينهما خطّ صريح (يطابق نموذج الوقت بالوثيقة: **وقت السوق من التكّة، لا يُصحّح بساعتنا**).

**⚠️ اعتماد لقطته كبنّاء (مو بمراجعة المستشار) — نفس جذر توقّف الصفقات:** معالج الإزاحة
(خطوة ٣) يشترك على `time.utc.synced`. لو ٦٠٨ نشر إزاحته **قبل** ما يشترك المعالج، بيفوتها
ويبقى الناقل على إزاحة صفر — **بالضبط سباق ٦١٩→٦٥١→٤١٣.** الحلّ موجود سلفًا: **ورقة ٠٣
(الحالة عند الاشتراك) تعيدله آخر إزاحة فورًا** — **بشرط تصنيف `time.utc.synced` كحدث "حالة"
(State)**، وهو كذلك (حقيقة ثابتة، لا أمر). فيلزم إدراجه ضمن تصنيف "الحالة" عند تنفيذ ٠٣.
(مثال حيّ إن الجذر واحد، والطبقة تحلّه مرّة لكل النظام.)

**مصفّى (تطبيق قاعدتك ضد المبالغة):** **ما بلمس عقد الذرة `AtomContext`.** إضافة ساعة
للعقد **مؤجّلة** — قيمتها قليلة (الذرة تاخد الوقت الرسمي من نبضات ٣/٨٠٦ إن احتاجته)،
وتدخّلها أكبر (تمسّ العقد المختوم). فبنكتفي بختم الناقل.

## ٥. فحص الخطوط الحمر
- **جاهلة:** الناقل يستعمل **رقم إزاحة عام** ويسمع **حدثًا عامًا** — بلا معنى أعمال، بلا
  معرفة أي ذرة. ✓
- **العزل:** ما يتغيّر. ✓ **العقد المختوم:** ما نلمسه (أجّلنا ساعة السياق). ✓
- **صدق المصدر (القاعدة ٥):** الناقل ما يدوس وقت مصدر خارجي — يضيف فقط عند الغياب. وقت
  السوق يبقى صادقًا. ✓
- **رفع:** ختم متّسق بدل خام. الافتراضي صفر = بلا كسر سلوك قائم. ✓

## ٦. الإثبات الحيّ (تشوفه بعينك)
- **قبل:** ختم الناقل = خام؛ بينما `official_time` (من ٣/٨٠٦) = خام + إزاحة → **مختلفان**
  حين تكون الإزاحة ≠ صفر.
- **بعد:** بعد وصول `time.utc.synced`، ختم الناقل = خام + إزاحة → **يطابق `official_time`.**
- **اختبار الافتراضي:** بلا مزامنة، الإزاحة صفر، والختم = سلوك اليوم بالضبط (لا انحدار).
- **اختبار تغيّر الإزاحة حيًّا (من المستشار):** إزاحة `+0.320` ثم تصل مزامنة `-0.018` →
  **الأحداث الجديدة فقط** تاخد الإزاحة الجديدة، **والقديمة ما يُعاد ختمها.** يُثبَت بالاختبار.
- **اختبار صدق المصدر (من المستشار):** حدث يحمل `timestamp` مسبقًا → الناقل **ما يغيّره**
  مهما كانت الإزاحة (وقت المصدر الخارجي محفوظ).

## ٧. المخاطر والتراجع
- **يمسّ `publish` (مسار ساخن) + توصيل بالـBootloader — خطر منخفض-متوسط:** لكن الإضافة
  صغيرة والافتراضي صفر (بلا كسر). يُثبَت بالاختبارات.
- **التراجع:** git (م٠). الختم يُعاد V2.0 بعد اكتمال أجزاء النواة.

## ٨. الأسئلة — مصفّاة (التقني على كلود · وسؤال المالك بسيط)

**قرارات تقنية حسمها كلود (لا تلزم المالك):**
- الناقل يحمل إزاحة (`_time_offset_s`)، يحدّثها من `time.utc.synced`، ويختم `time.time()+offset`.
- **ساعة السياق (`AtomContext`) مؤجّلة** (قيمة قليلة + تمسّ العقد) — الذرة تاخد الوقت من ٣/٨٠٦.
- **تطبيع التكّة · الجلسات · حدود نيويورك اليومية/الأسبوعية = شغل ذرات لاحقًا**، موجَّه بنموذج
  الوقت بالوثيقة — **مو بالنواة.**
- **ملاحظة المستشار عن `event_id`: محلولة سلفًا بورقة ٠٤ (مقفولة)** — `event_id` وحدة من
  المعرّفات الثلاثة، تُختَم عند النشر. فهويّة الحدث (`trace_id` + `event_id` + `parent_event_id`
  + `timestamp`) تكتمل بمجرّد تنفيذ ٠٤+٠٥ — لا حاجة لإضافة جديدة هون.

**للمالك (بلغة بسيطة) — سؤالان صغيران yes/no:**
> ١) الناقل يصير يختم بالوقت **المصحّح (خام + إزاحة ٦٠٨)** بدل ساعة الجهاز الخام، فكل الأوقات
> التلقائية بمرجع واحد. **صح؟**
> ٢) والناقل **ما يلمس أي وقت جاي مع الحدث** (زي وقت تكّة من MT5) — بس يضيف وقت لو الحدث
> بلا وقت أصلًا. **صح؟**

## ٩. قرار المالك
☑ **نفّذ كما هو** (المالك: «موفّق» · 2026-08-04)  ·  ☐ عدّل (اكتب)  ·  ☐ أوقف / غيّر
> التنفيذ الفعلي (كود) يجي مع «فتحة النواة الواحدة» بعد اكتمال ٠٦، ثم إعادة ختم V2.0.

---
**التالي بعد اعتمادها:** ٠٦ المهلات + تنظيف sys.modules (مشكلة التحميل الحار).
