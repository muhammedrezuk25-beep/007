# قسم ٥٥٠ — التنفيذ + الإدارة الذكيّة

> **الحالة:** ✅ **مبنيّ + مختبَر (Gate-1) 2026-08-06 — ١٠ ذرات جديدة + تعديل 513/611 + الإكسبرت الواحد.**
> مدقّق ٠ · ١٢/١٢ اختبار أخضر (٥٩ حالة). **Gate-2 الحيّ ينتظر:** تصريف الإكسبرت + إعادة تشغيل النواة +
> **موافقة المالك بتفعيل `enabled` على ديمو.** على **ديمو**. **مواصفة المالك (2026-08-06).**

## ✅ المعماريّة كما بُنيت (استفادت من ذرات قائمة — أنظف من التصميم الأوّل)
**مسار الدخول:** `467 قرار معتمَد` → `516 يتحقّق (risk.validation.completed)` → **`551 باني الأمر`**
(يدمج جهة+لوت هيكليّ 513+ستوب+هدف R:R) → **`552 مدقّق+بوّابة`** → لو `enabled=true` **`trading.final_decision`**
→ **`601` (قائم) يكتب `commands`** → **الإكسبرت الواحد** ينفّذ → `trade_events`.
**مسار التأكيد:** الإكسبرت → `trade_events` → **`611` (قائم، عُدّل يمرّر profit)** → **`563 تأكيد`** →
`market.outcome.realized` → `517` → `516 القاطع`.
**مسار الإدارة الذكيّة:** **`609` (قائم) مراكز** + **`512 وقف هيكليّ`** → الكواشف **`572 تعادل`/`573 تتبّع`/`574 جزئيّ`**
→ نيّات → **`570 منسّق`** (الأضيق يفوز · لا يُرخى) → **`575 مرسل إدارة`** (enabled) → `commands` → الإكسبرت.
**المراقبة:** **`550 مدير التنفيذ`** يجمّع العمود → `execution.unified.state`.

### 🔑 مفتاحا الأمان (كلاهما false افتراضيًّا — لا تنفيذ حتى يفعّلهما المالك على ديمو)
- **`552.enabled`** = بوّابة فتح الصفقات (مطفأ → `execution.order.preview` بدل `final_decision`).
- **`575.enabled`** = بوّابة أوامر الإدارة.

### ما تغيّر عن التصميم الأوّل (واقع فوق ورق)
- **لم أبنِ 553/571/554:** البوّابة = `601` القائم · المراكز = `609` القائم · التأكيد = `611`+`563`.
- **512 وقف هيكليّ جديد** (تحت قاع/فوق قمّة من سوينق 201) يغذّي 513 (buy_lot/sell_lot) و573.
- **الإكسبرت الواحد** (`mt5\QUANT_NQ.mq5`) = Feed+عمق DOM+إحماء+تقويم+تنفيذ · أضفت عمود `profit` (P&L دقيق).
- **⏸️ زيادة/scale-in مؤجّلة** (تزيد المخاطرة — تحتاج تصميم pyramiding + مراجعتك). الكواشف الحاليّة **وقائيّة فقط**.

### ⚠️ ملاحظات لمراجعتك (بالأوراق + الشرح)
- **halt vs الإدارة:** `601` يلغي كل `PENDING` عند halt (شامل أوامر الإدارة). تحسين لاحق: قصر الإلغاء على OPEN.
- **563 يحسب النتيجة لـCLOSED وPARTIAL** (مجموع P&L دقيق · عدّاد الخسائر المتتالية تقريبيّ).

---


## مواصفة المالك (حرفيًّا — لا تُخالَف)
**مخاطرة ١٪ ديناميكيّة (مو لوت ثابت) · ستوب + هدف · إدارة صفقة كاملة · إدارة مركز (يخفّف/يوسّع/يزيد مع
الاتجاه) · يحمي الربح لو رابح (breakeven + trailing) · على ديمو · «إدارة ذكيّة مو صنم · تتعامل مع ١٪ باحترافيّة».**

## 🔴🔴 قرار الإكسبرت الواحد (الأهمّ — من إحباط المالك المحقّ 2026-08-06)
المالك **منزعج بشدّة** من تعديل الإكسبرت مرّة ورا مرّة (إحماء ثم تنفيذ...). **الحلّ النهائيّ المتّفق عليه:**
- **إكسبرت واحد شامل** (`QUANT_NQ.mq5` مقترح) = كل الـFeed (تكّات/أسعار/حساب/مراكز/مواصفات/تقويم/**إحماء
  candles_history**) **+ التنفيذ الكامل** (من `NQ_Arm.mq5` القديم: يقرأ جدول أوامر → `CTrade` يفتح/يغلق/يعدّل/جزئيّ
  → يكتب `trade_events`). **يُصرَّف مرّة واحدة وخلص.**
- **🔑 المبدأ:** **الإكسبرت غبيّ — بس منفّذ أوامر (OPEN/CLOSE/MODIFY/PARTIAL).** **كل العقل بالبايثون**
  (متى/الحجم ١٪/الستوب الهيكليّ/الهدف/trailing/الزيادة/التخفيف). → أعدّل الذكاء بايثون **بلا لمس الإكسبرت أبدًا.**
- **مرجع كنز:** `Desktop\ذرات\NQ_Arm.mq5` = ذراع تنفيذ قديم شامل (BridgeOpen WAL · CTrade · قراءة أوامر
  LastCmdId · مزامنة مراكز · InpMaxCmdAgeSec=90 يرفض الأمر البائت · InpRequireStop=true · شاشة). و`mt5\QUANT_NQ_Feed.mq5`
  (v2.12) = الـFeed الحاليّ (فيه الإحماء اللي أضفته). **الدمج = الاتنين بإكسبرت واحد.**

### ❓ القرار المعلّق (يُسأل أوّل المحادثة الجايّة قبل كتابة ~٦٠٠ سطر MQL5):
**إكسبرت واحد جديد يدمج الاتنين (أنظف · موصى)؟ أو نبني على `NQ_Arm` القديم مباشرة ونضيف ناقص الـFeed؟**
كلمة وحدة من المالك ثم يُكتب الإكسبرت كامل **مرّة واحدة**.

## المعماريّة — طرفان
### أ. طرف بايثون (الذرات — أبنيها · كل الذكاء هون)
| # | الذرة | الدور |
|---|---|---|
| **الستوب الهيكليّ** | (جديدة — تحت آخر قاع للشراء/فوق آخر قمّة للبيع من 200) | يحدّد الستوب الحقيقيّ → يغذّي 513 |
| **513** ✅ مبنيّ | تحجيم المركز | لوت = (Equity×١٪)/(مسافة الستوب × قيمة الوحدة) — يستعمل الستوب الهيكليّ الحقيقيّ |
| **551** | باني الأمر | يدمج القرار المعتمَد (467) + تحقّق 516 + لوت 513 + ستوب + هدف (R:R) → أمر كامل |
| **552** | مدقّق الأمر | يتحقّق من اكتمال الحقول |
| **553** | مرسل الأمر | 🛑 يكتب الأمر بجدول الأوامر بالجسر · **بوّابة halt: تُقفل على `emergency.halt` وتُفتح فقط بإعادة تفعيل يدويّة** |
| **554/563** | حالة/تأكيد | يقرأ `trade_events` (نتائج الإكسبرت) → يغذّي 517 (P&L) → 516 |
| **570** | مدير الإدارة | منسّق إدارة الصفقة |
| **571** | متتبّع المراكز | من مراكز الجسر (611) |
| **572** | breakeven | +1R → ينقل الستوب للدخول (يحمي رأس المال) |
| **573** | trailing | مع كل قمّة/قاع هيكليّ جديد → يرفع الستوب (يحمي الربح) |
| **574** | إغلاق جزئيّ | 50%@1R (يقفل ربح، يخلّي الباقي يركض) |
| **575** | منفّذ الإدارة | يترجم قرارات الإدارة لأوامر MODIFY/PARTIAL/CLOSE للجسر |
| **(زيادة scale-in)** | (بالإدارة) | على ارتداد باتجاه مؤكّد رابح — ضمن المخاطرة |
| **550** | مدير التنفيذ | نمط B يجمّع |
| 560..568 | مراقبات | انزلاق/رفض/إعادة محاولة/تزامن/كمون/أولويّة/سلامة (مؤجّلة/حسب الحاجة) |

### ب. طرف الإكسبرت (الواحد الشامل — يُصرَّف مرّة)
يقرأ جدول الأوامر (Python يكتب: OPEN/CLOSE/MODIFY/PARTIAL) → `CTrade` ينفّذ → يكتب `trade_events`.

## عقد `trade_events` (الإكسبرت يكتب · 563 يقرأ فقط · لا تكتبه ذرة)
`id · event_type(OPENED|CLOSED|PARTIAL|REJECTED) · ticket · symbol · side(BUY|SELL) · volume ·
entry_price(منفَّذ فعلًا) · exit_price(CLOSED/PARTIAL فقط · NULL بالفتح) · open_time · close_time ·
reason(SL|TP|MANUAL|MARGIN|NO_MONEY) · written_at`. **أوقات UTC ثواني · سطر لكل حدث لا UPDATE · ticket ثابت
للصفقة · كتابة فورًا عند الحدث.**

## الإعدادات المهنيّة الافتراضيّة (قابلة لضبط المالك — أقرّها أو عدّلها)
`risk=١٪ · R:R=2 · breakeven=1R · إغلاق جزئيّ 50%@1R · trailing=هيكليّ · scale-in=مرّة على ارتداد`.

## 🛡️ الأمان
ديمو · القاطع ٥١٦ يقفل بوّابة ٥٥٣ عند ١٪ خسارة يوميّة/٣ خسائر (إعادة تفعيل يدويّة) · حارس شمعة `warmup`
(الشمعة التاريخيّة **لا** تفتح صفقة — علَم موضوع بـ602) · **كلود لا يفتح صفقة بيده — يبني الأداة والمالك يشغّلها.**
