# أمر تصليح — timeframe حقل هوية بالمستوى الأعلى (الخيار أ — الجذري)

**التاريخ:** ٢٠٢٦-٠٨-٠٧
**القرار:** اعتمد المالك الخيار **أ (الجذري)** — «من الجذر لا ترقيع».
**ورقة التشخيص المرجعية:** `سياق\تخبيص مفتاح timeframe — قسم الاحتمالات والثقة (٢٠٢٦-٠٨-٠٧).md`
**المنفّذ:** محادثة الذرات. هذا الأمر لا يمسّ `core/` إطلاقاً (النواة مختومة V1.8.0 — ناقل الأحداث بلا مخطط، لا يحتاج تعديلاً).

---

## ١) العطل (باختصار مصدّر)

كل مستهلكي قسم الاحتمالات (351..357) يبنون المفتاح هكذا:

```python
key = (symbol, str(payload.get("timeframe", "")))
```

لكن المدخلات نوعان يضعان الـtimeframe في مكانين مختلفين:

| المصدر | أين يضع timeframe | الدليل |
|---|---|---|
| الشمعة (103 / 602) | المستوى الأعلى `"60s"` ✓ | مثبت بورقة التشخيص |
| التحليل/البنية/السيولة/الإحصاء | داخل `metadata` فقط ✗ | مثلاً `atoms\151_الاتجاه\atom.py:167-169` و`atoms\201_القمم_والقيعان\atom.py:98-100` |

فالنموذج يخزّن حالة البنية تحت `(رمز, "")` ويبحث عنها عند وصول الشمعة تحت `(رمز, "60s")` → لا تطابق أبداً → **بلوك الاحتمالات (350) كله neutral دائماً → القرار (450) يخرج `wait` دائماً.**

## ٢) القاعدة المعتمدة (نص القانون)

> **`timeframe` حقل هوية بالمستوى الأعلى على كل حدث حالة يُنشر — تماماً مثل `symbol`.**
> كل ذرة تنشر حدثاً له مفهوم إطار زمني تضعه في أعلى الحمولة: `"timeframe": timeframe`.
> نسخة الـ`metadata` **تبقى كما هي** (لا حذف — «لا تدمج شي»، ولا نكسر أي قارئ حالي للـmetadata).
> منطق القراءة عند المستهلكين (351..357 وغيرهم) **لا يُمسّ** — هم يقرأون المستوى الأعلى أصلاً، وسيتطابقون تلقائياً فور تصليح الناشرين.

## ٣) الأنماط الثلاثة الموجودة فعلاً (مع قبل/بعد)

### النمط A — عنده متغيّر `timeframe` محلي صحيح، لكنه ينشره في metadata فقط
(الأغلبية الساحقة — مثال `atoms\151_الاتجاه\atom.py:167`)

```python
# قبل
base = {"symbol": symbol, "id": "trend", "cycle_id": cycle_id}
# بعد
base = {"symbol": symbol, "id": "trend", "cycle_id": cycle_id,
        "timeframe": timeframe}
```

لا شيء آخر يتغيّر في الذرة (الـ`meta` يبقى كما هو).

### النمط B — يقرأ timeframe من `metadata` الوارد ويعيد نشره في metadata
(مكتشف في `atoms\206_تحول_الهيكل_MSS\atom.py:84,100`)

```python
# قبل: meta_in.get("timeframe", "") فقط
# بعد: اقرأ الأعلى أولاً ثم metadata كاحتياط، وانشره بالأعلى
timeframe = str(payload.get("timeframe", "") or meta_in.get("timeframe", ""))
```

ثم أضِف `"timeframe": timeframe` إلى الحمولة المنشورة (النمط A).

### النمط C — يُسقط timeframe كلياً
(مكتشف في `atoms\166_دمج_التحليل\atom.py` — لا ذكر للكلمة في الملف أصلاً)

166 يدمج ذرات التحليل الـ15 لكل رمز، والحدث المدموج يخرج **بلا هوية إطار زمني**. المطلوب:
- يقرأ `timeframe` من المستوى الأعلى لكل حدث وارد (سيتوفر بعد تصليح النمط A).
- يجعل مفتاح تجميعه `(symbol, timeframe)` بدل `symbol` وحده.
- ينشر `"timeframe": timeframe` بالمستوى الأعلى في الحدث المدموج (الذي يغذّي 712 وما بعده).

## ٤) نطاق الكنس (القائمة يحسمها الأمر، لا الذاكرة)

الكنس المصدّر (٢٠٢٦-٠٨-٠٧) وجد:
- **٨٨ ملف** فيها النمط `"timeframe": timeframe` (نشر في metadata) — تغطي العائلات: **150 التحليل** (150، 151..165)، **200 البنية** (200، 201..205، 207، 208، 210)، **250 السيولة** (250، 251..258، 260)، **300 الإحصاء** (300، 301..316، 318)، **350 الاحتمالات** (350، 351..359)، **400 الاستراتيجيات** (400، 401، 402، 404..413)، **450 القرار** (450، 451، 453..457، 460..462، 466)، إضافة إلى **157، 158، 513**.
- **206** = النمط B، و**166** = النمط C (ليسا في قائمة الـ٨٨).
- **209 و259** (المدقّقان) لم يظهرا في أي كنس — الأرجح لا مفهوم timeframe عندهما؛ تحقّق ولا تعدّل إلا إذا وجدت نشراً بلا هوية.

**القائمة النهائية يحسمها هذا الأمر على جهاز المالك (PowerShell، أمامه):**

```powershell
Get-ChildItem C:\Users\NQ\QUANT_NQ\atoms -Recurse -Filter atom.py | Select-String -List -Pattern 'timeframe' | ForEach-Object { $_.Path }
```

كل ملف يخرج من هذا الأمر يُفحص ويُصنّف A أو B أو C ويُصلّح بالقاعدة. لا ملف يُستثنى بلا سبب مكتوب.

## ٥) ما لا يُلمس

- **`core/`** — مختومة، لا حاجة لأي تعديل.
- **منطق القراءة عند المستهلكين** — `payload.get("timeframe", "")` يبقى كما هو؛ لا تضِف قراءات احتياطية من metadata عند المستهلكين (هذا يعيد الازدواجية التي قتلناها).
- **نسخ metadata الحالية** — تبقى، لا حذف.
- **المخازن (712/713/718...)** — الصفوف القديمة المخزّنة بمفاتيح `""` تاريخ، لا يُعاد كتابتها ولا تُحذف (الحذف بيد المالك وحده).
- **الـEA** (`mt5\QUANT_NQ.mq5`) — لا علاقة له، لا يُمسّ.

## ٦) تحذير النشر الجزئي

التصليح **دفعة واحدة ثم إعادة تشغيل واحدة**. لو صُلّح بعض الناشرين دون بعض، تعود المفاتيح مختلطة بشكل أسوأ (بعضها `"60s"` وبعضها `""`). لا بوّابة فحص قبل اكتمال الدفعة كاملة.

## ٧) قواعد التسليم (الدستور)

- ترقية إصدار (**version bump**) لكل ذرة معدّلة — بلا استثناء.
- الكود والتعليقات إنجليزية فقط.
- المدقق (validator) = ٠ مخالفات بعد الدفعة.
- تحديث اختبار كل ذرة معدّلة (هرمتي — بلا `NQ_BRIDGE_DB` وأشباهه).
- PowerShell فقط، أمامية فقط، لا خلفية، **لا موك** في بوّابة الفحص.

## ٨) بوّابة الفحص ٢ (الإثبات الحي — المالك يشاهد)

بعد اكتمال الدفعة وإعادة تشغيل النواة (طرفية مرئية، `:8010`):

1. **إثبات خام:** حدث `analysis.trend.state` حقيقي (مثلاً XAUUSD) ظاهر على شاشة المالك وفيه `"timeframe": "60s"` **بالمستوى الأعلى** — إشارة خام يراها بعينه، لا استعلام صحة.
2. **إثبات وظيفي:** `probability.*.state` يخرج من الحياد الدائم بعد الإحماء (602 يعيد 1600 شمعة حقيقية → مادة فورية) — المفاتيح تتطابق فيتحرّك النموذج.
3. **إثبات العمود:** تجميع 451 يُظهر مساهمة الاحتمالات فعلاً (لم تعد صفراً دائماً)، وقسم الاحتمالات في غرفة القيادة يعرض قيماً حيّة.
4. الحكم النهائي «خلصت» للمالك وحده — بعد أن يرى، لا قبل.

---

# ملحق التنفيذ (as-built) — نُفّذ داخلياً ٢٠٢٦-٠٨-٠٧

**المنفّذ:** كلود مباشرة بأمر المالك («تصرف انت من داخل فتحت ختم وختمت») — لم يُرسل لمحادثة الذرات.

## ما نُفّذ
- **أرشيف قبل اللمس:** `backups\atoms_before_timeframe_fix_2026-08-07\` (158 ملف ذرة — الحذف بيد المالك).
- **دفعة آلية مرسّاة ومحروسة بـAST:** 83 ملفاً × 102 تعديلاً (إدراج `"timeframe": timeframe` بالمستوى الأعلى + ترقية إصدار MINOR لكل ملف معدّل). الحارس النطاقي رفض أي إدراج في دالة لا تملك المتغيّر.
- **يدوي:** 206 (متغيّر محلي + نشر أعلى، v1.1.0) · 166 (تمرير الهوية من `analysis.cycle.collected`، v1.1.0) · 512 (قراءة أعلى-أولاً + نشر أعلى، v1.1.0) · 454 (انظر الانحرافات).
- **المدراء السبعة (150/200/250/300/350/400/450):** وُجدوا سليمين أصلاً (ينشرون الهوية بالأعلى) — لم يُمسّوا.

## الانحرافات عن نص الأمر (بالحجّة)
1. **463/464 أُعفيا** من الحقل الأعلى: فلتران على مستوى الرمز (مراكز/طزاجة جسر) بلا مفهوم إطار زمني إطلاقاً — حشر إطار فارغ فيهما يكرّر الكذبة القديمة.
2. **454 عُدّل منطق استهلاكه** (خرقاً مقصوداً لبند «منطق القراءة لا يُمسّ»): بعد الدفعة كانت مفاتيح 463/464 سترسو على `(رمز,"")` بينما القرار على `(رمز,"60s")` → فلتر الطزاجة الحيّ المثبت كان سيموت بصمت. الحل: `_SYMBOL_FILTER_IDS` — تخزين الفلترين الرمزيين بمفتاح الرمز وحده ودمجهما عند الحكم.
3. **إصلاح كامن خارج النطاق (بدليل اختبار أحمر):** `confidence_filter` (460) كان ساقطاً من `_FILTER_IDS` في 454 — فلتر الثقة كان مُتجاهلاً بالكامل، واختبار 454 الموروث الفاشل يوثّق النيّة الأصلية. أُعيد للقائمة.
4. **اختباران موروثان مكسوران قبل الدفعة** (أُثبت ذلك بتشغيلهما على نسخة الأرشيف): اختبار 150 متيبّس على عهد محلّلَين اثنين → حُدّث ليطابق الـ15؛ اختبارات 901 السبعة تطلب fixture باسم `tmp` غير موجود → أُضيف جسر `tmp(tmp_path)`.

## البوّابة الأولى (فحصي أنا)
- المدقّق: **158 ذرة · 0 مخالفات · 0 تحذيرات**.
- الاختبارات: **158/158 ذرة سليمة · 759 اختباراً ناجحاً**.
- مسح AST نهائي: كل مواقع النشر ذات مفهوم الإطار الزمني تحمل الهوية بالأعلى (127 موقعاً OK؛ الباقي بلا مفهوم إطار أصلاً).
- تشغيل حيّ محدود أمامي 60 ثانية (`run_core.py --demo-seconds 60`): إقلاع 158/158 بلا فشل، والإحماء أعاد الشموع الحقيقية عبر العمود كاملاً.
- **برهان المخازن (بيانات حقيقية):** أحدث 40/40 صفاً في كلٍّ من `analysis.db` و`structure.db` و`liquidity.db` و`decisions.db` يحمل `"timeframe": "60s"` بالمستوى الأعلى — من `fusion` (166) حتى `decision_approval` (466) بنفس `cycle_id` الشمعة.

## بوّابة الفحص ٢ — ما تزال للمالك
إعادة تشغيل النواة الكاملة أمامه، ورؤية قسم الاحتمالات في غرفة القيادة يخرج من الحياد الدائم على شاشته. الحكم النهائي «خلصت» له وحده.
