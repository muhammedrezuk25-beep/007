# ورقة بناء — الحوكمة ١٥: غرفة القيادة — التنفيذ

**نوع الورقة:** اختيارات تنفيذ **قابلة للتغيير** — تابعة لمعمارية `١٤`، لا تمسّها.
**التاريخ:** 2026-08-06 · **الطبقة:** الحوكمة (طبقة ٢) · **الحالة:** مسودة للمراجعة.
**قرينتها:** `١٤ غرفة القيادة — المعمارية الخالصة` (الثابت). **هنا: التقنية والأدوات والأرقام والتشغيل.**

> **مبدأ الفصل (من المراجعة الخارجية):** تغيير أي بند بهذه الورقة **لا يمسّ معمارية `١٤`.** كل اختيار
> هنا **يخدم عقدًا معماريًّا من `١٤`** ويُذكر أيّ عقد يحقّق — فلو استُبدل، يكفي أن البديل يحقّق نفس العقد.

---

## ١. الإطار (Framework)
- **الاختيار المبدئي:** **React + Vite** · **TypeScript موصى به، لا حاسم.**
- **يحقّق:** `١٤ §٤/٥` (هيكل + Widgets) — **يُحصر في الهيكل والتنقّل، لا البيانات المتدفّقة** (`١٤ §٦`).
- **مبرّر:** نظام أنضج ودعم أول-درجة لمكتبات التداول/الجداول. **TypeScript موصى به** (عقود أنواع تقلّل
  الأخطاء بمشروع بهذا الحجم) لكنه **ليس قرارًا حاسمًا** — JavaScript نقي مقبول لو فضّله المنفِّذ.
- **قابل للاستبدال:** Svelte/Solid (تفاعل أدقّ، حِمل أخفّ) — **مقبول ما دام يحقّق عقد الرسم `١٤ §٦`**
  (البيانات عالية التردّد خارج شجرة التفاعل). **الحاسم العقد، لا اسم الأداة.**

## ٢. مكتبات الرسم والجداول والحالة
| يحقّق عقد `١٤` | الاختيار المبدئي | بديل مقبول |
|---|---|---|
| محرّك رسوم Canvas (`§٦`) | **TradingView Lightweight Charts** | Canvas مخصّص |
| جدول Virtualized (`§٥/٦`) | **AG Grid** | شبكة Canvas مخصّصة |
| اشتراك حالة دقيق (`§٦`) | **Zustand** | أي متجر دقيق التحديث |
| أيقونات SVG فقط (`§٦`) | **Lucide** | — |
| النقل (`§٣/٦`) | **WebSocket أصلي** + عميل REST | — |

**التوصية أعلاه = مبدئية · القرار النهائي بعد آراء المراجعين الخمسة.** (الحاسم أن البديل يحقّق نفس عقد `١٤`.)

## ٣. أرقام الأداء (تفعيل عقد الرسم `١٤ §٦`)
- **الهدف: 60 FPS** = ميزانية إطار **16.67ms** · **120 FPS** = **8.33ms** (يُستفاد تلقائيًّا إن دعمت
  الشاشة، بلا إجبار). أي عملية تتجاوز الميزانية = مشكلة تُقاس. (المبدأ — «رسمة واحدة لكل إطار» — في `١٤`.)

## ٤. لغة العرض (السبب التشغيلي لمكوّن الترجمة `١٤ §٩`)
- **المالك لا يقرأ إنجليزي ولا خام ولا JSON ولا مصطلحات.** فكل الشاشة **عربي بمعنى مترجَم**: أسماء الذرات
  عربية (تُشتقّ من اسم المجلّد)، الصحة تُترجَم لجملة («ما عم يقرا جسر التداول» لا `BRIDGE_UNREADABLE`).
- **عربي RTL · أرقام غربية · خطّ Cairo** (مقروء للمالك).

## ٥. مرجع الشكل (ذوق ومرن — غير مختوم)
- **غرفة العمليات V7 للمالك:** داكن `#05080f` + ذهبي `#f1c40f` + هيدر مركز قيادة + زرّ **إيقاف طارئ**
  أحمر — بس **كل رقم حقيقي من النواة** (كود V7 القديم كانت أرقامه محاكاة `random` = مرفوض).
- **الشكل ذوق ومرن، ليس مختومًا** — يتغيّر وقت ما المالك بدّه؛ الداتا منفصلة عن الشكل.

## ٦. أدوات البناء (شغل كلود — المالك لا يلمسها)
- **Node/Vite = أداة بناء عند كلود فقط.** المُخرَج **ملفات ثابتة (`dist`)**.
- **المكتبات تُحزَم أوفلاين (vendored)** مرّة، تُقدَّم بلا إنترنت.

## ٧. التشغيل والتوزيع (ويندوز · أوفلاين · بلا خلفية)
- سيرفر الحوكمة المحلي يقدّم `dist` — **بلا إنترنت، المالك لا يلمس أداة**.
- **النواة + اللوحة بترمنال ظاهر (foreground)** — بلا خلفية (قاعدة المالك).
- **المنفذ 8010** (8000 محجوز لنظام المالك القديم — لا يُلمَس). لأزرار التحكّم: **CORS** يُضاف في مُشغّل
  النواة (`run_core`, **خارج الختم** — لا مسّ نواة).
- **ويندوز/PowerShell فقط · بلا Bash · كود cross-platform.**

## ٨. خطة البناء المرحلية (كل مرحلة مُثبتة حيّة قبل التالية · شغلة واحدة نظيفة)
**انضباط ضد تكرار الفشل:** قطعة واحدة حيّة (لا محاولتان جنب بعض) · كل قسم حقيقي **لحظة عرضه** (لا قشرة
فاضية) · أرشفة أي متجاوَز + تصحيح الورق **نفس اللحظة**.
1. **الهيكل + المحرّك:** قشرة `١٤ §٤` + مسار الرسم `١٤ §٦` + طبقة الترجمة `١٤ §٩`. إثبات: يفتح/يتصل/يترجم.
2. **أول قسم حقيقي (الذرات) + تشغيل/إيقاف:** شبكة الذرات بالعربي + طزاجة + خام، **مع زرّي تشغيل/إيقاف**
   (خُطّافان جاهزان بالنواة، عبر البوّابة بتأكيد) — لأن المالك يفتح فيرى ذرات واقفة ويريد تشغيلها فورًا،
   فلا ننتظره للمرحلة ٤. **إثبات السلاسة** تحت تدفّق.
3. **بقية أقسام القراءة** على نفس المحرّك — حقيقي حيث داتا، «غير مبنيّ بعد» صادق حيث لا.
4. **بقية بوّابة الأوامر آخِرًا:** إعادة فحص · **تعديل إعداد (نموذج عربي، لا JSON — `١٤ §٨`)** · إيقاف طارئ
   — بالدرجات `١٤ §٨`، بتأكيد، تُسجَّل بالجورنال، **بعد** ثبوت القراءة.

## ٩. الإثبات الحيّ (معايير قبول قابلة للقياس)
- يفتح خلال ثوانٍ قليلة مع آلاف الأحداث · ساعات بلا نموّ ذاكرة · سلاسة تحت تدفّق كثيف · تحديث بطاقة لا يعيد
  رسم الصفحة · رسوم سلسة على تغيّر السعر · تغيير الحجم لا يكسر التخطيط · كل رقم له «خام» · **صفر بيانات وهمية**
  (وجود `random` = فشل) · **كل النصّ عربي** (وجود خام/إنجليزي على الشاشة = فشل).

## ١٠. مخاطر التنفيذ ومنعها
| الخطر | المنع |
|---|---|
| كل حدث يعيد رسم الإطار | البيانات المتدفّقة خارج تفاعل الإطار (refs/Canvas) + Zustand دقيق |
| مكتبة رسوم لا تتحمّل اللحظي | TradingView LWC (Canvas) |
| جداول بلا Virtualization | AG Grid |
| قشرة فاضية تبان كذب | كل قسم حقيقي لحظة عرضه؛ لا شِلّ متروك |
| بيانات وهمية | صفر `random`؛ «غير مبنيّ بعد» صراحةً |
| **التراجع:** أي متجاوَز يُؤرشَف لا يُمحى؛ النواة مختومة لا تُمَسّ؛ كل خطوة مُثبتة قبل التالية |

---

## ١١. قرار المالك
☐ نفّذ كما هو   ·   ☐ عدّل (اكتب)   ·   ☐ أوقف / غيّر

---
**ملاحظة:** كل ما في هذه الورقة **قابل للتغيير بلا مسّ معمارية `١٤`.** لو بدّلنا الإطار أو مكتبة أو رقمًا،
المعمارية تبقى كما هي.
