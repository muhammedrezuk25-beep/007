# تسليم جلسة — دورة الحساب الواحد + الإصلاحات + حدود المخاطر (٢٠٢٦-٠٨-٠٧)

> سياق مكتوب **قبل الضغط** — يلتقط كلّ عمل الجلسة الطويلة للمحادثة الجاية. الحالة مقيسة حيًّا لا مفترَضة.

## صفر — الحالة بسطر
النواة شغّالة **V1.8.0 · 161 ذرة · مدقّق ٠**. **دورة الحساب الواحد تمّت ومُثبتة حيًّا** (فتح→قرار→تنفيذ→إدارة→إغلاق→ربح محقّق→مخاطر). **فايبل** أنهى إصلاح الاحتمالات (350) وجمّد الجذر (النواة) — مجاله، ما لمسته أبدًا. **أنا** أنجزت: تصليح دورة الحساب + دفعة إصلاحات حقيقيّة + ثلاثيّ حدود مخاطر جديد — كلّها محمّلة وصحّية حيّ.

## أوّلًا — دورة الحساب الواحد (تمّت · البوّابة ٢)
- **باگ لافتة `timeframe`** (السبب الجذري لصفر صفقات): الشمعة تحمل timeframe بالأعلى، المشتقّ بالميتاداتا → مفاتيح `(symbol,timeframe)` ما تتطابق. **حُلّ محليًّا: 413/451 مفتاح-بالرمز** (مُثبت). الجذر العامّ = وراثة الهوية بالناقل (شغل فايبل الآن).
- **التوقيت واعٍ بالسوق (158 v1.2.0):** `crypto_symbols`→`crypto_24h` · `always_open_symbols`→`open`. BTC/الذهب يتداولان 24/24-5.
- **حارس «صفقة واحدة للرمز» (463 جديد):** يقرأ `by_symbol` من 609 → يحجب رمزًا محمولًا عند 454. أوقف التكديس.
- **تحجيم متناسب (513 v1.3.0):** مخاطرة 0.5% + `max_lot=20` (اختيار المالك) → لوت مختلف لكلّ رمز.
- **رجل الإغلاق (الإكسبرت v1.01):** كان يفلتر بـmagic صفقة الإغلاق (=0 لليدوي) → صُلّح ليفلتر بـmagic المركز → أي إغلاق (يدوي/SL/TP) يكتب `CLOSED`+ربح → 563/517/516. مُثبت (BTC ضرب SL → −$484 = 0.5% المقصودة).
- ضبط الديمو: 401 min_agreement=1 · 454 min_score=0 + فلتر الثقة استشاريّ.

## ثانيًا — دفعة الإصلاحات الحقيقيّة (فجوات سلك + ضجيج)
- **702 مخزن الصفقات (v2.1.0):** كان يسمع `trade.opened/closed` (ما ينشرها أحد) → **وُصل بـ`platform.trade_event`** (611). يخزّن الآن (stored حيّ).
- **464 فلتر الطزاجة (جديد):** النظام كان يقصف أسهم أمريكيّة مقفولة (NVDA/AMD/INTC) بأوامر مرفوضة **89% من الأحداث**. 464 يحجب أي رمز بياناته بايتة (طزاجة `updated_at` مقابل الأحدث، خالٍ من ساعة) → أوقف القصف (مُثبت 436→436).
- **704 الخط الزمني (v2.1.0):** أحداث ميّتة → `platform.trade_event`+`execution.order.rejected`+`risk.loss_reported`.
- **707 مخزن القرارات (v2.1.0):** `decision.approved`→`decision.approved.state` + احتياطي ميتاداتا.
- **717** = إنذار كاذب (بيانات نظيفة، 0 صف مستقبليّ فعلاً). **411** = placeholder صادق (مشاعر أخبار = ميزة مؤجّلة، رؤية المالك).

## ثالثًا — ثلاثيّ حدود المخاطر (قطع جديدة · قسم 500 · على تصميم المالك)
النظام صار يفتح/يدير/يقفل → الحاجز «لا صفقات» زال، فبُنيت المؤجّلة:
- **508 مدير التعرّض:** notional/الحقوق لكلّ حساب (609+619+618) → `emergency.halt` عند السقف (5000% قابل للضبط). حيّ: emitted=977.
- **507 حدود الربح:** هدف يوميّ + حماية إعادة الأرباح (563+619) → halt.
- **506 حدود الجلسة:** خسارة/صفقات الجلسة + سحب الحقوق (158+619+517+611) → halt.
- الثلاثة تُنفّذ عبر مسار `emergency.halt`→552 الموجود — **بلا لمس 516/500** (صفر تصادم مع فايبل).
- **ممكِّنان:** 563 v1.1.0 (+`account_id`+`result`) · 517 v1.1.0 (+`account_id`) → عزل المخاطر لكلّ حساب. (563 `result` صلّح إثراء 702 كمان.)

## رابعًا — فايبل (مجاله · ما لمسته أبدًا)
- المالك: **فايبل صلّح الاحتمالات (350) وجمّد الجذر (النواة).** قاعدتي: «لا تقرب على النواة/350/شغل فايبل ولا تنزع شغله أبدًا».
- شفته يضيف `timeframe` بأعلى الذرات (158/454/513…) لتصليح باگ التوقيت من المصدر (بنى فوق شغلي، محافظ عليه).
- **⚠️ ملاحظة تنسيق:** النواة الشغّالة تعرض 158 v1.2.0 و454 v1.4.0 (نسخي، قبل كنس فايبل). لو تعديلات فايبل (v1.3.0/1.5.0 على القرص) بدها تحميل → إعادة تشغيل النواة تحمّلها. **يُنسّق مع فايبل/المالك — ما ألمسها.** يُتحقّق حيّ أنّ 351 يطلع continuation لا neutral بعد التحميل.

## خامسًا — المعلّق (بترتيب · قرار المالك)
1. **إصلاح 516:** حدّ `MAX_OPEN_TRADES` **ميّت** (`_open_trade_count` لا يُحدَّث) — وصله بعدّ مراكز 609.
2. **دمج حالات المخاطر (تعرّض/ربح/جلسة) بمجمّع 500** → صورة موحّدة للّوحة.
3. **إنفاذ ألطف** (رفض أمر جديد بدل halt كامل) بدمج 516 — قرار سياسة المالك.
4. **تعدّد الحسابات/المحافظ (650/651)** — التوسّع؛ يحتاج تصميم المالك.
5. **محجوب ببيانات:** 106 عمق (0 صف بالجسر — شغل إكسبرت) · 107 شريط.
6. **البوّابة ٢ لحدود المخاطر:** تُثبَت لمّا تتحقّق حدود فعليّة (تعرّض/ربح/جلسة) حيًّا على صفقات.

## سادسًا — دروس تشغيليّة مهمّة
- **تغيير الإعداد يحتاج رفع نسخة** (الفحص الدوري كل ٥ث يقارن النسخة فقط · يتسلّل بين تعديلات) — عدّل الإعداد أوّلًا، ارفع النسخة آخرًا. (كلّف فوضى ٣٥ مركز.)
- **اختبارات الجسر:** `os.environ.pop("NQ_BRIDGE_DB")` بأوّل الاختبار وإلا يقرأ الجسر الحيّ بدل الملف المؤقّت (فخّ معروف).
- **الأحداث الميّتة:** ذرات كثيرة بُنيت على أسماء أحداث قديمة لا تُنشَر (`trade.opened`…) — تُوصل بالأحداث الحيّة (`platform.trade_event`…).
- **التوازي مع فايبل:** بناء **ذرات جديدة** (ملفّات جديدة) = صفر تصادم. تعديل الموجود = خطر. ابقَ بمجالك (مخازن/مخاطر/إكسبرت/بيانات)، فايبل بمجاله (تحليل/قرار/احتمالات/نواة).

## الملفّات المرجعيّة
`سياق/قسم ٥٠٠ — المخاطر` (محدّث: الثلاثي مبنيّ) · `سياق/تخبيص مفتاح timeframe — الاحتمالات` (لفهم الجذر) · ذاكرة `single-account-cycle-live` (الحالة الحيّة).
