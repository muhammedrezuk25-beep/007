# قسم ٣٥٠ — الاحتمالات (Probability)

> **الحالة:** 🟡 **النواة كاملة — ١٠ ذرات · مدقّق ٠ · اختبارات خضراء · «سقالة قابلة للمراجعة».**
> **⚠️ حدّ الرؤية:** النماذج (351..357,359) منهجيّتها **مبدئيّة قياسيّة — حافة المالك تحلّ محلّها.**
> **358 هيرست وحده قياس موضوعيّ صرف.** لا اعتماد قرار تداوليّ قبل مراجعة المالك (قاعدة 3).
> **المالك:** محمد رزوق · **المرجع الأمّ:** `سياق/حالة بناء الذرات.md` · `سياق/أسئلة مؤجلة — للآخر.md`.

## الغرض
عمود يحوّل **حقائق السوق الخام** (بنية 200 · سيولة 250 · إحصاء 300) إلى **احتمالات سيناريوهات**
لكل (رمز, دورة) — يُغذّي **400 الاستراتيجية**. كل نموذج يستهلك إشارة أساسيّة واحدة (عزل) ويُخرج احتمالًا
عبر **جدول عتبات معلَن** (شفّاف · قابل للضبط من اللوحة).

## الوحدات (١٠) — بلا دمج
| # | الذرة | المدخل الأساس | المخرج `probability.*` | الإشارة |
|---|---|---|---|---|
| 351 | نموذج الاتجاه | 207 اتجاه + 208 طور | `.trend` | continuation/exhaustion/neutral |
| 352 | نموذج الانعكاس | 205 CHoCH + 206 MSS | `.reversal` | reversal/neutral |
| 353 | نموذج الاختراق | 204 BOS + 255 FVG | `.breakout` | breakout/neutral |
| 354 | نموذج الارتداد | **254 كنس** (تلاشٍ) | `.pullback` | pullback/neutral |
| 355 | نموذج الزخم | 308 انحدار + 309 r² | `.momentum` | strong/weak/neutral |
| 356 | نموذج المدى | 358 هيرست + 207 اتجاه | `.range` | ranging/trending/neutral |
| 357 | دمج النماذج | النماذج الستّة | `.merged` | السيناريو المهيمن/neutral |
| **358** | **معامل هيرست** | إغلاقات (R/S حقيقي) | `.hurst` | trending/mean_reverting/random_walk |
| 359 | مجمّع الثقة | النماذج الستّة | `.confidence` | high/low_confidence |
| 350 | مدير الاحتمالات | ٩ وحدات (نمط B) | `probability.cycle.collected` | — |

## تدفّق الأحداث
`market_data.candle_closed` يقود كل نموذج ليُصدر **كل دورة** (يخزّن حالة إشارته الأحدث بين الأحداث المتقطّعة)
→ `probability.<x>.state` بنفس `cycle_id` → 357 يختار المهيمن · 359 يقيس الثقة → **350 المدير** يجمّع ٩ →
`probability.cycle.collected` → **400**. 356 يستهلك 358 بالحدث (عزل، لا استيراد).

## منهجيّة النماذج (⚠️ سقالة — بند 🔴‑4 بالأسئلة المؤجّلة)
كل نموذج: `احتمال = أساس + أوزان×(إشارات حاضرة)` بسقف · العتبات **إعدادات معلَنة**. 354 يتلاشى بعد الكنس
عبر الشموع. **هذا منطق قياسيّ شفّاف مبدئيّ — يُراجَع ويُستبدَل بحافة المالك.** 357 «مهيمن = أعلى درجة، وneutral
إن الفارق < neutral_band».

## 358 هيرست — قياس موضوعيّ (لا رأي)
R/S حقيقي: لكل مقياس n∈{8,16,32,…} `R/S=(مدى الانحرافات التراكميّة)/الانحراف` على قطع غير متداخلة → متوسّط
→ انحدار `log(R/S)` على `log(n)` = H. H>0.5 اتجاهيّ · <0.5 ارتداديّ · ≈0.5 عشوائيّ. نافذة 128.

## ⏸️ مؤجّل بوعي — عمليّات النماذج (360..368)
الطقم المرجعيّ يضع ٩ ذرات ML‑lifecycle/مراقبة (دقّة · انجراف · أولويّة · اختيار · مستودع · محمّل · مقارنة ·
إعادة معايرة · تاريخ إصدارات). **لم تُبنَ** — تفترض منظومة نماذج مدرَّبة/محفوظة غير موجودة + بعضها حوكمة
(قاعدة 15). **قرار المالك:** نبني منظومة نماذج مدرَّبة أم النماذج الحدسيّة تكفي؟ (بند 🔴‑5.)

## الفحص
Gate-1 (مدقّق ٠ + اختبارات خضراء) تمّ للعشرة. **Gate-2 الحيّ ينتظر إعادة تشغيل المالك للنواة** (تُحمَّل عند الإقلاع).
