# ورقة بناء — النواة ٠٧: إرجاع الالتفافات جوّا النواة (فكّ التبعية الخارجية)

**نوع الورقة:** تشريح تنفيذ كامل — يُجاب على **كل سؤال** فيه **قبل** أي سطر كود.
**التاريخ:** 2026-08-04 · **الوحدة:** النواة ① · الجزء ٠٧ · **الحالة:** ✅ **منفّذة ومختومة — Core V1.8.0 (2026-08-04)** — الأثر رجع جوّا الناقل، `trace_middleware` أُبطِل (جذع مهجور)، المُشغّل انحف لـ«تشغيل + توصيل». التفاصيل: ورقة ٠٨.
**يمسّ النواة** (كانت آخر تنظيف قبل الختم النهائي — وقد تمّ الختم).

> طريقتنا: من الجذر لا ترقيع · نرفع لا نبسّط · بسيط لا مبالغة · لا كود قبل قسم (٨) و"نفّذ".
> **هي ورقة تنظيف، لا ميزة:** بترجّع الإصلاحات اللي انعملت **برّا** النواة (التفافًا على الختم) إلى
> **جوّاها**، فما يبقى شي ملزوق برّا وقت الختم. (أصلها: فحص المالك — «النواة من سكربت خارجي · أسرار برّا · التفافات».)

---

## ١. ما هي الوحدة (بلغة بسيطة)
النواة كانت مقفولة (مختومة)، فكم إصلاح انعمل **من برّا** — ملزوق عليها وهي شغّالة، بدل ما يكون
جوّاها. هالوحدة بترجّع هالالتفافات **جوّا النواة** وبتمسح ملفات اللزق. الهدف: نواة **مكتفية بذاتها**،
بلا إصلاحات معلّقة برّا.

## ٢. الهدف و"ليش"
- **لا يبقى التفاف عند الختم:** الختم النهائي ما يصحّ وفي إصلاح ملزوق من برّا (مبدأ كسر التجميد:
  «لا يُفتح لسبب ثم يُكتشف الباقي بعد الختم»).
- **نواة مكتفية:** ما تعتمد على سكربت خارجي يحقن فيها منطقها.

## ٣. الموجود (قِسته بالكود — بالدليل)
- **(أ) توريث الأثر ملزوق من برّا:** `scripts/trace_middleware.py` يعمل **monkey-patch** لـ
  `EventBus.publish` من خارج الختم (`EventBus.publish = publish_with_inheritance`). **الملف نفسه
  مكتوب فيه:** «عند الفتحة: يُنقل جوّا الناقل ويُحذف هذا الملف — جسرٌ إلى الفتحة لا بديلًا عنها».
- **(ب) المُشغّل يهيّئ نواة من برّا:** `run_core.py` (خارج الختم) يعمل: `core_logger.configure`
  (إعداد سجل النواة) · `install_trace_inheritance` (لزق الأثر) · `set_secret_provider` (ربط الأسرار).
- **(ج) الأسرار خارج النواة:** حزمة `security/` — النواة جاهلة عنها (**صحّ معماريًا**)، بس مربوطة من المُشغّل.
- **(د) الختم متقاعد:** `core/CORE.lock.retired_2026-08-03` موجود — النواة **مفتوحة فعليًا الآن**.

## ٤. التشريح الدقيق — شو راح أعمل بالضبط
- **(أ) الأثر → جوّا الناقل:** منطق `trace_middleware` يصير **جزء من `EventBus.publish` نفسه** (هاد
  بالضبط شغل **ورقة ٠٤**). بعد ما ٠٤ تنزل → **يُحذف `scripts/trace_middleware.py`** ويُشال استدعاؤه
  من `run_core`. **(٠٧ تعتمد على ٠٤: ٠٤ تحطّ المنطق جوّا، ٠٧ تمسح اللزق.)**
- **(ب) المُشغّل ينحف — «يشغّل ويوصّل» لا «يحقن منطق»:**
  - **يرجع جوّا النواة:** إعداد سجل النواة + لزق الأثر (صار جوّا بـ٠٤) — من طبيعة النواة.
  - **يبقى بالمُشغّل (صحّ):** بدء الخادم/الـAPI، وربط الأسرار (النواة جاهلة). المُشغّل يبقى قشرة تشغيل
    نظيفة، بلا أي منطق نواة محقون.
- **(ج) الأسرار:** **تبقى برّا** (النواة جاهلة — خطّ أحمر). بس نتأكّد الربط عبر واجهة نظيفة وحيدة.
- **(د) الختم:** **آخر شي — بعد ما ٠٢..٠٧ كلها جوّا ومثبتة حيًّا.** **مو الآن.** (تُعاد الختمة V2.0 فقط
  لمّا ما يبقى ولا التفاف ولا مكسور — كلام المالك.)

**مصفّى (ضد المبالغة):** ما نضيف ميزة. بس نرجّع اللي برّا لجوّا، ونمسح اللزق. لا أكثر.

## ٥. فحص الخطوط الحمر
- **جاهلة:** الأسرار تبقى برّا (النواة ما تعرفها). ✓ الأثر منطق تقني عام بلا معنى أعمال. ✓
- **العزل:** ما يتغيّر. ✓
- **رفع لا تبسيط:** نواة **مكتفية بذاتها** أمتن من نواة تعتمد على لزق خارجي. ✓

## ٦. الإثبات الحيّ (تشوفه بعينك)
- **قبل:** `trace_middleware.py` موجود + مستدعى من `run_core`.
- **بعد:** الأثر يشتغل **بلا** الملف (منطقه جوّا الناقل) → **الملف انمسح**، والنظام يشتغل، والأثر متّصل
  (سلسلة واحدة). دليل حيّ: شغّل سلسلة (تكّة→…→أمر) → trace واحد، **بلا أي ملف خارجي**.
- **المُشغّل:** فتّش `run_core` → ما ضلّ فيه حقن منطق نواة (بس تشغيل وتوصيل).

## ٧. المخاطر والتراجع
- **يمسّ إقلاع النظام:** لكن التغيير **نقل منطق مثبت** (مو منطق جديد). يُثبَت بتشغيل حيّ **قبل** حذف أي ملف.
- **الترتيب مهم:** ٠٧ تجي **بعد** ٠٤ (الأثر لازم يكون جوّا قبل ما نمسح اللزق).
- **التراجع:** git.

## ٨. الأسئلة — التقني على كلود
**قرارات تقنية حسمها كلود (لا تلزم المالك):**
- الأثر جوّا الناقل (مع ٠٤) ثم **حذف** `trace_middleware.py`.
- الأسرار **تبقى برّا** (النواة جاهلة — صحّ).
- المُشغّل ينحف لقشرة «تشغيل + توصيل» بلا منطق نواة.

**للمالك — لا سؤال تقني، بس تأكيد مبدئك:** **ما نختم لحدّ ما يرجع كل شي جوّا** ولا يبقى التفاف. (كلامك.)

## ٩. قرار المالك
☐ نفّذ كما هو  ·  ☐ عدّل (اكتب)  ·  ☐ أوقف / غيّر

---
**الترتيب:** ٠٧ تُنفَّذ **ضمن فتحة النواة الواحدة مع ٠٢..٠٦** (تلمس نفس الملفات)، وهي **آخر تنظيف قبل
الختم النهائي.** فصارت أوراق النواة: **٠٢ العيون · ٠٣ الحالة عند الاشتراك · ٠٤ الأثر · ٠٥ الساعة ·
٠٦ المهلات · ٠٧ إرجاع الالتفافات** → كلها سوا، ثم الختم.
