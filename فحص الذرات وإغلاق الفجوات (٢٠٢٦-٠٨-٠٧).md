# فحص الذرات وإغلاق الفجوات — نحو الاعتماد والختم (٢٠٢٦-٠٨-٠٧)

> **طلب المالك:** «افحص شغلك (الذرات) وسُدّ كل الفجوات مشان نعتمدها ونختمها.»
> **الحدود:** لا نواة · لا احتمالات 350 (فايبل) · لا حوكمة (850/851/مجمّعات العرض).

## طريقة الفحص (منهجيّة لا انطباع)
1. **المدقّق البنيويّ** (`validate_atoms`) على ١٦١ ذرة.
2. **كل الاختبارات** (`test_atoms`) — هرمتيّة (`NQ_BRIDGE_DB` مُصفّى).
3. **رسم النشر/الاشتراك** (سكربت `wire_audit.py`): كل حدث مشترَك بلا ناشر (اشتراك ميّت/مجوّع) · كل حدث منشور بلا مستهلك (مقبرة/خطّاف).
4. **اكتمال المدراء** (`mgr_audit.py`): هل كل مدير قسم يجمّع **كل** وحداته؟

## الأرضية (البوّابة ١) — خضرا
**مدقّق ١٦١ · ٠ مخالفة · ٠ تحذير** · **١٦١/١٦١ ذرة · ٧٧٧ اختبار ناجح.**
(الأرضية «تكذب بالاتجاهين» — الإثبات النهائيّ = البوّابة ٢ الحيّة، المالك شايف.)

## ✅ فجوات وظيفيّة حقيقيّة — سُدّت (بمجالي · مدقّق ٠ + اختبار)
1. **516 قاطع الأمان — حدّ `MAX_OPEN_TRADES` كان ميّتًا (v1.1.0):** `_open_trade_count` لم يكن يُحدَّث أبدًا (لا مصدر) → الحدّ لا يرفض شيئًا. **الحلّ:** اشتراك بـ`platform.positions.state` (609) → يُحدَّث من `open_count` الحيّ. اختبار جديد `test_max_open_trades` (٦→٧).
2. **500 مدير المخاطر — لا يجمّع 506/507/508 (v1.1.0):** حدود التعرّض/الربح/الجلسة كانت تنشر بلا مستهلك → الصورة الموحّدة ناقصة. **الحلّ:** اشتراك بـ`risk.exposure.state`+`risk.profit_limits.state`+`risk.session_limits.state` → تُضمَّن بـ`risk.unified.state` لكل حساب. اختبار جديد `test_aggregates_limits` (٥→٦). (يجمّع ولا يفرض — المبدأ محفوظ.)
3. **114 موحّد البيانات — مقبرة بيانات موثّقة غلط (توثيق فقط):** الشرح ادّعى «التحليل 150 يقرأ `market_data.normalized`» والواقع أنّ التحليل يقرأ `candle_closed` (103) و701 يقرأ `price_received` (102) → `normalized` **بلا مستهلك حيّ**. صُحّح التوثيق (قاعدة ٥) → خطّاف مُهيَّأ بلا مستهلك، قرار المالك: وصل مستهلك أو تقاعد ذيل 113→114.

## 🟡 اكتمال المدراء — ٥ من ٦ يجمّعون كل وحداتهم
150 · 200 · 300 · 350 · 400 = **كامل**. **250 السيولة يستثني 256/257/258** (دلتا/CVD/امتصاص) — **محجوبة ببيانات** (تحتاج عمق/تدفّق أوامر L2 غير متوفّر). استثناؤها **متّسق** مع حجب 106/107 (لا عطل).

## 📋 السجلّ الصادق — بنود ليست عطلًا، لكن لا تُغلَق بيدي (قرار المالك قبل الختم)
**أ) محجوبة ببيانات (تحتاج مصدرًا غير موجود — خطّافات صادقة UNAVAILABLE):**
- **106 عمق** (`market.depth` بلا ناشر) · **107 شريط صفقات** (`market.trade` بلا ناشر) · **256/257/258** (دلتا/CVD/امتصاص — تعتمد على عمق/شريط). = قدرة واحدة مؤجّلة: **تدفّق الأوامر / عمق L2** — تحتاج جسرًا يقرأ العمق من الإكسبرت.

**ب) خطّافات مؤجّلة لطور قادم (قرار المالك):**
- **709 مخزن المحفظة** ← `portfolio.summary` من **650** (طور المحفظة — التالي بالخريطة).
- **706 مخزن النماذج** ← `model.persist_requested`/`load_requested` من **دورة حياة ML (360–368)** — رؤية المالك.
- **116 كشف الانقطاع** يراقب `feed.binance/rithmic/yahoo.tick` — **مصادر مستقبليّة** (الحيّ الآن mt5 فقط).
- **411 استراتيجيّة الأخبار** = خطّاف صادق UNAVAILABLE (الأخبار بلا اتجاه — تحتاج ميزة مشاعر + رؤية المالك).
- **708 سجل الرموز** يعرض خدمة حلّ عند الطلب (`resolve_requested`/`map.state`) — **مُهيَّأة، غير مستدعاة** (الذرات تستعمل الرمز الخام).

**ج) بنود كامنة (مسجّلة بأسئلة مؤجلة — قرار المالك):**
- **574 الإغلاق الجزئيّ:** علامة «قُبض» بالرام → إعادة تشغيل قد تعيد القبض (575 معطّل الآن فلا أثر). حلّ جذريّ: استنتاج القبض من نقص الحجم مقابل الفتح.
- **516 حالة القاطع بالرام:** إعادة التشغيل تفكّ الإيقاف وتصفّر عدّادات اليوم — مقصود أم snapshot؟

**د) نظافة (ليست ذرة · المالك يحذف بيده):**
- `scripts\مجلد جديد` = نسخة مكرّرة كاملة من `scripts\` — يُؤرشَف/يُحذف بيد المالك.

## 🔒 معنى الختم + التسلسل المقترح
- **الختم** = إضافة `atoms/` إلى بصمة سلامة الملفّات (007) — تُجمَّد الذرات كما جُمِّدت النواة (كشف أي تعديل لاحق).
- **قبله لازم البوّابة ٢ الحيّة (عين المالك):** إعادة تشغيل النواة كاملة → رؤية (١) الاحتمالات تخرج من الحياد (إصلاح timeframe) · (٢) 516/500 حيّ · (٣) لا عطل صامت.
- **الخطّافات المؤجّلة/المحجوبة تُختَم كما هي** (صادقة · مستقرّة · موثّقة) — الختم لا يتطلّب تفعيلها، يتطلّب صدقها.

## أدوات الفحص (scratchpad · للجلسة الجاية)
`wire_audit.py` (رسم النشر/الاشتراك) · `mgr_audit.py` (اكتمال المدراء).
