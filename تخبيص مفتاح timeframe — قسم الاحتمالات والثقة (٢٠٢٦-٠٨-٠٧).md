# تخبيص مفتاح الهوية `timeframe` — قسم الاحتمالات (٣٥٠) والثقة معطّلان

**التاريخ:** 2026-08-07
**لمن:** محادثة الذرات (قسم 350 مجالها — الإصلاح عندهم، لا بالنواة).
**القرار الحاكم:** 🔒 النواة مختومة V1.8.0 — **مافي فتح نواة خالص** (قرار المالك، صريح). فالحلّ الجذري (وراثة الناقل لمفاتيح الهوية، قاعدة ٢٢) **مؤجّل/خارج الطاولة**. الإصلاح = على مستوى الذرات فقط.

---

## المشكلة بسطر
`timeframe` مفتاح هوية (متل `symbol`)، بس موضوع **بشكل متضارب**: الشمعة (103) تحطّه **بالأعلى = '60s'**، بينما الذرات المشتقّة (البنية 207/208، النماذج 351…) تحطّه **بالميتاداتا فقط (الأعلى = None)**. فأي ذرّة تجمّع بمفتاح `(symbol, timeframe)` وتخلط الشمعة مع المشتقّ → المفاتيح ما تتطابق → التجميع يطلع فاضي.

## الدليل الحيّ (مقيس 2026-08-07)
- **207 البنية:** BTC = **هابط** · الذهب = **صاعد** (بنية حقيقيّة مكتشفة) · `timeframe` بالأعلى=None، بالميتاداتا='60s'.
- **351 احتمال الاتجاه:** كلّ الرموز **`neutral` · dir=none** — حتى BTC والذهب اللي 207 لقى فيهم اتجاه! السبب: يربط الشمعة بـ`(رمز,"60s")` والبنية بـ`(رمز,"")` → `tr=None` دايمًا → `neutral`.
- **359 مجمّع الثقة:** يجمّع النماذج بـ`(رمز, "")` (لأنّ مخرج النماذج timeframe بالميتاداتا) ويقارن بالشمعة `(رمز,"60s")` → `present=0` → **`low_confidence` دايمًا** → 460 يحجب كلّ شي.
  - **لهذا:** خلّيت فلتر الثقة (confidence_filter) **استشاريًّا** بـ454 وقت ضبط الديمو — وإلا كان يوقف كلّ الصفقات.

## المدى (الذرات المصابة — كلّها تجمّع بـ`(symbol, timeframe)`)
- **351–356** (النماذج الستّة: اتجاه/انعكاس/اختراق/ارتداد/زخم/مدى) · **357** (دمج النماذج) · **359** (مجمّع الثقة). **تأكّدوا من كلّ واحدة** (تأكّدت من 351 و359 حيًّا؛ البقيّة نفس النمط).
- **سابقة مطابقة:** 413 و451 كان فيهم نفس العلّة بالضبط — **اتصلّحوا هالجلسة** (مفتاح-بالرمز فقط، مثبت حيّ: الأصوات صارت تُحسب والصفقات تُفتح). نفس الوصفة تنطبق هنا.

## الحلّ (بمجالكم — الذرات · بلا نواة)
**مفتاح-بالرمز فقط** (إسقاط timeframe من المفتاح) — النظام حاليًّا **إطار واحد (60s)** فالرمز كافٍ للهوية. بالضبط متل 413/451:
- بدّلوا `_key = (symbol, timeframe)` → `_key = symbol` في **351–356 · 357 · 359** (وأي مُجمِّع مشابه).
- **بديل أنظف** لو ما بدكم تُسقطوا tf: اقرأوه بترتيب احتياطي `payload.get("timeframe") or metadata.get("timeframe","")` — يلتقط الاثنين. لكن مفتاح-بالرمز أبسط وكافٍ الآن.
- **لا تدمجوا ذرات** (قاعدة المالك) — بس صلّحوا المفتاح داخل كلّ ذرّة.

## ⚠️ تنبيه — لا تخدعكم النتيجة بعد الإصلاح
حتى بعد تصحيح المفتاح، **النماذج heuristic** (مثال 351: `احتمال = 0.55 + 0.05×التأكيدات`، سقف 0.9 — أرقام مضبوطة باليد) — رح **تشتغل** (تعطي continuation/exhaustion واحتمالات معقولة الشكل)، بس **مو معايَرة من تاريخ فعلي**. الاحتمال الحقيقي المقاس = التعلّم الآلي (360–368) = **مؤجّل يحتاج رؤية المالك**. (استثناء: **هيرست 358** رياضة موضوعيّة حقيقيّة — تحليل R/S.)

## بعد الإصلاح — الفحص ٢ الحيّ (إلزامي)
راقبوا حيًّا على النواة الشغّالة:
- `probability.trend.state` (351): لازم يطلع **continuation/exhaustion** لـ BTC/الذهب (مو neutral).
- `probability.confidence.state` (359): **high_confidence** لمّا النماذج تتّفق.
- وقتها ممكن (بقرار المالك على الديمو) ترجعوا فلتر الثقة (460) **حاجزًا** بدل استشاريّ في 454.

## القرار الحاكم (تكرار للتأكيد)
🔒 **النواة مختومة — لا تُفتح إطلاقًا.** الحلّ الجذري (وراثة الهوية بالناقل، قاعدة ٢٢) مؤجّل بقرار المالك. **الإصلاح الآن = ذرات، مفتاح-بالرمز، بمجال محادثة الذرات.**
