# ورقة بناء — الحوكمة ١٧: التحكم عن بعد (سفر المالك)

**التاريخ:** 2026-08-07 · **السبب:** المالك مسافر بعد أيام ويريد التحكم بشغله عن بعد.
**المبدأ (٠٩):** اللوحة وتلغرام وأي واجهة → نفس البوّابة. **نقل غرفة القيادة نفسها، لا بناء واجهة ثانية.**

## ١. الحل المعتمد: شبكة خاصة (Tailscale) + نفس اللوحة من الموبايل
- Tailscale = شبكة خاصة مشفّرة بين أجهزة المالك فقط (جهاز البيت + الموبايل) عبر الإنترنت — لا فتح منافذ على الراوتر، لا يصل إليها أحد غيره.
- على الموبايل: المتصفّح → `http://<عنوان-الجهاز-في-Tailscale>:8090` → **غرفة القيادة كاملة**: كل الأقسام والأزرار (إيقاف طارئ · تصفير · إعدادات · نسخ · أدوات).

## ٢. المبنيّ (جاهز بالكود — 2026-08-07)
1. **`transport.ts`**: عنوان بثّ النواة صار من عنوان الصفحة (`location.hostname`) — محليًّا 127.0.0.1 كما هو، وعن بعد يصير عنوان الجهاز تلقائيًّا. البثّ الحيّ يشتغل من الموبايل.
2. **`server.py`**: مفتاح تفعيل — وجود ملف **`var\governance\remote_on.txt`** = الخادم يفتح للشبكة (`0.0.0.0`)، غيابه = محلي فقط كما كان. يطبع حالة التفعيل عند الإقلاع.
3. **عند التفعيل يلزم أيضًا:** `config/core.yaml` → `api.host: 0.0.0.0` (للبثّ 8010) — **يقلبها كلود عند أمر المالك** قبل السفر + إعادة تشغيل.

## ٣. حدود أمان (مكتوبة بصدق)
- **الحماية = Tailscale + راوتر البيت (لا فتح منافذ للإنترنت).** بلا Tailscale، «مفتوح للشبكة» يعني شبكة البيت المحلية فقط (خلف NAT) — الإنترنت لا يصل.
- **مفتاح API للنواة لم يُفعَّل** رغم توصية `core.yaml` لأي host غير محلي — سببه تقني مقيس: متصفح الموبايل لا يستطيع إرسال ترويسة `X-API-Key` على WebSocket، فتفعيله يكسر البثّ. **الحدّ الأمني المعتمد = الشبكة الخاصة، لا مفتاح.** (لو أراد المالك طبقة مفتاح لاحقًا: تحتاج بروكسي WS بخادم الحوكمة — عمل أكبر، يُقرَّر لاحقًا.)
- ويندوز فايروول سيسأل مرة أولى «السماح لبايثون بالشبكة؟» → موافقة مرة واحدة.

## ٤. خطوات المالك قبل السفر (١٠ دقائق — كلود يرافقه خطوة خطوة)
1. تنزيل **Tailscale** على جهاز البيت + على الموبايل، وتسجيل دخول بنفس الحساب (حساب جوجل مثلًا) — **إنشاء الحساب وإدخال كلمات السر بيد المالك حصرًا.**
2. كلمة للكلود: «فعّل البعيد» → ينشئ `remote_on.txt` + يقلب `core.yaml` host + إعادة تشغيل غرفة القيادة → تجربة من الموبايل والمالك بالبيت (فحص ٢ قبل السفر!).
3. الجهاز يبقى شغّالًا طول السفر: غرفة القيادة + MT5 مفتوحين · **إيقاف السكون (Sleep) من إعدادات الطاقة** · يُفضَّل UPS.
4. **خط رجوع ثانٍ (موصى به):** برنامج سطح مكتب بعيد (AnyDesk أو Chrome Remote Desktop) — لو احتاج شيئًا خارج اللوحة (MT5 نفسه، إعادة تشغيل كاملة).

## ٥. تلغرام (طور لاحق اختياري — البوّابة جاهزة له)
تنبيهات push (صفقة فُتحت/أُغلقت · خلل · قاطع) + أوامر (`/status` `/halt` `/confirm`) → تُكتب بجسر بوّابة الأوامر (٩٠١) نفسه بكل قواعده (هوية · تأكيد بخطوتين · تجاهل الغريب). **يحتاج توكن بوت من المالك (BotFather) — يضعه المالك بنفسه بملف محلي، لا يُرسَل بالمحادثة.** يُبنى بكلمته.
