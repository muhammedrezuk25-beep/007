# قسم ٥٠٠ — المخاطر (Risk)

> **الحالة:** 🟢 **٧ ذرات مبنيّة (500/513/516/517 + 506/507/508 حدود جديدة 2026-08-07) · مدقّق ٠ · اختبارات خضراء · على تصميم المالك، قابل للمراجعة.**
> **المصدر:** تصميم المالك القديم (`Desktop\ذرات\atoms\500`) — READMEs لـ500/516 + كود الباقي (أفكار، إعادة
> بناء نظيفة بلا `time.time()`/ترويسة/عربي‑بالكود). **قرار المالك: «كمل».**
> **🛑 بوّابة الأمان قبل المال الحقيقيّ — لا تنفيذ حقيقيّ قبل موافقة المالك الصريحة (550).**

## الغرض
يحمي رأس المال: يتحقّق من كل طلب تنفيذ مقابل الحدود، يراقب الخسائر، ويقطع التداول عند الخطر.
**بوّابة المخاطر الآن بالمسار:** 467 (إرسال القرار) → `execution.order.requested` → **516 يتحقّق** →
`risk.validation.completed`.

## الوحدات (٣ نواة)
| # | الذرة | الدور |
|---|---|---|
| **516** | **قاطع الأمان (Circuit Breaker)** | يتحقّق من `execution.order.requested` (kill/عدد صفقات) → `risk.validation.completed {approved,reason}` · يراقب `risk.loss_reported` → خسارة يوميّة/متتالية → **يفعّل القاطع** + `emergency.halt` |
| **500** | **مدير المخاطر (مجمّع)** | يبني `risk.unified.state` (الصورة) — **يجمّع ولا يفرض** (516 وحده يفرض) |
| **517** | **نتيجة الصفقة** | P&L الصفقة + Equity → `risk.loss_reported {loss_pct, is_loss}` · **خطّاف صادق `NO_TRADES_YET`** حتى تُبنى صفقات |
| **513** | **تحجيم المركز (حجم اللوت)** | `لوت = (Equity×risk%) / (مسافة الوقف × قيمة الوحدة)` من 619 Equity + 618 مواصفات + سعر الشمعة → `risk.position_size.state` · يستعمله 551 (550) |

## 🛑 دلالات الأمان (516 — حرجة · مُختبَرة)
- **إعادة تفعيل يدويّة فقط:** القاطع يبقى مفعّلًا حتى `risk.kill_switch.reset_requested` صريح — **لا** يُصفّى
  بيوم جديد/إعادة تشغيل/زوال السبب (معيار Circuit Breaker حقيقيّ).
- **يوم جديد** (`SYS_DAY`) يصفّر الخسارة اليوميّة وعدد الصفقات **فقط**.
- **القاطع المفعّل = DEGRADED دائمًا** (التداول متوقّف، لا يقرأ أخضر). **لا ساعة** (قاعدة 13).
- الحدود (خسارة يوميّة 5% · متتالية 3 · صفقات يوميّة 20 · مفتوحة 5) **إعدادات معلَنة قابلة للضبط**.

## الأسباب القياسيّة (516)
`KILL_SWITCH` · `RISK_DAILY_LIMIT` · `MAX_CONSECUTIVE_LOSSES` · `MAX_DAILY_TRADES` · `MAX_OPEN_TRADES`.
(halt شامل للخسائر · رفض طلب فقط لتجاوز عدد الصفقات — صفر تأثير على مركز قائم.)

## ✅ 506/507/508 — بُنيت (2026-08-07 · الحاجز «لا صفقات» زال · النظام يفتح/يدير/يقفل)
- **506 حدود الجلسة:** خسارة/عدد صفقات لكلّ جلسة زمنيّة + سحب الحقوق من القمّة → `emergency.halt`. (158 جلسة · 619 حقوق · 517 خسارة · 611 صفقات).
- **507 حدود الربح:** هدف ربح يوميّ (قفل المكسب) + حماية إعادة الأرباح → `emergency.halt`. (563 نتيجة · 619 حقوق).
- **508 مدير التعرّض:** التعرّض الاسميّ (notional) لكلّ حساب مقابل الحقوق → `emergency.halt` عند تجاوز السقف. (609 مراكز · 619 حقوق · 618 مواصفات).
- **الإنفاذ:** الثلاثة تُطلق `emergency.halt` عند التجاوز → 552 يوقف الإرسال (إعادة يدويّة). **بلا لمس 516/500** (صفر تصادم مع عمل فايبل المتوازي).
- **إضافات ممكِّنة:** 563 v1.1.0 + 517 v1.1.0 يمرّران الآن `account_id` (+ 563 `result`) → عزل المخاطر لكلّ حساب (قاعدة ٢٢).
- **⏸️ لاحقًا (بإذن/رؤية المالك):** إنفاذ ألطف (رفض أمر جديد بدل halt) بدمج 516 · دمج حالات التعرّض/الربح/الجلسة بمجمّع 500 · إصلاح `MAX_OPEN_TRADES` الميّت بـ516 (`_open_trade_count` لا يُحدَّث — يحتاج وصله بعدّ مراكز 609).
- **الإثبات:** Gate-1 (مدقّق ٠ · 506:٦/٦ · 507:٥/٥ · 508:٤/٤). Gate-2 الحيّ عند رجوع النواة (فايبل واقفها للاحتمالات/الختم).

## الفحص
Gate-1 (مدقّق ٠ + اختبارات خضراء) للثلاثة (516: ٦/٦ · 500: ٥/٥ · 517: ٣/٣). **Gate-2 الحيّ عند إعادة تشغيل النواة.**
