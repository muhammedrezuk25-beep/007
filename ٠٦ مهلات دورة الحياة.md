# ورقة بناء — النواة ٠٦: مهلات دورة الحياة + تنظيف sys.modules (مشكلة التحميل الحار)

**نوع الورقة:** تشريح تنفيذ كامل — يُجاب على **كل سؤال** فيه **قبل** أي سطر كود.
**التاريخ:** 2026-08-04 · **الوحدة:** النواة ① · الجزء ٠٦ · **الحالة:** ✅ **محسومة — كل القرارات تقنية حملها كلود (لا تلزم المالك)** · جاهزة للتنفيذ ضمن فتحة V2.0
**يمسّ النواة** (آخر جزء قبل «فتحة النواة الواحدة» وختم V2.0).
**✅ منفّذة ومختومة — Core V1.8.0 (2026-08-04). التفاصيل: ورقة ٠٨.**

> طريقتنا: من الجذر لا ترقيع · نرفع لا نبسّط · بسيط لا مبالغة · لا كود قبل قسم (٨) و"نفّذ".
> **الأهمّ:** الأمانة فوق الورق — لقطت بالكود **جذرًا ثالثًا** (رجوع الذرة الموقّفة) أعرضه عليك، لا أفرضه.

---

## ١. ما هي الوحدة (بلغة بسيطة)
اليوم «التحميل الحار» (إيقاف/تحميل/ترقية ذرة والنظام شغّال) فيه **أربع ثغرات جذرية**:
- **(أ) مهلات ناقصة:** لمّا ذرة تتعلّق بـ`start()`/`stop()` أثناء التحميل الحار أو إعادة
  التشغيل، **بتجمّد المحرّك كلّه** — بينما نفس النداء **محميّ بمهلة أثناء الإقلاع**. حماية بنصّ
  ومكشوف بنصّ آخر.
- **(ب) sys.modules ما يتنظّف:** لمّا تُسحب ذرة، موديولاتها تبقى بذاكرة المفسّر للأبد
  (تراكم + أي مورد ممسوك بها يتسرّب).
- **(ج) — لقطة بالكود (عرض المالك):** الذرة اللي توقّفها من **زرّ الإيقاف** بتعلق **STOPPED
  بلا طريق رجوع** — لا rescan بيرجّعها ولا في زرّ تشغيل. **الإيقاف طريق باتجاه واحد.**
- **(د) الإقلاع يتجاهل «الإطفاء» (`startup_mode`) — أُضيفت 2026-08-04 بعد مقابلة ورقة فايبل + تحقّق بالكود:** الذرة المضبوطة `manual` (مطفية عمدًا: مزوّد مدفوع · ذرة تجريبية خطرة) **يتجاهلها المُقلِع (`bootloader`) ويشغّلها كل إقلاع**، بينما التحميل الحار وحده يحترمها → «المطفي» يرجع حيّ مع كل تشغيل، والحلّ المؤقّت (نقل المجلد) عبء يدوي.

## ٢. الهدف و"ليش"
- **متانة:** ذرة واحدة سيّئة (تتعلّق) **ما تجمّد** محرّك الاكتشاف ولا حلقة الصحة ولا طلب HTTP.
- **نظافة:** سحب الذرة = تطهير كامل (سجل + ناقل + صحة + **ذاكرة المفسّر**) — عزل حقيقي.
- **اكتمال دورة الحياة:** إيقاف الذرة يجب أن يقابله **تشغيلها** — لا حالة «ميتة بلا رجعة».

## ٣. الموجود نبني عليه (قِسته بالكود — بالسطر)
- **النموذج الجاهز (نقلّده):** `bootloader.py:264,267` يلفّ `initialize/start` بـ
  `asyncio.wait_for(timeout=atom_boot_timeout_s=30s)`، و`:324` كذلك للتنظيف. **صحيح.**
- **الثغرة أ — بلا مهلة:**
  - `hot_reload_service.py:282,285` (`initialize`/`start`) · `:329,331` (`stop`/`shutdown`) · `:307` (تنظيف).
  - `health_manager.py:199,204` (`stop`/`start` بإعادة التشغيل) — بينما `:154` (`health_check`) **محميّ**.
  - `api/app.py:183` (`stop()` بزرّ اللوحة) — طلب HTTP بيتعلّق لو الذرة علّقت.
- **الثغرة ب — sys.modules:** `bootloader.py:212,219` يسجّل `_atom_{id}_{module}`، و`:314`
  يسجّل المساعدات `_atom_{id}_sib_{name}`. **`hot_reload_service.py:319 _hot_unload` ما يمسح
  أيًّا منها.** (الاستيراد يُعيد التنفيذ طازجًا كل مرّة — فالكود ما «يبيت»، لكن الموديولات **تتراكم**.)
- **الثغرة ج — رجوع الموقّفة:** `api/app.py:194` يترك الذرة **مسجّلة + STOPPED**. و`_rescan_once`
  (`:110` جديدة، `:188` مفقودة، `:124` ترقية) **يتجاهل المسجّلة الموجودة بنفس الإصدار** → لا رجوع.

## ٤. التشريح الدقيق — شو راح أعمل بالضبط (بسيط)
**الجذر أ (المهلات) — توحيد بمكان مركزي:** ثابت واحد `LIFECYCLE_TIMEOUT_S` (نفس ٣٠ث) **يعيش
بمكان مركزي لدورة الحياة** (+ دالة لفّ مشتركة `call_lifecycle(fn)`)، **لا داخل `hot_reload`** —
فكل المسارات (Bootloader · hot_reload · health restart · stop endpoint) تعتمد **نفس السياسة**
(تصحيح المستشار: المشكلة أصلًا «سياسة دورة حياة مطبّقة جزئيًا»، لا مجرّد مهلة). نلفّ **كل** نداء
دورة حياة خارج `boot` بـ`asyncio.wait_for`.
**سياسة الحالة بعد المهلة (سؤال المستشار الأقوى — حسمته تقنيًا):** الذرة تُوسم `FAILED` (الحالة
القائمة، **بلا حالة مختومة جديدة**) **مع سبب مميّز** `LIFECYCLE_TIMEOUT` بالـ`last_error` — فطبقة
٢ تميّز «مات بمهلة» عن «انهار» بلا تضخيم العقد. ثم تُنظَّف كالمعتاد.

**الجذر ب (sys.modules) — تطهير عند السحب:** بـ`_hot_unload`، بعد إلغاء التسجيل، **امسح كل
مفتاح يبدأ ببادئة الذرة** من `sys.modules` (الرئيسي + `_sib_`). **والبادئة تُعرَّف مرّة وحدة بدالة
بالـBootloader** (`atom_module_prefix(id)`) يستعملها **الطرفان** — التسجيل والتطهير — فلو تغيّر
التنسيق مستقبلًا ما ينكسر التنظيف بصمت (تصحيح المستشار). بأفضل جهد (المرجع الحيّ لو بقي يمنع
الجمع — نصدق بهالحدّ، زي `wipe` بالأسرار).

**الجذر ج (رجوع الموقّفة) — أعرضه، لا أفرضه (قسم ٨):** الإصلاح **مو تلقائي على rescan الدوري**
(وإلا كل ٥ ثواني بيصارع إيقافك المتعمَّد) — بل **فعل صريح**: يعيد `start()` + `watch()` للذرة
المسجّلة الموقّفة. المكان قرارك (نواة أم طبقة ٢).

**مصفّى (ضد المبالغة):** ما نضيف مكتبة، ولا نغيّر توزيع الأحداث، ولا نلمس عقد الذرة. مهلة + مسح
مفاتيح + (احتمالًا) فعل تشغيل صغير. لا أكثر.

## ٥. فحص الخطوط الحمر
- **جاهلة:** المهلة **رقم**، والتطهير **تقني** (مفاتيح موديول) — بلا معنى أعمال. ✓
- **العزل:** التطهير الكامل **يقوّي** العزل (ما يبقى أثر ذرة بذاكرة المفسّر). ✓
- **رفع لا تبسيط:** يسدّ تجميدًا + تسريبًا + طريقًا مسدودًا — قدرة أكبر، بلا حذف أي شيء. ✓
- **الافتراضي محافظ:** ذرة سليمة تنهي بأقلّ من المهلة = بلا تغيير سلوك. ✓

## ٦. الإثبات الحيّ (تشوفه بعينك)
- **اختبار المهلة (`start`):** ذرة تجريبية `start()` فيها `sleep` طويل → التحميل الحار **ما يتجمّد**؛
  بعد المهلة تُوسم `FAILED` بسبب `LIFECYCLE_TIMEOUT` وتُنظَّف، والمحرّك يكمل.
- **اختبار مهلة `stop()`/`shutdown()` (من المستشار):** ذرة تتعلّق **بالإغلاق** → السحب الحار **ما
  يتجمّد**؛ بعد المهلة تُوسم `FAILED` بسبب `LIFECYCLE_TIMEOUT` وتُنظَّف. (تعلق بالإغلاق كما بالبدء — يُثبَت الاثنان.)
- **اختبار sys.modules:** حمّل ذرة (بملف مساعد) ثم اسحبها → **لا مفتاح `_atom_{id}_*` باقٍ** في
  `sys.modules`.
- **اختبار ج (إن اعتُمد):** أوقف ذرة من الزرّ ثم شغّلها → **ترجع RUNNING** ويرجع رصدها بالصحة.
  (هاد عرض المالك الحيّ نفسه.)

## ٧. المخاطر والتراجع
- **يمسّ مسارات ساخنة** (`_hot_load/_hot_unload`, `_restart`, `stop endpoint`) — لكن الإضافات
  صغيرة ومحافظة، وتُثبَت بالاختبارات قبل أي شيء.
- **(أ) أخطر من (ب) (من المستشار):** المهلة تمسّ المسار الساخن وقد تقطع ذرة بطيئة **مشروعة** (لهيك
  القيمة سخيّة ٣٠ث)؛ أمّا تطهير `sys.modules` فأثره محصور. نضبط قيمة المهلة بحذر.
- **التراجع:** git (م٠). الختم يُعاد V2.0 بعد اكتمال أجزاء النواة كلها (٠٢..٠٦ سوا).

## ٨. الأسئلة — مصفّاة (التقني على كلود · وسؤال المالك بسيط)

**قرارات تقنية حسمها كلود (لا تلزم المالك — بعد تصفية رأي المستشار):**
- **أ:** ثابت مهلة موحّد **بمكان مركزي لدورة الحياة** + دالة لفّ مشتركة، تعتمدها كل المسارات (لا داخل hot_reload).
- **سياسة الحالة بعد المهلة (سؤال المستشار الأقوى — حسمته):** `FAILED` + سبب مميّز `LIFECYCLE_TIMEOUT` — يكفي طبقة ٢ للتشخيص بلا حالة مختومة جديدة.
- **ب:** بادئة وحدات الذرة **مُعرَّفة مرّة بالـBootloader** يستعملها التسجيل والتطهير معًا؛ المسح بأفضل جهد.
- **startup_mode (تصحيح 2026-08-04 — الواقع فوق الورق):** كان مركّنًا هون غلط. الحقيقة المقيسة بـgrep: `hot_reload_service.py:127-132` **يحترمه**، بس `core/bootloader.py` فيه **صفر ذكر** لـ`startup_mode` → **المُقلِع يتجاهله تمامًا.** فالبند **ثغرة حقيقية (د)**، لا مركونة. **الحلّ (بالفتحة):** المُقلِع يطبّق **نفس فلتر hot_reload حرفيًا** — الذرة `manual` تُسجَّل ولا تُشغَّل، وتظهر في «استُبعدت: startup_mode=manual». **اختبار القبول:** مانيفست `manual` → غائبة عن «بدأت»، حاضرة في «استُبعدت»، وتشغيلها اليدوي عبر API يعمل.

**للمالك (بلغة بسيطة) — سؤالك الوحيد الحقيقي (الجذر ج):**
> لمّا توقف ذرة من الزرّ، هلق بتعلق موقّفة بلا رجعة. بدنا **زرّ/فعل يرجّعها تشتغل**.
> **مكانه:** ① بالنواة الآن (ضمن هالفتحة) · ② أأجّله لطبقة ٢ (اللوحة/التحكّم — مكان الأزرار طبيعيًا).
>
> **رأيي الصريح:** المهلات (أ) وsys.modules (ب) **ننفّذهم الآن** — هدول جذرا النواة الحقيقيان.
> أمّا زرّ الرجوع (ج) فهو **فعل تحكّم** = بيته **طبقة ٢** حسب معماريتك — بس النواة لازم تعطي
> **خُطّاف صغير** (تعيد تشغيل ذرة موقّفة) تستدعيه الطبقة. فأقترح: **أ+ب بالنواة الآن، وخُطّاف
> «التشغيل» جاهزًا، والزرّ نفسه بطبقة ٢.** موافق؟

## ٩. القرار
☑ **محسوم (قرار تقني — كلود يحمله، مو المالك):**
- **أ (المهلات) + ب (تنظيف sys.modules) → بالنواة** ضمن فتحة V2.0.
- **ج (رجوع الذرة الموقّفة):** النواة تعطي **خُطّاف تشغيل صغير**، و**الزرّ نفسه بطبقة ٢** (مكان الأزرار والتحكّم — نفس الطبقة اللي عم نبنيها).
- **د (startup_mode) → بالنواة ضمن الفتحة:** المُقلِع يطبّق نفس فلتر hot_reload، فالذرة المطفية تضل مطفية عبر الإقلاع. (أُضيف 2026-08-04 من مقابلة ورقة فايبل + تحقّق بالكود — الواقع فوق الورق.)
> سبب التصنيف تقني: «نواة ولا طبقة ٢» ليس قرار رؤية — المالك مهندس رؤية لا مبرمج، فالتقني على كلود (2026-08-04).

---
**التالي بعد اعتمادها:** **فتحة النواة الواحدة** — تنفيذ ٠٢..٠٦ سوا (مبدأ «لا نفتح النواة مرتين»)
مع الاختبارات، ثم **إعادة ختم V2.0**. وبعدها م٢ (طبقة الحوكمة/الدماغ).
