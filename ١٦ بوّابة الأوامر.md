# ورقة بناء — ١٦: بوّابة الأوامر (تشريح قبل التنفيذ)

**التاريخ:** 2026-08-07 · **بأمر المالك:** «بوابة اوامر» (بعد مراجعة أفكار `901` القديمة — أفكار فقط، صفر نسخ كود).
**تبني على:** `٠٩` مكوّن ٥ (بوّابة الأوامر) · `١٤ §٨` (درجات السلطة · كل كتابة ببوّابة واحدة) · قالب الذرة (٢٣ قاعدة).
**الفجوة المقيسة اللي تحلّها:** `516`/`552` يسمعان `risk.kill_switch.reset_requested` **ولا ناشر له** — لو قفل القاطع ما في يد تصفّيه. والإيقاف الطارئ (`emergency.halt`) بلا زر بيد المالك.

## ١. القيد المعماري المقيس
النواة المختومة **ما عندها منفذ نشر أحداث** (قراءة + stop/start/rescan فقط — مقيس من `core/api/app.py`).
فالنشر للناقل لازم **ذرة**، والأمر يوصلها عبر **ملف جسر** — نفس نمط جسر الإكسبرت (601/611) والأخبار (616). **النواة لا تُمسّ.**

## ٢. القطع الثلاث
1. **ذرة `901_بوابة_الأوامر`** (على القالب): نبضية على `SYS_SECOND` (806، «الآن» من `official_time` — قاعدة ١٣، لا حلقة خلفية).
   كل نبضة تقرا `var/governance/commands.db` (SQLite · WAL · busy_timeout): صف `PENDING` → تتحقّق (أمر معروف · طازج ≤ `max_age_s` وإلا `EXPIRED`) → تنشر الحدث → تعلّم الصف `DONE` → تنشر `system.commands.state` (عدّادات + آخر أمر؛ لاحقة حالة = يُعاد للوحة المتأخّرة — قاعدة ١٦).
   **الأوامر:** `halt` → `emergency.halt` · `kill_switch_reset` → `risk.kill_switch.reset_requested` (كلاهما بلا لاحقة حالة → لا يُعادان أبدًا).
2. **خادم الحوكمة** (`server.py`): `POST /gov/command` **بخطوتين** (فكرة 901 القديمة الذهب): طلب → رمز تأكيد + ملخّص عربي → تأكيد خلال `60ث` → كتابة الصف بالجسر. المهلة بالخادم (طبقة حوكمة — مسموح لها الساعة).
3. **اللوحة:** زر **⛔ إيقاف طارئ** أحمر بالهيدر (تصميم `١٤ §٤`) + زر **تصفير قاطع الأمان** بقسم المخاطر — كلاهما بحوار تأكيد صريح (درجة «أوامر خطرة» `١٤ §٨`).

## ٣. الخطوط الحمر المفحوصة
- النواة المختومة لا تُمسّ ✓ · الذرة لا تعرف الحوكمة (تقرا ملف جسر فقط — قاعدة ١٥) ✓ · لا حلقة خلفية (نبضة) ✓ · لا ساعة بالذرة (official_time) ✓ · حرس معرّفات SQL (قاعدة ١٨) ✓ · أحداث الأوامر لا تُعاد (قاعدة ١٦) ✓ · التأكيد بخطوتين للأخطر ✓ · الغلط/البايت يُرفض بصمت ويُعدّ ✓.
- **حدود اليوم بصدق:** تنفيذ الأمر بيتأخّر لحدّ نبضة (≤ ~ثانية) — مقبول لأوامر طوارئ يدوية. المشغّل الوحيد = اللوحة المحلية (localhost)؛ تلغرام لاحقًا يمرّ بنفس البوّابة.

## ٤. الإثبات الحيّ (الفحص ٢ — شرط التسليم)
1. الذرة تُحمَّل حيّة (rescan/إعادة تشغيل) وتظهر باللوحة.
2. زر «تصفير القاطع» → تأكيد → خلال ثانية يظهر `risk.kill_switch.reset_requested` بالمراقبة و`system.commands.state` يتحدّث — **بعين المالك**.
3. «إيقاف طارئ» يُفحَص بحذر (يوقف الإرسال فعلًا): يظهر `emergency.halt` + 552 تتحوّل «halted» + المدير يعرضه. التراجع = تصفير القاطع.

**القرار:** أمر المالك صادر — يُنفَّذ.

---
## ✅ نُفِّذ (2026-08-07)
- **الذرة `901_بوابة_الأوامر`** بُنيت على القالب: ٧/٧ اختبارات · المدقّق ١٥٦/٠ · **حُمّلت حيّة بلا إعادة تشغيل** (`POST /api/rescan`).
- **إثبات حيّ على النواة الحقيقية (مقيس):** أمر `kill_switch_reset` حقيقي كُتب بالجسر → الذرة نفّذته بالنبضة → الصف `DONE` بختم وقت → صحتها `HEALTHY: halt=0 reset=1`. (الإيقاف الطارئ ما فُعِّل بالتجربة — عمدًا.)
- **الخادم**: `POST /gov/command` بخطوتين (رمز تأكيد ٦٠ث) → كتابة بالجسر. **اللوحة**: زر ⛔ إيقاف طارئ بالهيدر + زر تصفير القاطع بقسم المخاطر + سجلّ البوّابة (من `system.commands.state`).
- **⏳ باقي بوّابة الفحص ٢ بعين المالك:** إعادة فتح «غرفة القيادة» (يسكّر النافذة ويدبل-كليك الـbat — الخادم القديم شغّال بلا المنفذ الجديد) → يضغط «تصفير قاطع الأمان» → يشوف `reset` يزيد بعينه.
