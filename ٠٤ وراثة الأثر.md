# ورقة بناء — النواة ٠٤: وراثة الأثر (trace_id / parent_event)

**نوع الورقة:** تشريح تنفيذ كامل — يُجاب على **كل سؤال** فيه **قبل** أي سطر كود.
**التاريخ:** 2026-08-03 · **الوحدة:** النواة ① · الجزء ٠٤ · **الحالة:** ✅ **مُعتمَدة (مقفولة)** — رأي ثالث + موافقة المالك على المعرّفات الثلاثة
**يمسّ النواة** (جزء من الختم V2.0). **حزمة واحدة مع ٠٣** (هويّة الحدث المعاد).
**✅ منفّذة ومختومة — Core V1.8.0 (2026-08-04). التفاصيل: ورقة ٠٨.**

> طريقتنا: من الجذر لا ترقيع · نرفع لا نبسّط · بسيط لا مبالغة · لا كود قبل قسم (٨) و"نفّذ".

---

## ١. ما هي الوحدة (بلغة بسيطة)
اليوم كل حدث بالنظام بياخد **معرّف عشوائي جديد**، حتى لو هو **نتيجة** حدث قبله. فالنتيجة:
النظام بينشر ٢٥٠ حدث **بلا رابط بينهم** — زي ٢٥٠ ورقة مبعثرة، ما بتعرف مين سبّب مين.

الحلّ: كل حدث **يورّث أثر أبيه** — يعني لما حدث يسبّب حدث تاني، التاني بياخد **نفس المعرّف**
(سلسلة)، وبيسجّل **مين أبوه** (شجرة سببية). فتصير: "تكّة → شمعة → إشارة → قرار → أمر" —
**سلسلة وحدة نقدر نتتبّعها**، بدل كومة.

## ٢. الهدف و"ليش"
- **نجاوب "وين انقطع المسار / مين بدأ الغلط"** — مستحيل اليوم لأن الأحداث مقطوعة.
- **شرط لحلقة التعلّم الإحصائي** (٣٦٠ لازمه يربط توقّع بنتيجته — بلا الأثر مستحيل).
- **أساس رؤية طبقة ٢:** تشوف "شو صار بين النواة والذرات" كسلسلة، لا فوضى.

## ٣. الموجود نبني عليه (قِسته بالكود — `core/event_bus.py`)
- **الآلية نصف مبنيّة:** الناقل **يستورد `current_trace_id`** (من `core.logger`) و**يضبطه في
  `_invoke` قبل كل معالِج** (السطر `current_trace_id.set(payload.get("trace_id"))`).
- **بس ما يقرأه عند النشر:** السطر `base.setdefault("trace_id", str(uuid.uuid4()))`
  بيحط معرّف عشوائي دايمًا — **يتجاهل أثر الأب الموجود بالبيئة.** هون بالضبط الإصلاح.
- (نفس الملاحظة من ورقة كسر التجميد — بند صفر، "أقوى بند".)

## ٤. التشريح الدقيق — شو راح أعمل بالضبط (بسيط)
**ملفان يُمسّان: `core/event_bus.py` (+ متغيّرات بيئة صغيرة في `core/logger.py`).**

**ثلاثة معرّفات — الشجرة الدقيقة (قرار المالك: موافق):**
- **`trace_id`** = هوية **الرحلة كلها** (السلسلة).
- **`event_id`** = هوية **هذا الحدث وحده** (رقم فريد لكل حدث). **جديد.**
- **`parent_event_id`** = **معرّف الحدث الأب** (مين سبّبه بالضبط) → شجرة حقيقية لا سلسلة. **جديد.**
- `parent_event` (اسم الأب) = للقراءة البشرية فقط.

**عند النشر (`publish`) — أربع ختمات:**
1. `setdefault("event_id", str(uuid.uuid4()))` — كل حدث ياخد رقمه.
2. `setdefault("trace_id", current_trace_id.get() or str(uuid.uuid4()))` — يورّث الرحلة أو يبدأها (جذر).
3. `setdefault("parent_event_id", current_event_id.get())` — رقم الأب من البيئة.
4. `setdefault("parent_event", current_event.get())` — اسم الأب (بشري).

**في `_invoke`:** أضبط `current_event_id` و`current_event` بقيم **الحدث المُعالَج حاليًا**،
فلما المعالِج ينشر، الحدث الجديد ياخد أباه صح (تمرير event_id+الاسم لـ`_invoke`).

**بسيط عمدًا:** أربع تاگات تُختَم عند النشر — النواة **بس تختم (غبية)**، وطبقة ٢ **تبني
الشجرة** منها. لا مكتبة تتبّع ولا نظام كبير. (تصفية ضد المبالغة.)

## ٥. فحص الخطوط الحمر
- **جاهلة:** `trace_id` و`parent_event` **معرّفات تقنية** — لا معنى أعمال. النواة تبقى عمياء. ✓
- **العزل:** ما يتغيّر — حقول محقونة تلقائيًا (المادة 31). ✓
- **رفع لا تبسيط:** يوصل السلسلة بدل ما يقطعها. سلوك التوزيع بلا تغيير. ✓
- **متوافق مع ٠٣:** الحدث المُعاد يحمل أثره الأصلي — فالسلسلة تبقى صحيحة عند الإعادة. ✓

## ٦. الإثبات الحيّ (تشوفه بعينك)
- **قبل:** شغّل سلسلة (تكّة→شمعة→إشارة→قرار) → **٤ معرّفات رحلة مختلفة** (مقطوعة).
- **بعد:** نفس السلسلة → **رحلة (trace) واحدة**، وكل حدث يحمل **رقمه (event_id) ورقم أبيه
  (parent_event_id)** → شجرة.
- **اختبار التفرّع (إلزامي — من الرأي الثالث):** تكّة → إشارة A → أمر A، **وبنفس الوقت**
  تكّة → إشارة B → أمر B → **نفس الـtrace**، بس **كل فرع يتميّز بـ`event_id`/`parent_event_id`**
  فلا يختلط الفرعان. **هذا سبب الحاجة لرقم الحدث لا اسمه.**
- **اختبار الجذر:** حدث بلا أب (تكّة أصلية) → يبدأ رحلة جديدة، `parent_event_id` فارغ.

## ٧. المخاطر والتراجع
- **يمسّ `publish` و`_invoke` (مسارات ساخنة) — خطر منخفض-متوسط (لا صفر):** لكنه ~٣ أسطر
  إضافية، يُكتب بحذر ويُثبَت بالاختبارات.
- **التراجع:** git (م٠). الختم يُعاد V2.0 بعد اكتمال أجزاء النواة.

## ٨. الأسئلة — مصفّاة (التقني على كلود · وسؤال المالك بسيط)

**قرارات محسومة (رأي ثالث + موافقة المالك):**
- **ثلاثة معرّفات:** `trace_id` (الرحلة) · `event_id` (الحدث الفرد) · `parent_event_id`
  (الأب بالرقم لا الاسم) → شجرة دقيقة حتى عند التفرّع.
- الجذور تبدأ رحلة جديدة؛ `parent_event_id` فارغ.
- **حزمة مع ٠٣:** الإعادة تحافظ على `event_id` و`trace_id` الأصليين → الشجرة ما تنكسر.
- النواة تختم فقط؛ طبقة ٢ تبني الشجرة.

## ٩. قرار المالك
☐ نفّذ كما هو  ·  ☐ عدّل (اكتب)  ·  ☐ أوقف / غيّر

---
**التالي بعد اعتمادها:** ٠٥ الساعة (الناقل + السياق).
