# تسليم — غرفة القيادة (لوحة الحوكمة React) · للمحادثة الجاية

**التاريخ:** 2026-08-06 (مُحدَّث) · تُقرأ مع الذاكرة + `٠٠ السياق` + الدستور (قواعد ٣–١٥). **الحاكم: قواعد المالك.**

---

## ١. الحالة الحيّة
- **تطبيق سطح مكتب** (لا متصفّح): `غرفة القيادة.bat` بجذر المشروع → يشغّل `governance/app.py` (pywebview + WebView2). يفتح النواة + الخادم + نافذة اللوحة. **دبل-كليك واحد، صفر PowerShell.**
- **الخادم**: `governance/server.py` على **:8090** (يقرا النواة :8010 قراءة فقط). **النواة**: Core V1.8.0 على :8010 — **١٤٥ ذرة** حيّة (تزيد من محادثة الذرات).
- **التشغيل**: النواة لازم أوّل. لو المنافذ مشغولة، `app.py` يعيد استخدامها. **قفل نسخة واحدة على :8099** (دبل-كليك مزدوج ما يكرّر). `app.py` **يفرض بايثون البيئة (venv)** لتشغيل النواة/الخادم (بايثون النظام يُسقِط النواة — درس مدفوع).

## ٢. 🔴 التحديث الذاتي (مهم جدًّا — حلّ «اللوحة تكذب»)
- **الخادم يبعت `Cache-Control: no-store`** على `index.html` وكل `/gov/*` → لا تخزين WebView.
- **`/gov/version`** يرجّع اسم ملف JS المبني · الواجهة تسحبه كل ٧ث وتعيد تحميل نفسها لو تغيّر.
- **`app.py` يفتح النافذة برابط فريد `/?v=timestamp`** → يتخطّى أي كاش قديم.
- **نبض REST**: المحرّك يسحب `/gov/atoms` كل ٤ث + الشبكة تسحب `/gov/graph` كل ٥ث → **أي ذرة جديدة تنزل حيًّا بلا reload.**
- **النتيجة: أبني، والمالك ما يلمس شي — النافذة تحدّث نفسها.**

## ٣. الأقسام (٢٢ مبنيّ · كلها داتا حقيقية)
**الرئيسية** · الشبكة · الذرات · المدير · **الشارت** · السوق · **التحليل · البنية · السيولة · الإحصاء التحليلي · الاحتمالات · الاستراتيجيات · القرار · المخاطر · التنفيذ** · المحافظ · الاتصال · المراقبة · السجل · التنبيهات · الإحصاء · الإعدادات · التشخيص.

**✅ الرئيسية v4 (2026-08-07 · حكم المالك على v3: «شغل رخيص، لا مكان بدي أراقب فيه»):** v3 (حشر الأقسام الكاملة بمربعات متساوية) أنتج نصوصًا مقصوصة وسكرولات وفوضى — **اتشالت**. v4 = **قمرة قيادة مصمّمة بهرمية**: ① شريط ٦ أرقام حاكمة كبيرة بلا قصّ (Equity · ربح عائم · قاطع · بوّابة · سلامة الذرات · اتصال) · ② الشبكة الحيّة كبيرة (7fr) + **جدول «الأنبوب لكل رمز»** (5fr): سطر واحد = سعر ← تحليل ← بنية ← أصوات استراتيجيات ← قرار ← صفقة+ربحها (دمج ٦ أقسام بسطر) · ③ ثلاث موجزات حيّة (آخر أوامر التنفيذ · الأحداث · السجل، ٥ أسطر كل واحد). زر ⤢ لكل بانل. صفر سكرول صفحة. **درس:** جدار الأقسام الكاملة = wrong granularity؛ القمرة عناصر مبنيّة لغرضها (ورقة ١٤ §٥ widgets).
**🔄 الشبكة v3 (نفس اليوم، بأمره «قيسها على منظور بشري»):** بدل كرة الشعر — **خط إنتاج مقروء RTL**: عمود لكل عائلة بعنوان عربي (`titleSprite`): المنصّة والجسور→بيانات السوق→التحليل→البنية→السيولة→الإحصاء→الاحتمالات→الاستراتيجيات→القرار→المخاطر→التنفيذ + شريط «النظام والصيانة» فوق و«المخازن» تحت (`stageOf(id)` بنطاقات الأرقام). d3-force انشال. تحكّم يدوي كامل (سحب=تدوير · عجلة=زوم ٢٤٠..٢٤٠٠ · لا دوران ذاتي ولا parallax) · توهّج مخفَّف (Bloom 0.45) · أسماء بلا توهّج · كل الأسماء ظاهرة.

**✅ التنفيذ انبنى (2026-08-06 · عرض فقط):** `sections/Execution.tsx` — بوّابة الأمان (من صحّة ٥٥٢: `LIVE`/`PREVIEW_ONLY`) · عدّادات ٥٥٠ (`execution.unified.state`) · سلسلتا فتح/إدارة (pills بصحّة الذرات 467→516→512→513→551→552→601 و572/573/574→570→575) · ٣ سجلّات دوّارة (أوامر/إدارة/نتائج، آخر ٤٠، وقت وصول محلّي). **صفر أزرار — ولا كتابة.** الأمان بالنواة نفسها: ٥٥٢ `enabled:false` (معاينة فقط) + ٥٧٥ `DISABLED`. فتح البوّابة = قرار المالك حصرًا.
**✅ آخر ٤ مكوّنات الحوكمة (2026-08-07 «عملهم كلهم» · كلها مثبتة حيًّا):**
1. **النسخة الاحتياطية الموحّدة** (٠٩ مكوّن ٧): `POST /gov/backup` → zip بـ`var\backups\snapshot_*.zip` (الذرات+النواة+الإعدادات+الحوكمة+الورق · **يستثني var الضخمة ~445MB** معلَنًا) — مثبت: ٩٤٢ ملف/1.5MB. زر «خُد لقطة» بالسكربتات بتأكيد.
2. **السجل الموحّد بزر** (٠٩ مكوّن ٢): `GET /gov/unified-log` يدمج جورنال النواة + trade_events + أوامر البوّابة بخط زمني واحد + أخطاء الذرات المعلّقة — زر «📜 السجل الموحّد الكامل» بقسم السجل، مترجم بالكامل. مثبت: ٢٣٧ سطر.
3. **إنذار الصمت** (فكرة 999 حيّة): `store.flowStats` (عدد/أول/آخر نبضة لكل تيار) → التنبيهات تنذر «تيار كان ينبض ووقف» عند صمت > max(60ث، ٥×متوسط نبضه) بعد ≥٦ نبضات — أسماء عربية للتيارات المعروفة (`STREAM_AR`).
4. **زر إعادة فحص الذرات**: `POST /gov/rescan` (بروكسي خُطّاف النواة) — بطاقة بالسكربتات.
**⏸️ بقرار المالك (لم تُبنَ):** تلغرام (يحتاج عبارة مرور الأسرار) · «وضع قراءة فقط» عام · الرئيسية كافتتاحية.

**✅ جردة ذرات الحوكمة القديمة (~12: 700مجمّع·750عيلة·801·999·662-666) — قرار «اتركها» مؤكَّد نهائيًّا (2026-08-07):** كلها إمّا مبنية بالحوكمة أو مكانها هناك (قاعدة ٢٣) — **لا تُبنى كذرات أبدًا**. الفكرتان النافعتان بُنيتا («سكّرها» بأمر المالك): **لمحة المخازن** (بطاقة بالتشخيص: كل ذرات ٧٠٠ بحالتها المترجمة) + **حالة النسخ** (`GET /gov/backups` يجرد القرص → بطاقة النسخ تعرض: الآلية اليومية آخر نسخة/كم محفوظة · اليدوية كذلك).

**✅ السكربتات انبنى (2026-08-07):** `sections/Scripts.tsx` + `POST /gov/tool/{seal|validator|tests}` بالخادم (whitelist ثابتة · subprocess ببايثون البيئة · مهلات · `NQ_BRIDGE_DB` يُحيَّد — درس الجسر). ٣ أدوات فحص/قراءة فقط: **فحص ختم النواة** (`freeze_core verify` — مطلوب أصلًا بورقة ٠٩ مكوّن ٨) · **المدقّق البنيوي** · **اختبارات الذرات** (بطيئة، معلَن). الترجمة فوق + زر «ورّيني الخام» (لا ثقة عمياء). مثبت حيًّا: seal → «سليمة ومطابقة V1.8.0». **🎉 بهيك كل أقسام اللوحة المخطّطة مبنيّة — ٢٣ قسم، صفر «قريبًا».**

## ٣.٥ 🐛→✅ تعديل إعداد الذرة كان يكذب — انصلح من الجذر (2026-08-07 · المالك طلب «تأكد من تعديل ذرة صحيح»)
- **الكذبة المكتشفة (مقيسة سلوكيًّا):** `write_atom_config` كان يعدّل المانيفست ويطلب rescan ويقول «أُعيد تحميل الذرة» — **والنواة ما تعيد تحميل ذرة موجودة إلا لمّا يتغيّر `version` المعلَن** (عقدها الرسمي: «المقارنة بالإصدار حصرًا لا ببصمة الملف» — `hot_reload_service._rescan_once`). النتيجة: الملف يتغيّر والذرة الشغّالة عالقديم. أُثبت بفحص سلوكي: أمر عمره ١٣٠ث انرفض EXPIRED رغم إن الملف يقول 150.
- **الحل (بالحوكمة، النواة ما انلمست):** التعديل صار: يكتب القيم + **يرفع رقم التصحيح تلقائيًّا** (v1.0.0→1.0.1…) → النواة تعيد التحميل بعقدها → **يتحقّق فعلًا** (يستطلع `/api/atoms` حتى يشوف النسخة الجديدة حيّة) قبل ما يقول «أُعيد التحميل»؛ ولو ما تأكّد يقولها بصدق. + **يسجّل سطرًا بـ`التاريخ.md` تبع الذرة** (قاعدة ٦). أرقام integral تنكتب int لا `150.0`.
- **مثبت حيًّا على 901:** 150→DONE لأمر عمره ١٣٠ث (نافذة اتطبّقت) · رجوع 120 (v1.0.2) · رفض تحت-الحد ورفض مفتاح غريب بالعربي.
- **⚠️ أثر جانبي مقبول:** إعادة التحميل تصفّر عدّادات الذرة (نسخة جديدة) — صادق.

## ٣.٦ ✅ محلّل الصمت الحيّ بقسم التشخيص (2026-08-07 · قرار فوّضه المالك «اشتغل خيار أحسن»)
- **الأصل:** المالك جاب `scripts\مجلد جديد\exercise.py` (أداة قياس-بالموك من 2026-08-02 + نسخ قديمة من سكربتات المشروع). **الحكم:** فكرة تصنيف الصمت = ذهب · لكنها موك (ممنوع) + أسماء أحداث قديمة + **خطر: تشغيلها يكتب شموع مصنوعة بمخازن `var/store` الحقيقية — ما تُشغَّل أبدًا كما هي.**
- **المبني بدلها (بلا موك):** `Diag.tsx` v2 — يصنّف كل ذرة ساكتة **على النظام الحيّ**: ⚠ وصلها مدخلها وما نطقت (الوحيدة اللي تستاهل تحقيق) · يتيمة (محدا ينشر مدخلها) · مجوّعة (ناشرها ساكت، بأسماء المستنّاهن) · دورية/مستهلِكة/واقفة (طبيعي). + **وصلات مكسورة** نظامًا: مدخل-بلا-ناشر (أحمر) ومخرج-بلا-مستمع (رمادي، حالات `.state/.collected…` تُستثنى = للوحة). المصادر: `/gov/graph` صار يرجّع `pubs`/`subs` الخام + `store.flows` (نبض الأحداث الفعلي). القياس «منذ فتح اللوحة» — معلَن بصدق بالواجهة.
- **🔴 اكتشافات فورية (بالمانيفستات الحقيقية · للمالك/محادثة الذرات):** ١٦ مدخل بلا ناشر — **يشبه أخطاء تسمية:** 707 يستنى `decision.approved/rejected` · 702 يستنى `trade.opened/closed/rejected` · 704 يستنى `execution.confirmed`/`risk.limit.hit` (الأسماء الفعلية اليوم مختلفة — نفس عيلة غلطة 166→712). **مقصود/مؤجّل:** 116 (مصادر مستقبلية binance/rithmic/yahoo · depth/trade من الإكسبرت) · 706 (نماذج مؤجّلة) · 709 (`portfolio.summary` — 650 لسا) · 708.

## ٣.٧ 📜 عقد الحوكمة للمرحلة الجاية (2026-08-07 · المالك: «كل هالحكي بدي أتحكم فيه من اللوحة»)
محادثة الذرات جهّزت استبيان المرحلة (تغذية/إكسبرت · رموز وأدوار · **العيار** · تحليل لكل أنبوب · **محرّك إدارة المركز الدائم** بحالاته الأربع · مخاطرة/ستوب · تعدد حسابات). **شرط الحوكمة عليها — ٤ بنود (أُرسلت للمالك ليبعثها):** ① كل رقم/قائمة/عيار يتغيّر بقرار المالك = **إعداد معلَن بالمانيفست** (قاعدة ١١ — لا أرقام مدفونة) → اللوحة تلقطه وتعدّله حيًّا تلقائيًّا · ② كل حالة تُعرض = **حدث بلاحقة حالة** فيه symbol+account_id (قاعدة ١٦) — خصوصًا حالة المحرّك (منكشف/هادئ/متحوّط/عكسي) والعيار الفعّال · ③ أوامر التحكم الخطرة عبر **جسر بوّابة ٩٠١** حصرًا — لا مسار موازٍ · ④ قائمة رموز الإكسبرت من بايثون = موافَق (تصير إعدادًا معلَنًا). **وبعد بناء ذراتهم: وجوه متخصصة باللوحة** (منزلق العيار لكل رمز · عرض الحالات الأربع بالرئيسية · جدول $ لكل حساب×رمز) بنمط §٤.

## ٤. 🔑 نمط بناء الأقسام (اتبعه للأقسام الجاية)
كل عائلة ذرات إلها **مدير ينشر `X.cycle.collected`** (أو ناشر `market.X.updated`) **لكل رمز**، فيه `results` مفهرَسة بمعرّف الوحدة:
1. **اقرأ كود الذرة أولًا** (قاعدة ٤) — معرّفات الوحدات + حقل القيمة (`metadata.value` إحصاء · `metadata.probability` احتمالات · `signal` استراتيجيات/قرار).
2. `store.ts`: أضف `X: Record<symbol, XState>` + `setX`.
3. `engine.ts`: في حلقة الأحداث، `if (name === 'X.cycle.collected') st.setX(p.symbol, payload)`.
4. `sections/X.tsx`: بطاقة لكل رمز تقرا `store.X`.
5. `App.tsx`: فعّل `['x','الاسم',true]` + استيراد + فرع render.
6. **الشارت** استثناء: `/gov/candles?symbol&tf&limit` بالخادم يبني OHLC من التكّات المخزّنة (`var/store/market_data.db`, ٤٣٢ألف تكّة) → `lightweight-charts` (TradingView). فريمات ٥ث→يوم.

**المصادر المؤكَّدة:** analysis→`analysis.raw.completed`(166) · structure→`market.structure.updated`(210) · liquidity→`market.liquidity.updated`(260) · stats→`stats.cycle.collected`(300) · probability→`probability.cycle.collected`(350) · strategy→`strategy.cycle.collected`(400) · decision→`decision.cycle.collected`(450) · risk→`risk.unified.state`(500, **عالمي مو لكل رمز**، NO_INPUT_YET لحدّ أوّل صفقة).

## ٥. ⚠️ الشبكة — درس مؤلم (اقرأ قبل ما تلمسها)
الشبكة سبّبت **لفّ طويل ومرهِق للمالك** (فقاعات→«لعبة طابات»→2D«ماين كرافت»→WebGL كرات→WebGL سينمائي→2D نظيف→رجوع). **المالك قال حرفيًّا «اتركه جننتني».**
- **الحالي = `Network.tsx` = WebGL سينمائي** (نقاط متوهّجة glow + ACES + Bloom + ضباب + حركة عائمة + أسماء هولوجرامية للمحاور + tooltip + vignette/scanlines + كاميرا parallax). مبني بـ`three@0.160.1` + `d3-force`.
- **🐛 غلط مصلَّح (2026-08-06 · كشفه المالك بصورة شاشته «ما نربطت»):** من أول نسخة سينمائية، **ولا خط كان ينرسم فعليًّا** — `d3.forceLink` يبدّل `source/target` من رقم لكائن عقدة بعد المحاكاة، والكود يفهرس بالرقم → قايمة الوصلات فاضية دايمًا (خطوط ونبضات على صفر). الحل: أخذ الـ`id` بالحالتين. + **التوهّج خُفِّف بطلبه** (Bloom 0.85→0.45، عتبة 0.06→0.12). درس: لا تسليم لشي بصري بلا عين المالك — متصفّحي ما بيشغّل WebGL.
- **تعديل بأمر صريح من المالك (2026-08-06 «اربطها بخطوط ونعرض أحداث كيف عم تمشي»):** النسيج الهادي = خطوط خافتة (k=0.18) · مرور حدث حقيقي → الوصلة تشتعل بلون ذرّتيها + دفعة بياض (`heat` يخبو ×0.945/إطار) + نبضة ضوء تمشي ناشر→مشترك · **النبضات العشوائية الزخرفية انشالت نهائيًّا** (كانت تمشي بلا حدث = وهمي) — كل حركة عالشبكة الآن = حدث فعلي من النواة (`store.flows`).
- نسخ محفوظة: `NetworkCinematic.tsx` (نسخة من الحالي). النسخة «٢د نظيفة» و«كرات OrbitControls» كانتا بملفات مؤقتة أثناء الجلسة.
- **🛑 لا تعيد ضبط الشبكة إلا بأمر صريح جديد من المالك.** التصميم ذوقه (اسأل/وريه، لا تفرض، لا تخمّن «بعده»). المحرّك/التقني شغلك.

## ٦. البنية التقنية
- الفرونت `governance/ui/src/`: `core/store.ts` (Zustand — بلا تخزين · شرائح لكل عائلة) · `core/engine.ts` (WS→طابور→rAF + نبض REST 4ث/15ث) · `core/transport.ts` (WS `:8010/ws/events` + REST) · `core/i18n.ts` · `core/analyzerLabels.ts`/`settingLabels.ts` (ترجمات) · `App.tsx` (إطار + nav + AtomModal) · `sections/*`.
- **قناتان بالبثّ**: `event`(فوري: `market.tick` سعر · كل الأحداث) + `snapshot`(كل ٣ث: حالة الذرات). السعر لحظي، الـ٣ث بس صحّة الذرات — **مو تعارض.**
- بناء: `npm run build --prefix "...\governance\ui"` (PATH: `C:\Program Files\nodejs`). المخرَج `ui/dist` يقدّمه الخادم.

## ٧. قواعد حاكمة
- **الدستور (٣–١٥)** بالذاكرة `operating-constitution.md`: استشر قبل البناء · مصدر كل رقم · اختبر قبل التسليم · جدول مقارنة بأي تفرّع · صنّف الثقة · اعترف بالخطأ فورًا · ذكّر بالقواعد بعد كل مرحلة. **القاعدتان ١ و٢ لسا ما وصلوني.**
- التصميم = ذوق المالك · بلغته العربية · ويندوز/PowerShell (لا Bash tool) · لا خلفية · لا داتا وهمية (حيث ما العقل ناقص → «لسا مو مبني» أو NO_INPUT صادق).
- **متصفّح كلود الداخلي يخفي التبويب** → صور المالك = وسيلتي أشوف الشكل الحقيقي (خصوصًا WebGL/الحركة).

## ٨. التالي
1. **التنفيذ (٥٥٠)** أوّل ما ذراته تنبني — بموافقة المالك الصريحة + أمان (demo/0.01) فقط. لا توصيل تنفيذ بلا موافقة.
2. أي عائلة ذرات جديدة → قسمها بالنمط (§٤) بدقائق.
3. المخاطر (٥٠٠): تتعبّى أوّل ما تصير أوّل صفقة/خسارة (risk.unified.state).
4. **لا تلمس الشبكة** بلا أمر.
