# ورقة تشريح — تعدّد الحسابات: بطاقة البيئة + 651 + 650 + 610

> **تصميم المالك المباشر (٢٠٢٦-٠٨-٠٧).** هذه الورقة **تلتقط تصميمه بأمانة** (لا منهجيّة كلود) + تضيف القيود التقنيّة والأسئلة التي تحتاج قراره. **قبل أيّ كود · تُراجَع وتُعتمَد أوّلًا.**

## القاعدة الحاكمة (نصّ المالك)
**الحسابات مستقلّة بالقرار والتنفيذ · مشتركة بالرؤية المحفظيّة فقط.** حتى لو نفس الاستراتيجيّة — **حالتها الداخليّة ليست مشتركة.**
- 650 **لا ينفّذ تداولًا.** · 651 **لا يكرّر بيانات 619/609/517.** هما **طبقة تجميع/عزل/رؤية فوق أنابيب مستقلّة.**

## ٠. الأساس الذي يُثبَّت **قبل أيّ كود** (أمر المالك الصريح)
1. **شكل بطاقة البيئة (Environment Card).**
2. **عقد الأحداث الذي يحمل `account_id` كهويّة سياق** (لا metadata في نهاية الحدث).
> «هذان هما الأساس الذي سيمنع ترقيع تعدّد الحسابات لاحقًا.» — لا نكتب كود 650/651 قبل حسمهما.

## ١. بطاقة البيئة (Environment Card) — الأهمّ
لكلّ أنبوب تداول تعريف بيئته:
```
EnvironmentCard
├── environment_id        (هويّة الأنبوب)
├── account_id
├── terminal_id
├── broker
├── platform
├── credentials/reference (مرجع للسرّ لا السرّ نفسه — قاعدة ٨)
├── symbol_mapping        (خريطة رموز المنصّة ← الرمز الموحّد)
└── connection_identity   (مسار الجسر/الطرفيّة)
```
- **الأسرار تبقى خارج البطاقة** (قاعدة ٨): تحمل **مرجعًا** لا القيمة.
- **البطاقة غير مختومة** → تبديل الحساب/المنصّة = تعديل بطاقة، **بلا كسر ختم** (يحلّ «بدي أغيّر حساب ومنصّة»).
- `symbol_mapping` = المكان الطبيعيّ **لحصر الأصول المسموح تداولها** (يلامس مشكلة #1 «التحكّم بالأصول»).

## ٢. عقد هويّة الأحداث
كلّ حدث تنفيذ/حساب يحمل الهويّة **بالمستوى الأعلى** لا بالآخر:
```
Event
 ├── account_id
 ├── environment_id
 ├── timestamp
 └── payload
```
فحين يصل `execution.confirmed` → **لا سؤال «تابع لأيّ حساب؟»** — الجواب في الحدث.
- **⚠️ قيد النواة المختومة (حقيقة تقنيّة):** الطريقة المثاليّة = **الناقل يورّث الهويّة** (قاعدة ٢٢، مثل `trace_id`) — لكن **النواة مختومة V1.8.0، لا تُفتح** (قرارك). فالحلّ **بلا تغيير ناقل:** كلّ **مصدر يبصم `account_id`/`environment_id` بأعلى حدثه، والناشرون يمرّرونها** — **نفس نمط إصلاح `timeframe` المُثبَت حيًّا.** (619 يبصم الحساب أصلًا · نعمّم النمط على مسار التنفيذ.)

## ٣. 651 — متتبّع الحسابات (مالك فصل الحسابات)
```
619 ─────┐
517 ─────┼──→ 651 ──→ portfolio.accounts_summary
609 ─────┘
```
- يجمّع لكلّ `account_id` من **مصادره القائمة** (619 حالة الحساب · 517 نتيجة · 609 مراكز).
- **لا يعيد حساب** الرصيد/الحقوق/P&L/`open_count` — **يقرأها، لا ينسخ الحقيقة.** (ممنوع 619→653 · 619→654 …)
- يملك **فصل حالات الحسابات** (كلّ حساب معزول).

## ٤. 650 — مدير المحفظة (تجميع فقط)
```
Account A ─┐
Account B ─┼──→ 650 ──→ Portfolio View
Account C ─┘
```
يُخرج: إجمالي رأس المال · إجمالي الانكشاف · المخاطر المجمّعة · الأداء · ملخّص لكلّ `account_id`.
- **لا يقرّر BUY/SELL · لا يغيّر استراتيجيّة حساب · قراءة فقط** (طبقة المحفظة، قاعدة ٢٣).

## ٥. العزل (طبقة التنفيذ لكلّ أنبوب)
```
ACCOUNT_A:  618-A → 619-A → 601-A → Execution-A
ACCOUNT_B:  618-B → 619-B → 601-B → Execution-B
ACCOUNT_C:  618-C → 619-C → 601-C → Execution-C
```
- **ممنوع:** A decision → B execution · A risk → B risk · A position → B position.
- **نقطة اللقاء الوحيدة:** A/B/C → 651 → 650.
- **🔴 سؤال تشغيليّ مفتوح (يحسمه القرار):** كيف تعمل عدّة أنابيب والنواة تحمّل **نسخة واحدة لكلّ `id` ذرة**؟
  - (أ) **حالة مفتاحها `account_id` داخل نفس الذرات** (رؤية ٠٠ السياق: «خيوط داخل نفس الذرات، لا نسخة لكلّ حساب») — عملية/ناقل واحد.
  - (ب) **نسخة ذرة لكلّ أنبوب** (قاعدة ٢٣: «نسخة لكلّ طرفيّة/حساب») — تحتاج آليّة تعدّد نسخ (قد تلمس النواة = مختومة).
  - **التوتّر:** ٠٠ يقول (أ) · قاعدة ٢٣ تقول (ب) · والنواة المختومة تحمّل نسخة واحدة/`id`. **يُحسَم في هذه الورقة — قرارك.** (العزل مضمون بالحالتين ما دام مفتاح `account_id` صارمًا؛ الفرق تشغيليّ.)

## ٦. 610 — تيليجرام (ليس «مخًّا ثانيًا»)
```
Telegram → 610 → 901 Command Gateway → النظام المخوّل
```
- 610: يستقبل أمر تيليجرام · يتحقّق من النوع/الصلاحيّة حسب العقد · يرسله لـ**901** · يبثّ المسموح فقط.
- **لا يتجاوز 901 · لا يكتب مباشرة في 601/618/619.** السلطة التنفيذيّة **مكان واحد (901).**

## ٧. ما لا يُبنى (قرار المالك — يكرّر مصادر قائمة)
- **652** (حالة الاتصال تندمج في 651) · **653–657** (رصيد/حقوق/هامش/هامش حرّ/مستواه) · **658** (P&L) · **659** (الصفقات المفتوحة).
- كلّها **619/517/609 توفّرها** → بناؤها نسخ مكرّرة من نفس الحقيقة (فخّ 513: لا نبني بالاسم).

## المكوّنات الجوهريّة النهائيّة (٥)
1. **بطاقة البيئة** — هويّة وعزل كلّ حساب. 2. **651** — تتبّع الحسابات وفصل حالاتها. 3. **650** — تجميع المحفظة والرؤية. 4. **610** — واجهة تيليجرام. 5. **901** — بوّابة الأوامر (قائمة، تبقى السلطة المركزيّة).

## الملفّات التي تُمَسّ (تقدير — يُثبَّت بعد الاعتماد)
- **جديد:** بطاقة البيئة (ملفّ إعداد + آليّة قراءة) · 651 · 650 · 610.
- **يُعدَّل (تمرير الهويّة top-level):** مسار التنفيذ/الحساب (619/601/609/517/551/552/563 …) — **دفعة نمط `timeframe`.**
- **يُعاد استعماله:** 901 (بوّابة الأوامر) · 619/517/609 (مصادر 651).
- **لا يُمَسّ:** النواة (مختومة) · 350 (فايبل).

## المخاطر والتراجع
- **الخطر الأكبر:** تمرير الهويّة جزئيًّا (بعض الأحداث بحساب وبعضها بلا) → خلط حسابات. **الوقاية:** دفعة واحدة + فحص AST (مثل timeframe) + بوّابة فحص قبل الاكتمال.
- **تراجع:** كلّه ذرات جديدة + تمرير حقل (غير كاسر للحساب الواحد الحاليّ) → يُعزَل ويُتراجَع عنه.
- **الحساب الواحد الحاليّ يبقى شغّالًا** طوال البناء (الهويّة الإضافيّة لا تكسره).

## 🔴 القرارات التي تحسمها الورقة (تحتاج جوابك قبل الكود)
1. **نموذج التشغيل:** (أ) حالة مفتاحها `account_id` داخل نفس الذرات · أم (ب) نسخة أنبوب لكلّ حساب؟ (البند ٥.)
2. **الحسابات المتعدّدة عمليًّا:** طرفيّات MT5 منفصلة؟ نفس البروكر أم مختلف؟ (يحدّد `terminal_id`/مسار الجسر لكلّ أنبوب — ولكلّ طرفيّة جسر `nq_brain.db` خاصّ أم مشترك؟)
3. **حصر الأصول (#1):** هل `symbol_mapping` في البطاقة هو مكان **القائمة البيضاء للرموز المسموح تداولها**؟
4. **الأسرار:** أين مخزن الأسرار الذي تشير إليه `credentials/reference`؟ (النواة/الذرة جاهلتان — قاعدة ٨.)

## بوّابة الفحص ٢ (الإثبات الحيّ — بعد الاعتماد والبناء)
حسابان (أو حساب + بيئة ثانية) يعملان معزولَين · `execution.confirmed` يحمل `account_id` صحيحًا · 651 يُخرج `accounts_summary` لكليهما · 650 يجمّع الإجمالي · **المالك يرى ذلك بعينه.**

---
**الخطوة الآن:** المالك يراجع + يجاوب القرارات الأربعة → أُحدّث الورقة → **يعتمد** → أبني (بلا كود قبل الاعتماد). لا نواة · لا 350 · لا حوكمة.
