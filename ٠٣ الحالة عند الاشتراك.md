# ورقة بناء — النواة ٠٣: الحالة عند الاشتراك (Last-Value Replay)

> **تصحيح تسمية (رأي ثالث):** هي **ليست "نبضة" (Heartbeat)** — لا إرسال دوري. بل **إعادة
> آخر حالة عند الاشتراك فقط** (Last-Value Replay).

**نوع الورقة:** تشريح تنفيذ كامل — يُجاب على **كل سؤال** فيه **قبل** أي سطر كود.
**التاريخ:** 2026-08-03 · **الوحدة:** النواة ① · الجزء ٠٣ · **الحالة:** ✅ **مُعتمَدة (مقفولة)** — بعد رأي ثالث + تصفية المالك · القاعدة: حالة تُعاد · أمر لا
**يمسّ النواة** (جزء من الختم V2.0). **هذا الجذر #١ — أخطر وأهمّ جزء، ومنه ترجع الصفقات.**
**✅ منفّذة ومختومة — Core V1.8.0. ووُسّعت بورقة ١١:** المشترك على الكل (firehose) ياخد آخر حالة لكل تدفّق عند الاتصال (اللوحة ما تفضى)، + **حارس أوامر** يمنع إعادة أي أمر (تنفيذ مزدوج). التفاصيل: ورقة ٠٨.

> طريقتنا: من الجذر لا ترقيع · نرفع لا نبسّط · العزل/الدقة/الجودة خطوط حمر · **بسيط، لا
> مبالغة** · لا كود قبل ما تُجاب أسئلة القسم (٨) وتقول "نفّذ".

---

## ١. ما هي الوحدة (بلغة بسيطة)
اليوم: الذرة اللي **تشترك متأخّرة** (أو تُعاد تحميلها) **تفوتها الحالة الأخيرة للأبد** —
لأن الناشر نشرها مرّة وسكت. هاد قتل ٤ أشياء بنفس الجذر: **619→651→413 (توقّف الصفقات)**
· أوامر تلجرام · أزرار اللوحات.

**الحلّ من الجذر — بالناقل، مرّة وحدة لكل النظام (لا ترقيع كل ذرة):** الناقل **يحتفظ بآخر
قيمة لكل حدث**، ويسلّمها **فورًا لأي مشترك جديد** لحظة اشتراكه. فالمتأخّر ياخد الحالة
الحالية على طول.

## ٢. الهدف و"ليش"
- **يحلّ الجذر #١ اللي بيقتل ٤ أعراض** — بمكان واحد (الناقل)، مو بترقيع ٦١٩ ولا غيرها.
- **يرجّع الصفقات كحلّ جذري:** ٦٥١ ياخد حالة الحساب → لقطة → ٤١٣ يمسك الحسابات → الإشارات
  تمشي → صفقة تنفتح.

## ٣. الموجود نبني عليه (قِسته بالكود — `core/event_bus.py`)
- `subscribe(event_name, handler, subscriber)` — مكان تسجيل المشترك الجديد.
- `publish(event_name, payload)` — يبني `base` (الحمولة + الحقول المحقونة). **هون نلتقط آخر قيمة.**
- اليوم: **لا احتفاظ** — المشترك الجديد ياخد **الأحداث المستقبلية فقط.**

## ٤. التشريح الدقيق — شو راح أعمل بالضبط (بسيط)
**ملف يُمسّ: `core/event_bus.py` فقط.**
1. **في `__init__`:** `self._last_event: dict[str, dict] = {}` — **آخر حدث كامل** لكل اسم
   (سمّي `_last_event` لا `_last_value` — تصحيح الرأي الثالث: `base` حدثٌ كامل بطابعه
   ومصدره وأثره، لا مجرّد حمولة).
2. **في `publish`:** بعد بناء `base` مباشرة، `self._last_event[event_name] = base` —
   **وقت النشر، لا بعد نجاح التسليم** (Replay = آخر حالة **منشورة**، لا آخر حالة **وصلت**).
   حدث واحد لكل اسم — ذاكرة تافهة.
3. **في `subscribe`:** بعد التسجيل، إن وُجد حدث محفوظ (وكان **قابلًا للإعادة** — انظر ٤)،
   **سلّمه فورًا للمعالِج الجديد عبر نفس `_invoke` بالكامل** (مهلة · عزل · عدّادات) — **بلا
   أي مسار خاص** (شرط الرأي الثالث: مسار واحد لا مساران).
4. **قابل للإعادة أم لا — بمعيار معماري واضح (لا قائمة أسماء يدوية):**
   - **حدث "حالة" (State)** — حقيقة ثابتة (حالة حساب · سعر · هيكل) → **يُعاد.**
   - **حدث "أمر" (Command)** — فعل يُطلق (تنفيذ · شراء/بيع) → **لا يُعاد** (إعادته = تنفيذ ثاني).
   - القرار بطبيعة **الحدث** لا برغبة المشترك (مركزي، عند مصدر الحدث). تصنيف صريح مركزي.
5. **هويّة الحدث المعاد (حاسمة — حزمة مع ٠٤):** الإعادة **تحافظ على الحدث الأصلي كما هو** —
   **نفس `event_id` و`trace_id`** والطابع والمصدر (تسليم متأخّر للأصل، لا نشر جديد). فلا
   يُحسب `published`، بل `replayed`. **وهذا متوافق مع ٠٤ (المعرّفات الثلاثة)** — فالشجرة
   السببية ما تنكسر عند الإعادة.

**بسيط عمدًا:** لا retention policy ولا TTL ولا per-subscriber — آخر حدث حالة يُعاد
للمشترك الجديد، وبس.

## ٥. فحص الخطوط الحمر
- **جاهلة:** الناقل يحتفظ بحمولة خام ويعيدها — بلا فهم معناها. ✓
- **العزل:** ما يتغيّر — لسا تواصل بالأحداث فقط. ✓
- **رفع لا تبسيط:** يضيف متانة (المتأخّر يشتغل) بلا تغيير التوزيع القائم. ✓

## ٦. الإثبات الحيّ (وهاد الأهمّ — تشوفه بعينك)
- **الاختبار الحاسم على النظام الحيّ:** بعد التغيير، ٦٥١ لازم ياخد حالة الحساب (مع إنه
  يُقلع بعد ٦١٩) → ينشر لقطة → ٤١٣ يمسك الحسابات → الإشارات تمشي → **صفقة تنفتح على الديمو.**
  **هاد إثبات الجذر #١: التداول رجع كحلّ جذري، لا رقعة.**
- **اختبار وحدة:** انشر حدث X (حالة) بقيمة V، بعدين اشترك معالِج جديد على X → ياخد V فورًا.
- **اختبار الأمر:** حدث "أمر" → المشترك الجديد **ما ياخد** القيمة القديمة.
- **اختبار الاشتراك قبل أي نشر (إلزامي — من الرأي الثالث):** اشترك على حدث **لم يُنشر بعد**
  → **ما يصير شي** (لا إعادة لحدث غير موجود).
- **اختبار الهويّة:** المُعاد يحمل **نفس `trace_id`** الأصلي، ويزيد `replayed` لا `published`.

## ٧. المخاطر والتراجع
- **يمسّ `publish` و`subscribe` (مسارات ساخنة) — خطر منخفض-متوسط (لا صفر):** يُكتب بحذر،
  ويُثبَت بالاختبارات قبل أي شي.
- **الذاكرة:** حمولة وحدة لكل حدث — تافهة بمقياسنا.
- **التراجع:** git (م٠). والختم يُعاد V2.0 بعد اكتمال أجزاء النواة كلها.
- **دقّة:** إعادة التسليم لازم تمرّ بنفس عزل/مهلة `_invoke` — ما نكسر ضمانات المادة 89.

## ٨. الأسئلة — مصفّاة (التقني على كلود · وسؤال المالك بسيط)

**قرارات تقنية حسمها كلود (مصفّاة + رأي ثالث · لا تلزم المالك):**
- **معيار الإعادة:** بطبيعة الحدث — **حالة (State) تُعاد · أمر (Command) لا** — تصنيف
  مركزي عند مصدر الحدث، لا قائمة أسماء يدوية ولا رغبة مشترك.
- **الهويّة:** المُعاد = **الحدث الأصلي كما هو** (نفس `trace_id`)، تسليم متأخّر لا نشر
  جديد → يزيد `replayed` لا `published`. **متوافق مع ٠٤.**
- **المسار:** الإعادة عبر نفس `_invoke` بالكامل، بلا مسار خاص.
- **التخزين:** وقت النشر، لا بعد التسليم. **الترتيب:** بعد ٠٢.

**للمالك (بلغة بسيطة) — سؤالك الوحيد:**
> **"الحالات" (حالة الحساب · السعر) تُعاد للمتأخّر — وهاد اللي بيرجّع الصفقات.**
> **و"الأوامر" (شراء/بيع) ما تتكرّر أبدًا — خط أمان.**
> **هاي القاعدة اللي بدك ياها، صح؟**

## ٩. قرار المالك
☐ نفّذ كما هو  ·  ☐ عدّل (اكتب)  ·  ☐ أوقف / غيّر

---
**التالي بعد اعتمادها:** ٠٤ وراثة الأثر (trace_id/parent_event).
