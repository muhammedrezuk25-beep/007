# دورة حياة النظام — التصميم الكامل (من أول Tick لآخر إغلاق)

> **رؤية المالك (محمد رزوق) · 2026-08-04.** الهدف: نظام تداول احترافي يدعم **عدة حسابات · عدة رموز ·
> عدة صفقات · ذكاء يتعلّم** — **بلا إعادة بناء لاحقًا.** الشرط: البنية مخبوزة بالهوية من البداية.
> **هاي المرجع اللي كل ذرة تتبنى عليه.**

---

## المبدأ الحاكم: الهوية على كل حدث
عشان ما نعيد التصميم عند التوسّع، **كل حدث يحمل مفاتيح هويّته:**
| المفتاح | على أي حدث | ليش |
|---|---|---|
| `account_id` | أي حدث يمسّ حساب/مركز/أمر/مخاطر/محفظة | تعدّد الحسابات — لا خلط |
| `symbol` | أي حدث يمسّ رمزًا | تعدّد الأزواج |
| `ticket` | أي حدث يمسّ صفقة | تعدّد الصفقات |
| `strategy_id` | أي حدث من/عن استراتيجية | تعدّد الاستراتيجيات |
| `model_id` · `model_version` · `confidence` · `prediction` | أي حدث تحليل/تداول مرتبط بالذكاء | معرفة أي نموذج قرّر · قياس أدائه · التراجع عنه |

---

## دورة النظام — ١٨ مرحلة

**١ — استقبال السوق:** مصادر (MT5 · Binance · Yahoo · أخبار · تقويم · Order Book · Volume) →
`market.tick` · `market.depth` · `market.news` · `market.calendar` · `market.volume`.

**٢ — توحيد البيانات:** كل مصدر → شكل موحّد `{account_id, provider, symbol, bid, ask, spread, timestamp}`.

**٣ — بناء حالة السوق:** لكل رمز `State` مستقل (NQ100 · EURUSD · XAUUSD · BTCUSDT…) فيه:
`trend · volatility · session · liquidity · volume · strength`.

**٤ — التحليل:** كل محلّل مستقل (Trend · Momentum · Volume · Correlation · Structure · News · AI)
→ يجتمعوا بـ`analysis.consolidated`.

**٥ — الاستراتيجيات:** كل وحدة مستقلة (Breakout · Pullback · Mean Reversion · News · Scalping · AI)
→ `strategy.signal`.

**٦ — تقييم الإشارات:** دمج `confidence + AI score + market state + risk state` → اختيار أفضل إشارة.

**٧ — إدارة المخاطر (لكل حساب، لا رمز):** لكل حساب: `Daily Loss · Drawdown · Exposure · Margin ·
Free Margin · Equity · Balance`. فشل أي شرط → **رفض الصفقة**.

**٨ — توزيع رأس المال:** لكل حساب → لكل استراتيجية → لكل رمز → لكل صفقة (مثال: حساب A ١٠٪ · استراتيجية ٤ ٤٠٪ · NQ → حجم).

**٩ — بناء الأمر:** القرار → `BUY/SELL · SL · TP · Volume · Deviation · Comment · Magic`.

**١٠ — التنفيذ (لكل حساب Bridge مستقل):** حساب١→MT5#1 · حساب٢→MT5#2 · حساب٣→MT5#3. **الحسابات ما تشوف بعضها.**

**١١ — التأكيد:** MT5 يرجّع `ticket · price · volume · time` → `execution.confirmed`.

**١٢ — مراقبة الصفقة (لكل Ticket):** `Profit · Loss · Duration · MAE · MFE · Trailing · Break Even`.

**١٣ — الإغلاق:** SL / TP / Manual / AI Exit / Time Exit / Risk Exit → `market.outcome.realized`.

**١٤ — التعلّم:** يستقبل (Signal · Market · Indicators · News · Decision · Entry · Exit · Profit · Loss ·
Duration · Account · Strategy · Symbol) → يحسب: هل التوقّع صحّ؟ ليش؟ الثقة؟ دقّة النموذج؟

**١٥ — التدريب:** كل فترة (١٠٠ / ٥٠٠ / ١٠٠٠ صفقة) يعيد التدريب → `model.updated`.

**١٦ — تقييم النموذج:** قديم vs جديد → الأفضل `Activate` · الأسوأ `Rollback`.

**١٧ — التخزين:** يحفظ كل شي (أحداث · صفقات · نماذج · قرارات · تحليلات · أخطاء · إحصائيات) —
**لا يحذف إلا بسياسة احتفاظ محدّدة.**

**١٨ — المراقبة:** صحة كل ذرة · حالة كل حساب · حالة كل نموذج · الربح/الخسارة · زمن التنفيذ · التأخير ·
الذاكرة · المعالج.

---

## الخلاصة
بُنِيَ هيك من البداية → **ما نحتاج إعادة تصميم** عند الانتقال من حساب لعدة حسابات، أو من رمز لمئات
الرموز، أو عند إضافة نماذج ذكاء جديدة. **الهوية على كل حدث = مفتاح التوسّع بلا كسر.**
