# قسم ٣٠٠ — الإحصاء (Statistics)

> **الحالة:** ✅ **كامل — ١٨/١٨ ذرة · مدقّق ٠ · اختبارات خضراء.** (بُني 2026-08-06.)
> **المالك:** محمد رزوق · **المرجع الأمّ:** `سياق/حالة بناء الذرات.md` · `سياق/قالب الذرة.md`.
> **الفحص:** Gate-1 (مدقّق+اختبار) تمّ للكلّ. **Gate-2 الحيّ:** الجزء الأول (١٢) مُثبَت حيّ سابقًا ببثّ
> `/ws/events` · **الستّة الجديدة (307/314/315/316/318/300) تنتظر إعادة تشغيل المالك للنواة** (تُحمَّل عند الإقلاع).

## الغرض
عمود **إحصاء خام موضوعيّ** على إغلاقات الشموع لكل (رمز, فريم) — نافذة متدحرجة، كل ذرة تنشر
`stats.<x>.state` بالعقد الموحّد، والمدير يجمّعها لقطةً واحدة لكل دورة. **يُغذّي قسم 350 (الاحتمالات).**
لا رأي تداوليّ هنا — أرقام فقط (المعنى التداوليّ في 350+).

## الوحدات (١٨) — بلا دمج أي قطعة (قرار المالك «لا تدمج شي»)
| # | الذرة | المخرج `stats.*` | الإشارة |
|---|---|---|---|
| 301 | المتوسط | `.mean` | computed (metadata.value) |
| 302 | الوسيط | `.median` | computed |
| 303 | المنوال | `.mode` | mode / uniform |
| 304 | الانحراف المعياري | `.std` | computed (عيّنة n−1) |
| 305 | التباين | `.variance` | computed (عيّنة n−1) |
| 306 | معامل الاختلاف | `.cv` | computed (std/|mean|) |
| **307** | **الارتباط الإحصائي** | `.correlation` | strong/moderate/weak/reference (بيرسون على العوائد مقابل مرجع USTEC) |
| 308 | الانحدار الخطّي | `.regression` | rising/falling/flat |
| 309 | معامل التحديد r² | `.r_squared` | strong_fit/weak_fit |
| 310 | درجة Z | `.zscore` | extreme/normal |
| 311 | الالتواء | `.skewness` | right/left/symmetric |
| 312 | التفلطح | `.kurtosis` | heavy/light/normal |
| 313 | كاشف الشذوذ | `.outlier` | outlier/normal (IQR) |
| **314** | **التوزيع** | `.distribution` | peak/body/tail (مدرّج تكراري) |
| **315** | **المدى** | `.range` | at_high/at_low/mid (min/max/range+موقع) |
| **316** | **جودة البيانات** | `.quality` | clean/degraded (**تعدّ الخبيث، لا تُسقطه** — حقيقة خام متل 164) |
| **318** | **مقارنة الفترات** | `.period_compare` | rising/falling/stable + vol expanding/contracting |
| **300** | **مدير الإحصاء** | `stats.cycle.collected` | نمط B — يجمّع ١٧ وحدة لكل دورة |

## تدفّق الأحداث
`market_data.candle_closed` → كل وحدة (301..316,318) تحسب على نافذتها وتنشر `stats.<x>.state`
→ **300 المدير** يجمّعها بـ`cycle_id` (اكتمال ١٧ أو مهلة `SYS_SECOND`) → `stats.cycle.collected` → **350**.
307 يحتاج رمزًا مرجعيًّا (USTEC) ويحاذي العوائد بالتداخل الزمني.

## قرارات هندسيّة (كلها معلَنة بالمانيفست · قابلة للضبط من اللوحة)
- **انحراف/تباين = عيّنة (n−1)** — المعيار الإحصائيّ (سؤال مؤجّل: تفضّل المجتمع n متل بولنجر؟).
- نافذة افتراضيّة **20** · عتبات: z=2 · skew=0.5 · kurt=1 · IQR=1.5 · 307 strong=0.7/moderate=0.4 ·
  314 bins=10/tail=0.25 · 315 high=0.8/low=0.2 · 316 min_quality=0.95 · 318 drift=0.001/vol=0.1.
- **لا مخزن إحصاء بعد** — الوحدات تنشر والمدير يجمّع؛ 350 يستهلك مباشرة. (سؤال مؤجّل: نضيف مخزن 700؟)
- **307↔160:** الاثنان بيرسون على العوائد — **منفصلان بعمودين** (160 تحليل→166 · 307 إحصاء→300/350)،
  نفس مبدأ فصل 304/305. سؤال مؤجّل: تفضّل 307 يستهلك 160؟ (بورقة الأسئلة المؤجّلة.)

## مين يقرا المخرجات
- **350 الاحتمالات** (المستهلك الأساس) — يقرأ `stats.cycle.collected` (لقطة موحّدة) أو أحداثًا مفردة.
- جودة 316 المنخفضة → 350 يخفّض الثقة بكلّ إحصاء ذاك الرمز (لا قرار على بيانات متعفّنة).

## عزل ووقت (قواعد 7 · 13)
كل وحدة نافذة لكل (رمز, فريم)، تحسب بنفسها (تكرار متعمَّد للعزل — لا استيراد ذرة أخرى). المدير لا يقرأ
الساعة؛ «الآن» والمهلة من `official_time` بنبضة `SYS_SECOND`.
