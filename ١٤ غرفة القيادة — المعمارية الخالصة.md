# ورقة بناء — الحوكمة ١٤: غرفة القيادة — المعمارية الخالصة

**نوع الورقة:** مرجع معماري **مستقلّ عن التقنية** — طويل العمر، لا يرتبط بأداة.
**التاريخ:** 2026-08-06 · **الطبقة:** الحوكمة (طبقة ٢) · **الحالة:** مسودة للمراجعة.
**تَخلُف:** `١١`/`١٢` (متجاوَزتان) · **تبني على:** `٠٩` (تصميم الحوكمة) + النواة **V1.8.0 المختومة**.
**قرينتها:** `١٥ غرفة القيادة — التنفيذ` (كل اختيار تقني/مكتبة/أداة/أرقام أداء **هناك**، لا هنا).

> **أُعيد هيكلتها (2026-08-06) بعد مراجعة خارجية:** فُصل التصميم عن التنفيذ. هذه الورقة **مبادئ ومعمارية
> فقط**؛ لا اسم Framework، لا اسم مكتبة، لا أرقام FPS، لا أدوات بناء. مقياس المالك يبقى: **أعلى مستوى، لا
> تبسيط** («نرفع لا نبسّط»)؛ وهي **أهمّ طبقة** — نافذة المالك للتحكّم والرؤية والقرار.

---

## ٠. القيود المعمارية (ثابتة — تُحكَم داخلها كل قطعة)

1. **عملية منفصلة، عزل صارم.** الحوكمة تقرأ النواة عبر شبكة محلية فقط (بثّ + استعلام). **لا تستورد كود
   النواة، ولا تدخل جوّا ذرة.** حذفها لا يغيّر حرفًا في النواة/الذرات، والتداول يكمل بدونها.
2. **الحقيقة في النواة، لا في اللوحة.** الحوكمة **بلا حالة مخزّنة** (لا DB، لا تاريخ محفوظ). تقرأ الحيّ،
   وتستعلم التاريخ من مصدره. تموت وتعود → تقرأ من جديد.
3. **كل معروض يرجع لأصل خام.** لا رقم بلا أصل يُتتبَّع؛ **لا بيانات وهمية/محاكاة**. حيث لم يُبنَ العقل →
   يُعلَن **«غير مبنيّ بعد»** صراحةً، لا رقم مُختلق.
4. **العرض يُخرِج معنى، لا خامًا.** طبقة العرض تُظهر **معنى مترجَمًا**، لا حالات خام ولا مصطلحات نيّئة
   (طبقة الترجمة مكوّن معماري — قسم ٩).
5. **مستقلّة عن التقنية.** أي Framework/مكتبة/أداة **قابلة للاستبدال** ما دامت تحقّق عقود هذه الورقة
   (خاصة عقد مسار الرسم — قسم ٦). الاختيارات الفعلية في ورقة `١٥`.

---

## ١. الفكرة
**ليست لوحة — بل محرّك تُبنى فوقه كل اللوحات.** كل قسم يصير **Widget** فوق نفس المحرّك، بنفس مسار
البيانات ونفس مسار الرسم. باب واحد (تطبيق واحد)، مصدر حقيقة واحد (النواة)، مسار رسم واحد.

---

## ٢. الأهداف المعمارية (مبادئ — بلا أرقام)
- **باب واحد** يجمع الرؤية والتحكّم والقرار.
- **يعمل ساعات** بلا بطء ولا تسريب ذاكرة · **لا يتجمّد** تحت تدفّق كثيف · **لا يعيد رسم الصفحة كاملة**.
- **يدعم عدة شاشات** بتخطيط متكيّف.
- **قابل للتوسّع** — أي قسم/Widget جديد يُركَّب فوق نفس المحرّك بلا إعادة تفكير بالأداء.

---

## ٣. عقد التكامل مع النواة (سطح القراءة والتحكّم)
النواة تقدّم — والحوكمة تستهلك — **حقائق خام فقط**:
- **بثّ شامل (Firehose):** كل حدث خام عند نشره، عبر قناة واحدة، محكوم بطابور إسقاط عند الفيض.
- **إعادة عند الاشتراك:** المشترك الجديد يأخذ **آخر حالة لكل تدفّق فورًا** (لا فراغ عند الفتح)؛
  **الأوامر لا تُعاد** (خطّ أمان ضد التنفيذ المزدوج).
- **استعلام (لقطات/تاريخ):** قوائم الذرات · المقاييس · الجورنال · تقرير الإقلاع · الصحة.
- **عدّادات الناقل:** نُشر/سُلّم/بلا مشترك/مهلة/خطأ/أُعيد (محاسبة الإسقاط).
- **شجرة الأثر:** معرّف الرحلة/الحدث/الأب (سلسلة سببية).
- **خُطّافات التحكّم:** إيقاف/تشغيل/إعادة فحص ذرة (الميكانيكا بالنواة، القرار بالحوكمة).

---

## ٤. Layout Engine — شكل الشاشة (البنية الفعلية)

مناطق ثابتة، والمنطقة المتغيّرة وحدها هي `Workspace`. **بلا Sidebar** يأكل العرض (قرار المالك) —
التنقّل **شريط علوي أفقي**، والسياق الفرعي (لو لزم) **داخل** `Workspace`:

```
┌──────────────────────────────────────────────────────────┐
│ Header      هوية غرفة العمليات · الساعة · اتصال · إيقاف طارئ │  ← ثابت
├──────────────────────────────────────────────────────────┤
│ Navigation  شريط أقسام أفقي (الرئيسية · الذرات · التداول …) │  ← ثابت
├──────────────────────────────────────────────────────────┤
│ Workspace   القسم الحالي فقط (شبكة Widgets)                │  ← متغيّر
│             [قد يحوي شريط سياق فرعيًّا داخله عند اللزوم]      │
├──────────────────────────────────────────────────────────┤
│ Status Bar  آخر تحديث · طزاجة عامة · اتصال النواة           │  ← ثابت
└──────────────────────────────────────────────────────────┘
```

- **مناطق التخطيط شبكة (Grid) متكيّفة:** تملأ حين يتّسع المكان، وتنزل بلطف حين يضيق — **بلا انكسار**
  على أي شاشة (لا اعتماد كامل على وحدات النافذة المطلقة).
- **الـ`Workspace` وحده يتبدّل** عند تنقّل الأقسام؛ الهيدر/الشريط/الستاتس ثابتة لا تُعاد بناؤها.

---

## ٥. Widget Architecture — قلب غرفة القيادة

كل شاشة = مجموعة **Widgets** فوق المحرّك. الـWidget وحدة عرض مكتفية بعقد واضح:

**① العقد (Contract):** كل Widget يعلن:
- **مدخلاته:** أسماء التدفّقات (بادئات أحداث) + الاستعلامات التي يقرأها — **من طبقة البيانات المشتركة، لا
  يجلب لحاله اتصالًا خاصًّا**.
- **مخرجه:** دالة **رسم نقية** ترسم في **منطقته المخصّصة فقط** (لا تلمس بقية الشاشة).
- **مصدر طزاجته:** أي تدفّق يحدّد «حيّ/متأخّر/مقطوع» له.

**② دورة الحياة (Lifecycle):**
```
Register → Mount → Activate ⇄ Deactivate → Dispose
```
- **Register:** يُسجَّل ضمن قسمه (خريطة قسم → Widgets).
- **Mount:** يُخصَّص له مكان + يشترك على تدفّقاته عبر الطبقة المشتركة.
- **Activate:** مرئي → الرسم مشتغل.
- **Deactivate:** مخفيّ → **الرسم معلَّق، والاشتراك باقٍ** (حالته تبقى طازجة).
- **Dispose:** يلغي الاشتراك ويحرّر موارده (Canvas/مؤقّتات) — عند إزالة القسم أو إغلاق التطبيق.

**③ التركيب/الإزالة (Registration/Disposal):** القسم يعلن Widgets؛ المحرّك يركّب widgets القسم الفعّال
ويعطّل الباقي؛ الإزالة تطهير كامل (لا اشتراك متروك، لا Canvas متسرّب).

**④ الرسم (Rendering):** كل Widget يرسم **أمريًّا في منطقته** (خارج شجرة تفاعل الإطار — قسم ٦)، لا يعيد
بناء غيره. تحديث Widget واحد **لا** يمسّ الباقي.

---

## ٦. طبقة البيانات + مسار الرسم (العقد الحاكم للأداء — مبدأ لا أرقام)

**طبقة بيانات موحّدة:** اتصال بثّ **واحد** + عميل استعلام؛ **حالة لحظية في الذاكرة فقط** (آخر قيمة لكل
تدفّق)، تشترك عليها كل الـWidgets **اشتراكًا دقيقًا** (كلٌّ يستيقظ لما يخصّه فقط).

**عقد الرسم (ثابت، أيًّا كانت التقنية):**
```
حدث → طابور (تجميع) → إطار عرض واحد → رسمة واحدة
```
- **القاعدة الحاكمة: لا يُعاد الرسم مع كل حدث.** مهما تعدّدت الأحداث في الإطار الواحد → **رسمة واحدة**.
- **البيانات عالية التردّد خارج شجرة تفاعل الإطار** — تحديث أمري/Canvas مباشر؛ الإطار للهيكل والتنقّل فقط.
- **رسوم كثيفة → Canvas** (لا عناصر DOM تنفجر). **جداول كبيرة → Virtualization** (يُرسَم المرئي فقط).
- **الرسم مربوط بميزانية الإطار**؛ ما يتجاوزها = مشكلة تُقاس (القيم العددية في ورقة `١٥`).

---

## ٧. إدارة الأداء بين الصفحات

عند فتح قسم، ماذا يحصل لـWidgets الأقسام الأخرى؟
- **الاشتراك بالبيانات يبقى** (حالتها تبقى طازجة عبر البثّ الواحد + الحالة اللحظية المشتركة).
- **الرسم يُعلَّق** للمخفيّ (لا رسم لما لا يُرى) → صفحة مخفيّة كلفة رسمها ~صفر.
- **عند العودة للقسم:** يرسم **حالته الحاليّة فورًا** من الحالة اللحظية — بلا إعادة جلب، بلا فراغ.
- **التدفّق العالي المتراكم للمخفيّ يُسقَط الأقدم** (طابور محكوم) — لا ذاكرة تنمو، لا رسم متأخّر يُدفَع دفعة.

**النتيجة:** عشرات الأقسام ممكنة؛ المرئي وحده يرسم، والكل يبقى صحيحًا وطازجًا.

---

## ٨. نظام الصلاحيات والسلطة

حتى بمستخدم واحد، **المعمارية تميّز ثلاث درجات** (لا تُخلَط):
| الدرجة | أمثلة | البوّابة |
|---|---|---|
| **قراءة** (افتراضي) | عرض/مراقبة/سجل | بلا تأكيد |
| **كتابة** | تعديل إعداد ذرة | تأكيد + تحقّق من القيمة |
| **أوامر خطرة** | إيقاف · حذف · Backup · Freeze · إيقاف طارئ | **تأكيد صريح** + تمييز بصري |

- **كل كتابة/أمر تمرّ ببوّابة واحدة**، بالدرجة المناسبة، وتُنادي خُطّافات النواة.
- **تُسجَّل بجورنال النواة** (الحوكمة بلا ذاكرة → الأثر بالنواة: مين، شو، إيمتى).
- **اللوحة وأي واجهة أخرى (تلغرام لاحقًا) → نفس البوّابة** (السلطة بمكان واحد).
- **تعديل الإعداد = نموذج عربي، لا محرّر JSON:** لكل ذرة نموذج **يُولَّد من إعداداتها المعلَنة** (قالب
  الذرة قاعدة ١١: نوع · حدّ مسموح · افتراضي) → **حقل عربي واضح لكل إعداد** («المهلة بالثواني» · «الحجم
  الأدنى»)، والقيمة الغلط تُرفض **قبل** الإرسال. **المالك لا يرى JSON ولا مانيفست.** (تصميم النموذج
  التفصيلي لكل ذرة = ورقة لاحقة.)
- **قراءة أولًا، تحكّم بعدها:** البوّابة ودرجاتها (الكتابة/الخطرة) تُبنى **بعد** ثبوت القراءة —
  **عدا تشغيل/إيقاف الذرة** (خُطّافان جاهزان بالنواة، وهما جوهر شاشة الذرات): يرافقان الشاشة مباشرة عبر
  البوّابة بتأكيد.

---

## ٩. طبقة الترجمة (مكوّن معماري رفيع)
- **دالة نقية:** `حقيقة خام → معنى معروض` (قابلة للاختبار، كل مخرج مشتقّ من أصل).
- **اسم الذرة يُشتقّ تلقائيًّا من مصدره** (لا خريطة يدوية تُنسى).
- **كل شاشة تحمل:** آخر تحديث + مؤشّر طزاجة + وصول للأصل الخام.
- **(السبب التشغيلي — لغة المالك — في ورقة `١٥`؛ هنا المكوّن فقط.)**

---

## ١٠. الخطوط الحمر (لا واحدة تُكسَر)
- **لا تكذب:** كل عنصر يرجع لأصل خام يُظهره.
- **مو مصدر الحقيقة:** بلا تخزين؛ تموت وتعود تقرأ من جديد.
- **الطزاجة صدق:** لا يُعرَض قديم كأنه حيّ.
- **لا وهمي:** صفر بيانات مخترعة؛ «غير مبنيّ بعد» صراحةً.
- **العزل محفوظ:** المراقبة من فوق (بثّ/استعلام)، لا دخول ذرة.

---

## ١١. الأقسام (خريطة معمارية — حقيقي حيث توجد داتا · صادق حيث لا)
**قرار:** النطاق 850–869 = **أقسام داخل التطبيق الواحد، لا ذرات** (كانت واجهات متخفّية كذرات). كل قسم =
مجموعة Widgets فوق المحرّك، مصدرها تدفّقات النواة لمجالها. حقيقي حيث توجد الداتا (الذرات · التداول · السوق ·
الاتصال · المراقبة · السجل · التنبيهات · التشخيص · التحليل جزئيًّا)، و**«غير مبنيّ بعد»** حيث لم يُبنَ العقل
(الاستراتيجيات · القرار · المخاطر · الاحتمالات · المحافظ حتى اكتمال الأنبوب).
- **«الرئيسية» تُبنى آخِرًا:** هي **تجميع** لبقية الأقسام (صحة + حساب + تنبيهات + اتصال بلمحة)، فتعتمد على
  اكتمالها — فلا تُبنى مبكرًا.

---

## ١٢. أسئلة معمارية مفتوحة (توصيتي أولًا · ثم رأي المراجعة · القرار النهائي بعد الآراء)

| السؤال | توصيتي الهندسية | البديل المقبول |
|---|---|---|
| **Sidebar** | بلا شريط جانبي — تنقّل علوي (لا يأكل العرض) | سياق فرعي داخل `Workspace` لأقسام قليلة عند اللزوم |
| **حبيبات الـWidget** | أصغر وحدة = «بطاقة/لوحة» مكتفية بـ**تدفّق واحد وعقد رسم واحد** | تجزئة أدقّ لقسم كثيف جدًّا |
| **الصلاحيات** | ٣ درجات + **«وضع قراءة فقط» عام** يقفل كل كتابة بضغطة (أمان رخيص) | ٣ درجات فقط |
| **إدارة المخفيّ** | تعليق الرسم + إبقاء الاشتراك **للكل** (موحّد وبسيط) | فصل كليّ للأقسام فائقة الثقل فقط |

**القرار النهائي في كل ما سبق: بعد آراء المراجعين.**

---

## ١٣. قرار المالك
☐ المعمارية صحّ، انتقل للتنفيذ (ورقة `١٥`)   ·   ☐ عدّل (اكتب)   ·   ☐ أوقف / غيّر

---
**ملاحظة:** هذه الورقة **معمارية خالصة، طويلة العمر**. اختيار Framework والمكتبات والأدوات وأرقام الأداء
وتفاصيل التشغيل → **ورقة `١٥`** (قابلة للتغيير بلا مسّ هذه المعمارية).
